# Pinned 2026-07-12 (Phase 1 fix จาก AUDIT.md ข้อ C5)
# เหตุผล: CI ติดตั้งใหม่จากไฟล์นี้ทุกรอบ — เวอร์ชันลอยทำให้ระบบพัง/ตัวเลขเพี้ยนเงียบ ๆ
# ห้ามอัปเกรดตัวใดโดยไม่รันเทสต์ก่อน โดยเฉพาะ yfinance (behavior เปลี่ยนบ่อย)
# หมายเหตุ: ตัด pandas-ta (เลิกพัฒนา, พังกับ numpy 2) → ใช้ ta ผ่าน analysis/ta_compat.py
#           ตัด google-genai (ไม่มีโค้ดเรียกใช้)
#
# 2026-08-07 (AUDIT_2026-08-06 ข้อ D4.1): เดิมไฟล์นี้ pin แค่ 28 ตัวจาก 134 ตัวที่ติดตั้งจริง
# คำอ้าง "pins every package" ใน CLAUDE.md จึงไม่จริง และตัวที่ผลิตตัวเลขลอยหมด
# (scipy 1.17.1, numba 0.66.0, llvmlite 0.48.0, starlette 0.41.3, urllib3 2.7.0)
# ⇒ สองรอบติดตั้งที่ควรให้ผลเท่ากันให้ตัวเลขต่างกันได้โดยไม่มี commit ไหนเปลี่ยน
# ตอนนี้ pin ครบทั้ง transitive closure ตามเวอร์ชันที่ชุดเทสต์ผ่านจริง (Python 3.12, linux)
# เพิ่ม dependency ใหม่ = ต้องเติมทั้งตัวมันและ transitive ของมันด้วย
# (tests/test_docs_and_deps.py จะแดงถ้ามีตัวไหนหลุด pin)

# ---------------------------------------------------------------------------
# dependency ตรง — ตัวที่โค้ดของโปรเจกต์ import เอง
# ---------------------------------------------------------------------------
numpy==1.26.4
pandas==2.2.3
yfinance==0.2.61
ta==0.11.0
vectorbt==0.26.2
# prophet: 1.1.6 + pandas 2.2 พังเฉพาะเส้นทาง extra regressors (crosstab duplicate labels)
# → forecaster ใช้แบบ univariate แทน (regressor อนาคตเดิมก็ถูก fill ค่าคงที่ = pseudo-informative)
# ห้ามอัป prophet เป็น 1.3.x: มันลาก numpy≥2.5 มาชน numba/vectorbt (ทดลองแล้ว 2026-07-18)
# cmdstanpy ต้อง pin: 1.3.0 เพิ่ม validation ที่ reject cmdstan ใน wheel ของ prophet
prophet==1.1.6
cmdstanpy==1.2.4
numpy-financial==1.0.0

matplotlib==3.9.2
plotly==5.24.1
streamlit==1.41.1
# pyarrow เดิมหลุดจากการ pin (มากับ streamlit) แล้วไหลถึง 25.0.0 — pin ค่าที่ติดตั้งจริงไว้กันไหลต่อ
# (25.0.0 + ตารางบางแบบเคย segfault ใน AppTest — เลี่ยง st.dataframe ในหน้า Technical ดู comment ใน app.py)
pyarrow==25.0.0

fastapi==0.115.6
uvicorn==0.34.0
websockets==14.1
python-multipart==0.0.20
pydantic==2.10.4
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
apscheduler==3.11.0
schedule==1.2.2

requests==2.32.3
python-dotenv==1.0.1
feedparser==6.0.11
praw==7.8.1
fredapi==0.5.2
reportlab==4.2.5

anthropic==0.42.0

# ---------------------------------------------------------------------------
# transitive — ไม่มีโค้ดของเรา import ตรง ๆ แต่ pip ติดตั้งมาให้ตัวข้างบน
# เวอร์ชันทั้งหมดคือค่าที่ติดตั้งอยู่ในสภาพแวดล้อมที่ชุดเทสต์ผ่าน (`pip check` สะอาด)
# ---------------------------------------------------------------------------
altair==5.5.0
annotated-types==0.8.0
anyio==4.14.2
asttokens==3.0.2
attrs==26.1.0
beautifulsoup4==4.15.0
blinker==1.9.0
cachetools==5.5.2
certifi==2026.7.22
cffi==2.1.1
# chardet: reportlab ต้องการแบบไม่ระบุเวอร์ชัน พอปล่อยลอยมันไหลถึง 7.4.3 แล้ว
# requests.check_compatibility (ซึ่งเช็คตัวที่ติดตั้ง ไม่สนว่ามาจาก extra ไหน) ยิง
#   RequestsDependencyWarning: urllib3 (2.7.0) or chardet (7.4.3)/charset_normalizer (3.4.9)
#   doesn't match a supported version!
# ทุกครั้งที่ import requests — requests ประกาศช่วงไว้เอง (chardet<6,>=3.0.2)
# ทดสอบ 5.2.0 ในคอนเทนเนอร์แล้ว: คำเตือนหาย · reportlab ยังสร้าง PDF ได้ · requests ปกติ
chardet==5.2.0
charset-normalizer==3.4.9
click==8.4.2
comm==0.2.3
contourpy==1.3.3
curl-cffi==0.16.0
cycler==0.12.1
dateparser==1.4.2
dill==0.4.1
distro==1.9.0
executing==2.2.1
fonttools==4.63.0
frozendict==2.4.7
gitdb==4.0.12
gitpython==3.1.58
greenlet==3.5.4
h11==0.16.0
holidays==0.102
httpcore==1.0.9
httpx==0.28.1
idna==3.18
imageio==2.37.4
importlib-resources==7.1.0
ipython==9.16.1
ipython-pygments-lexers==1.1.1
ipywidgets==8.1.8
jedi==0.20.0
jinja2==3.1.6
jiter==0.16.0
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyterlab-widgets==3.0.16
kiwisolver==1.5.0
# llvmlite/numba/scipy/scikit-learn: เข้าเส้นทางตัวเลขโดยตรง (vectorbt, prophet)
llvmlite==0.48.0
markdown-it-py==4.2.0
markupsafe==3.0.3
matplotlib-inline==0.2.2
mdurl==0.1.2
multitasking==0.0.13
mypy-extensions==1.1.0
narwhals==2.24.0
numba==0.66.0
packaging==24.2
parso==0.8.7
peewee==4.3.0
pexpect==4.9.0
pillow==11.3.0
platformdirs==4.11.0
prawcore==2.4.0
prompt-toolkit==3.0.53
protobuf==5.29.6
psutil==7.2.2
ptyprocess==0.7.0
pure-eval==0.2.3
pycparser==3.0
pydantic-core==2.27.2
pydeck==0.9.3
pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
referencing==0.37.0
regex==2026.7.19
rich==13.9.4
rpds-py==2026.6.3
scikit-learn==1.9.0
scipy==1.17.1
sgmllib3k==1.0.0
six==1.17.0
smmap==5.0.3
sniffio==1.3.1
soupsieve==2.9.1
stack-data==0.6.3
stanio==0.5.1
starlette==0.41.3
tenacity==9.1.4
threadpoolctl==3.6.0
toml==0.10.2
tornado==6.5.7
tqdm==4.70.0
traitlets==5.16.1
typing-extensions==4.16.0
tzdata==2026.3
tzlocal==5.4.4
update-checker==1.0.0
urllib3==2.7.0
watchdog==6.0.0
wcwidth==0.8.2
websocket-client==1.9.0
widgetsnbextension==4.0.15
