# ผลตรวจ Vaultis รอบ 2026-08-06 — สรุปและแผนแก้ไข

สถานะ: **เอกสารตรวจ ไม่ใช่บันทึกสิ่งที่ทำแล้ว** — ยังไม่มีข้อไหนถูกแก้
ที่มา: ตรวจ 10 รอบ (R1–R10) + ผู้ตรวจซ้ำที่พยายามหักล้างทุกข้อ + completeness critic 1 รอบ
เกณฑ์: ทุกข้อต้องมีคำสั่งที่รันจริงและผลลัพธ์จริงกำกับ — "อ่านโค้ดแล้วน่าจะพัง" ไม่นับ

## สภาพ ณ เวลาตรวจ

| รายการ | ค่า |
|---|---|
| HEAD | `9861db8` (feat: Google News RSS) |
| สถานะ git | **ยังไม่ commit** — โค้ดเฟส 1 ทั้งหมดเป็น unstaged diff 31 ไฟล์ +1,890/−272 + ไฟล์เทสต์ใหม่ 9 ไฟล์ |
| ชุดเทสต์ (mount ทับ + มีเน็ต) | **572 passed** |
| ชุดเทสต์ (ไม่มีเน็ต) | **`Interrupted: 2 errors during collection` — ไม่มีเทสต์สักตัวได้รัน** (ดูข้อ 0.3) |
| สมุดบัญชีจริง | `portfolio/data/transactions.csv` = 1 บรรทัด (header ล้วน) |
| คลัง alert จริง | `alerts/data/price_alerts.json` = 19 ไบต์ (`{"alerts": []}`) |
| `.docker-data/vaultis.db` | **มีข้อมูลขยะจากการตรวจรอบก่อน** (ดูข้อ 0.1) |

## สถิติผลตรวจ

| รายการ | จำนวน |
|---|---|
| ข้อที่เสนอทั้งหมด (หลังตัดข้อที่หักล้างในรอบตรวจเอง) | 90 |
| พบเพิ่มโดยผู้ตรวจซ้ำระหว่างทาง | 27 |
| พบเพิ่มโดย completeness critic | 7 |
| **รวมที่พิจารณา** | **124** |
| ผ่านการตรวจซ้ำ (มีคนรันซ้ำเอง) | 65 |
| ถูกหักล้าง | 4 |
| **เหลือหลังยุบข้อซ้ำข้ามรอบ** | **71** |

แยกตามความรุนแรงหลังตรวจซ้ำ (ความรุนแรงของผู้ตรวจซ้ำชนะความรุนแรงของผู้พบ):

| ระดับ | จำนวน | ความหมาย |
|---|---|---|
| critical | **0** | ไม่มีข้อไหนรอดที่ระดับนี้ — ทุกข้อที่ผู้พบให้ critical ถูกลดชั้นตอนตรวจซ้ำ |
| high | 12 | ผลิตตัวเลขเงินผิด / ทำลายข้อมูล / ทำให้งานอัตโนมัติตายถาวร |
| medium | 31 | รายงานผิด แต่ยังไม่ถึงขั้นเปลี่ยนเงินโดยตรง หรือมีตัวจำกัดชัดเจน |
| low | 28 | สัญญา API / เอกสาร / คุณภาพโค้ด / บั๊กที่ยังไม่มีผู้เรียก |

**ข้อสังเกตที่ต้องอ่านก่อนเชื่อตัวเลขข้างบน**

- ~49% ของข้ออ้างในรอบก่อนถูกหักล้าง รอบนี้เหลือ 6% (4/65) — ไม่ใช่เพราะโค้ดแย่ลง แต่เพราะทุกรอบถูกบังคับให้รันโค้ดจริงก่อนรายงาน ข้อที่พิสูจน์ไม่ได้ถูกตัดตั้งแต่ในรอบเอง
- 25 ข้อ **ไม่มีใครตรวจซ้ำ** (รอบ R7 ทั้งรอบ + บางข้อของรอบอื่น) — ทุกข้อในเอกสารนี้ระบุไว้ว่าผ่านการตรวจซ้ำหรือไม่
- ความรุนแรงของ 3 ข้อยังขัดกันระหว่างผู้ตรวจซ้ำคนละคน (H9, H10, H11) — ระบุไว้ในแต่ละข้อ

---

## 0. ข้อควรรู้ก่อนลงมือ

### 0.1 ⚠️ คำสั่งรันเทสต์ที่ CLAUDE.md สั่งไว้ เขียนทับฐานข้อมูลจริงของผู้ใช้ได้ — และเขียนไปแล้ว

`docker-compose.yml` service `tests` ตั้ง `DATABASE_URL: ""` เพื่อตัด Postgres ออกจากชุดเทสต์ตามเจตนา แต่ **ไม่ได้ตัด SQLite**:

```yaml
  tests:
    environment:
      VAULTIS_DB_PATH: /data/vaultis.db
      DATABASE_URL: ""
    volumes:
      - ./.docker-data:/data       # ← ฐาน goals / net worth / monthly reports ตัวจริง เขียนได้
```

ทุกอย่างที่รันด้วย `docker compose --profile dev run --rm tests` แล้วแตะ `backend.database.SessionLocal` — เทสต์ก็ดี สคริปต์ตรวจก็ดี — เขียนทะลุถึงไฟล์บน host ทันที

**หลักฐานว่าเกิดขึ้นแล้วจริง** (ผมคัดลอกฐานไป `/tmp` แล้วเปิดแบบ read-only ในคอนเทนเนอร์ ไม่ได้แตะไฟล์จริง):

```
docker compose --profile dev run --rm --no-deps -v <scratch>:/probe tests python -c "sqlite3.connect('file:/probe/db_copy.db?mode=ro', uri=True) ..."

tables: ['networth_snapshots', 'config', 'investment_goals', 'monthly_reports']
== investment_goals rows= 1
    {'id':'1','name':'VOO','target_amount_thb':'1.0','current_amount_thb':'1.0',
     'monthly_contribution_thb':'1.0','target_date':'2027-01-01','created_at':'2026-08-06 14:06:44.226182'}
== networth_snapshots rows= 1
    {'id':'1','snapshot_date':'2026-08-06','net_worth_thb':'1.0',
     'assets_json':'[{"name": "VOO", "type": "cash", "value_thb": 1.0}]','created_at':'2026-08-06 14:06:44.283782'}
```

ค่า `1.0` บาททุกช่องและชื่อเป้าหมายว่า `"VOO"` ไม่ใช่ข้อมูลที่ผู้ใช้กรอก แต่เป็นรูปทรงของ body ที่ generate จาก openapi schema ตอนรอบ R8 ยิงทุก endpoint · `mtime` ของ `.docker-data/vaultis.db` = `Aug 6 21:06` = `14:06 UTC` ตรงกับ `created_at` พอดี

**ต้องทำก่อนเริ่มเฟสอื่น**

1. เพิ่ม `VAULTIS_DB_PATH: /tmp/test_vaultis.db` (หรือ `:memory:`) ให้ service `tests` แล้ว **ถอด `- ./.docker-data:/data` ออก** — ชุดเทสต์ไม่มีเหตุผลใดต้องเห็นฐานจริง
2. แก้ CLAUDE.md ให้คำสั่งที่แนะนำเป็นคำสั่งที่ปลอดภัย
3. ตัดสินใจว่าจะลบ 2 แถวขยะทิ้งหรือไม่ (**เป็นการตัดสินใจของผู้ใช้ ไม่ใช่ของผู้แก้โค้ด**)

หมายเหตุที่ถ่วงดุล: รอบ R9 ตรวจแล้วว่า `md5sum` ของ `portfolio/data/transactions.csv`, `alerts/data/price_alerts.json`, `config.json` **ไม่เปลี่ยน** ก่อน-หลังรันชุดเต็ม 572 ตัว — ชุดเทสต์เองไม่ได้เขียนฐาน ผู้เขียนคือสคริปต์ตรวจที่เรียก `SessionLocal` ตรง ๆ แต่ช่องเปิดอยู่เหมือนกัน

### 0.2 คำสั่งที่ยืนยันว่าใช้ได้ (เห็นโค้ดที่แก้บน host ทันที)

```bash
# สคริปต์ตรวจ (เร็วกว่า pytest มาก)
docker compose --profile dev run --rm -v /home/da00/code/Vaultis:/app tests python - <<'PY'
...
PY

# เทสต์เฉพาะไฟล์
docker compose --profile dev run --rm -v /home/da00/code/Vaultis:/app tests pytest -q tests/test_xxx.py
```

โค้ดถูก bake เข้า image — ไม่ mount ทับ = ได้ "ผ่าน" ปลอมจากโค้ดเก่า

### 0.3 ชุดเทสต์รันไม่ได้เลยถ้าไม่มีเน็ต

```
docker run --network none ... pytest -q
ERROR tests/test_etf_analysis.py - ValueError: no OHLCV
ERROR tests/test_screener.py    - IndexError: single positional indexer is out-of-bounds
!!! Interrupted: 2 errors during collection !!!    21 warnings, 2 errors in 5.19s
```

ทั้งสองไฟล์เขียน `asyncio.run(test())` ไว้ที่ระดับโมดูล → **ยิง yfinance จริงตั้งแต่ตอน collect** ⇒ เลข "572 passed" ขึ้นกับสถานะเซิร์ฟเวอร์ภายนอกที่โปรเจกต์ไม่ได้คุม ต้องแก้ก่อนเพิ่ม CI (ข้อ D1–D2)

### 0.4 ข้อห้ามระหว่างแก้

- **ห้ามเขียนทับ** `portfolio/data/transactions.csv` และ `alerts/data/price_alerts.json` (สมุดจริงของผู้ใช้)
- **ห้ามเรียก LLM ที่เสียเงิน** — stub client ทุกครั้ง ห้ามตั้ง `VAULTIS_LLM_AUTO` เป็นค่าจริง
  - กับดักที่รอบ R4b เหยียบเข้าจริงและรายงานตัวเอง: เรียก `backend.routers.backtest.run_backtest(payload)` ตรง ๆ ใน Python → `include_ai` ได้ค่าเป็นอ็อบเจกต์ `Query(False)` ซึ่ง **truthy** → วิ่งเข้า `generate_summary(..., user_initiated=True)` = เงินออกจริง 1 ครั้ง **ห้ามเรียก router function ตรง ๆ ให้ใช้ TestClient เสมอ**
- **ห้ามยิง** Discord / Telegram / LINE webhook จริง

---

## 1. ตารางสรุปทุกข้อ (เรียงตามความรุนแรง)

`ตรวจซ้ำ` = มีผู้ตรวจคนที่สองรันซ้ำเองและยืนยัน · `แผนเดิม` = FIX_PLAN.md ครอบอยู่แล้วหรือไม่

| # | ประเด็น | ไฟล์หลัก | ระดับ | ตรวจซ้ำ | แผนเดิม |
|---|---|---|---|---|---|
| H1 | คำสั่งเทสต์ mount ฐานจริงแบบเขียนได้ (มีข้อมูลขยะแล้ว) | `docker-compose.yml` | high | ผมเอง | ไม่ |
| H2 | อ่าน `price_alerts.json` ไม่ได้ → เขียนทับด้วยลิสต์ว่าง | `alerts/price_alert.py` | high | ✓✓ | ไม่ |
| H3 | เทียบ `created_at` tz-naive กับ cutoff tz-aware → รายงานรายเดือนตายถาวร | `report_service.py` | high | ✓ | ไม่ |
| H4 | นับดอกเบี้ยเฉพาะส่วนที่จ่ายไหว → `interest_saved` พลิกเครื่องหมาย | `debt_service.py` | high | ✓ | ไม่ |
| H5 | PDF แปลง NaN เป็น 0.00 ทุกตาราง | `utils/pdf_export.py` | high | ✓✓✓ | ไม่ |
| H6 | ข้อความไทยใน PDF เป็นสี่เหลี่ยมดำทั้งหน้า | `utils/pdf_export.py` | high | ✓ | ไม่ |
| H7 | drift advisory สร้างเป้าหมายชุดที่สองจาก subset | `dashboard/app.py` | high | ✓ | บางส่วน (3.4) |
| H8 | XIRR ฐาน USD ถูกลบด้วยเงินเฟ้อไทยเป็น real return | `dashboard/app.py` | high | ✓ | บางส่วน (3.1) |
| H9 | เงินลงทุนรวมนับทุกแถว แต่กำไรนับเฉพาะที่มีราคา | `tracker.py` + `portfolio_service.py` | high* | ✓✓✓ | ✓ (ยังไม่ยืนยัน) |
| H10 | `ffill()` ปั้นแท่งราคาเป็น "วันนี้ +0.00% 🟢" + เข้า RSI | `etf_service.py` | high* | ✓✓ | ไม่ |
| H11 | Net Worth ตัด ETF ที่ราคาหายเงียบ + `etf_live` โกหก | `networth_service.py` | high* | ✓✓✓ | ไม่ |
| H12 | CI ส่ง "Daily Price Check" 2 ใบต่อวัน เลขขัดกันเอง | `.github/workflows/scheduler.yml` | high | ✓ | ไม่ |

`*` = ผู้ตรวจซ้ำคนละคนให้คนละระดับ (ดูรายละเอียดในหัวข้อของข้อนั้น)

รายการ medium/low อยู่ในเฟส B–D ด้านล่าง — ไม่ทำตารางซ้ำเพราะยาวเกินอ่าน

---

## 2. การจัดเฟสให้ทำขนานได้

| เฟส | ขอบเขต | ทำขนาน |
|---|---|---|
| **0** | ปิดความเสี่ยงของกระบวนการ (H1) + ตาข่ายเทสต์/CI (D1–D3) | ทำก่อนทุกอย่าง |
| **A** | 12 ข้อ high ที่ไฟล์ไม่ทับกัน — 9 สายอิสระ | ✅ ขนานได้ |
| **B** | medium ที่ไฟล์ไม่ทับกัน — 12 สาย | ✅ ขนานได้ |
| **C** | `portfolio/tracker.py` และ `dashboard/app.py` — หลายข้อทับไฟล์เดียวกัน | ❌ ทีละคน |
| **D** | low / เอกสาร / สุขอนามัย | ✅ ขนานได้ |

**สายที่ห้ามชนกัน**

- `utils/fx.py` (B10) แตะผู้เรียก 3 ไฟล์: `tracker.py` (C1), `networth_service.py` (A5), `rebalance_service.py` (B8) → **ทำ B10 หลัง A5 และ C1 เสร็จ**
- `utils/pdf_export.py` มี 4 ข้อ (H5, H6, M-PDF-1, M-PDF-2) → รวมเป็นสายเดียว
- `report_service.py` มี 5 ข้อ (H3, M1, M2, M3, M4) → รวมเป็นสายเดียว

---

## เฟส 0 — ปิดความเสี่ยงของกระบวนการ (ทำก่อนทุกอย่าง)

### 0-A. `docker-compose.yml` — ถอดฐานจริงออกจาก service `tests` (= H1)

ดูรายละเอียดและหลักฐานที่ข้อ 0.1 · **แนวแก้**: ตั้ง `VAULTIS_DB_PATH: /tmp/test_vaultis.db` และลบ `- ./.docker-data:/data` ออกจาก service `tests` แล้วแก้คำสั่งใน CLAUDE.md
**เทสต์ที่ต้องเขียน**: `tests/test_db_isolation.py` — assert ว่า `backend.database._db_path` ที่รันในชุดเทสต์ **ไม่ได้อยู่ใต้ `/data`** (กันไม่ให้ใครเผลอเติม mount กลับมา)

### 0-B. เทสต์ 4 ไฟล์ที่ต้องต่อเน็ต (= D1)

**อาการที่วัดได้**

```
offline เต็มชุด → Interrupted: 2 errors during collection (0 tests run)
มีเน็ต เต็มชุด → 572 passed in 45.59s
pytest -v tests/test_etf_analysis.py tests/test_screener.py → collected 2 items ... 2 passed in 19.28s
                                                              (ยิงเน็ตซ้ำสองรอบ: ตอน collect + ตอน run)
pytest --collect-only tests/test_pipeline.py → no tests collected   (ฟังก์ชันชื่อ main() จึงไม่ถูก collect)
AST scan: tests/test_screener.py → เทสต์ที่ไม่มี assert/raises: ['test']
```

**แก้ไขถ้อยคำจากรายงานเดิม (R9 เขียนเกินจริง 3 จุด — ผู้ตรวจซ้ำจับได้)**

- `tests/test_etf_analysis.py` **มี** assert (`assert info.symbol == symbol`) — ไฟล์ที่ไม่มี assert เลยคือ `test_screener.py` ไฟล์เดียว
- นับ `is not None` ได้ 16 ครั้งไม่ใช่ 14 และมี 3 assert ที่ตรวจของจริง
- ประโยค "`test_pipeline.py` เรียก `get_ai_advice()` = เสี่ยงค่า LLM" **ชวนเข้าใจผิด** — บรรทัดนั้นอยู่ใต้ `if __name__ == "__main__"` pytest แค่ import โมดูล **ไม่มีความเสี่ยงเรื่องค่าใช้จ่าย**

**แนวแก้** ย้าย 4 ไฟล์ (`test_etf_analysis`, `test_screener`, `test_backtest`, `test_forecast`) ไป `scripts/smoke/` หรือติด `@pytest.mark.network` + `addopts = -m "not network"` · ลบ `asyncio.run(test())` ระดับโมดูลทันที · เปลี่ยนชื่อ `tests/test_pipeline.py` ไม่ให้ขึ้นต้นด้วย `test_`

### 0-C. ไม่มี CI ตัวไหนรัน pytest เลย (= D2)

```
ls .github/workflows/     → scheduler.yml (ไฟล์เดียว)
grep -rn "pytest" .github/ → ไม่มีผลลัพธ์
```

และไฟล์เดียวที่มีนั้น trigger เป็น `schedule:` 3 cron + `workflow_dispatch:` เท่านั้น — **ไม่มี `on: push` หรือ `on: pull_request` เลย** ⇒ ไม่ใช่แค่ไม่มี pytest แต่ไม่มี trigger ใดผูกกับการแก้โค้ด เลข 572 จึงพึ่งวินัยของคนรันล้วน ๆ

**แนวแก้** เพิ่ม `.github/workflows/tests.yml` (`on: push, pull_request`) — **ทำหลัง 0-B** ไม่งั้น CI จะแดงเพราะ Yahoo ไม่ใช่เพราะโค้ด

### 0-D. ตาข่ายเทสต์ที่ขาดในจุดที่กำลังจะไปแก้ (= D3)

รอบ R9 รัน mutation testing 44 ตัวบนเส้นฐาน 568 (ตัด 4 ไฟล์ที่ต้องต่อเน็ต) — **รอด 18 ตัว** ตัวที่สำคัญ:

| mutation | ผล | ทำไมสำคัญ |
|---|---|---|
| `RSI_OVERSOLD 30 → 40` | 568 passed | เลื่อนเส้นได้ 10 จุด → `_timing_score` 10–30 จาก 100 → tilt 0.6–1.4 → **เงิน DCA จริงทุกเดือน** |
| `RSI_OVERBOUGHT 70 → 60` | 568 passed | เดียวกัน |
| `check_alerts` สลับ `>=` เป็น `<=` | 568 passed | alert "สูงกว่า X" กลายเป็น "ต่ำกว่า X" — call-probe ยืนยันว่า `check_alerts()` **ไม่ถูกเรียกสักครั้ง**ตลอด 568 เทสต์ |
| `ScreenerEngine` AND → OR ทุกพรีเซ็ต | 568 passed (และ **572 passed** เมื่อรวมไฟล์ที่ต่อเน็ต) | `run()` ไม่เคยถูกเรียกเลย — เฟส 2.1 ของแผนเดิมจะแก้โดยไม่มีตาข่าย |
| `signal_strength * 7 → * 10` | 568 passed | สเกล "ความแรง x/10" เพี้ยนทั้งชุด |
| `portfolio_mu_sigma: sigma × 252` แทน `× √252` | 568 passed | **σ ที่ป้อน Monte Carlo หน้า Goals** — ชนกับงานเฟส 4 ① ของแผนเดิมพอดี |
| `portfolio_mu_sigma: mu × 12` แทน `× 252` | 568 passed | μ ที่ป้อน MC |
| `grouped["pnl_thb"] − invested × 1.01` | 568 passed | P&L รายกองที่โชว์บนจอ (ยอดรวมถูกตรึงไว้ แต่รายตัวไม่) |
| `calculate_volatility × annualization` แทน `× √` | 568 passed | คอลัมน์ Volatility (สูงเกินจริง 15.9 เท่า) |
| `_signal_label: >= 70 → >= 60` | 568 passed | ป้าย Strong Buy/Buy ที่เป็นข้อสรุปแรกของทุกหน้าจอ |
| `dca._normalize_weights` ถอด normalize | 568 passed | น้ำหนักที่ไม่รวมเป็น 100% เข้าผลจำลอง |

**บริบทถ่วงดุลที่ต้องอ่านคู่กัน** — ค่าคงที่สายเงินตัวอื่นถูกตรึงไว้แน่นหนา ผู้ตรวจซ้ำรัน mutation เองแล้วถูกฆ่าหมด: `DIME_FEE_RATE 0.0015→0.0030` (7 failed) · `TILT_MIN/MAX 0.6,1.4→0.2,2.0` (4 failed) · `US_DIVIDEND_WITHHOLDING 0.15→0.30` (3 failed) · `fx MIN/MAX 20,50→0.01,10000` (4 failed) ⇒ **เป็นหลุมเฉพาะจุด ไม่ใช่ความอ่อนแอเชิงระบบ**

**แก้ป้ายผิดใน FIX_PLAN**: หลักฐาน coverage ที่ R9 อ้าง (`price_alert.py 18%`, `engine.py 18%`) **ผลิตซ้ำจาก image นี้ไม่ได้** — `import coverage` → `ModuleNotFoundError` และ `pytest --cov` → `unrecognized arguments` ตัวเลขนั้นมาจากการติดตั้งเพิ่มนอกสภาพแวดล้อมมาตรฐาน วิธีที่ทำซ้ำได้คือ **call-probe** (แทรกบรรทัดเขียนไฟล์ที่หัวฟังก์ชันในสำเนา) ซึ่งให้ผลเด็ดขาดกว่า

**เทสต์ที่ต้องเขียน (ทำก่อนเฟส A/B/C ที่เกี่ยวข้อง)**

1. `tests/test_price_alert_trigger.py` — ตารางความจริง 6 ช่อง (above/below × ราคา >, =, < เป้า) + เคส "ดึงราคาไม่ได้ต้องไม่ถูกนับว่า checked" → ปลดล็อกทั้ง H2 และ B6
2. `tests/test_signal_rules_boundary.py` — `rsi ∈ {29.9, 30.0, 30.1, 69.9, 70.0, 70.1}` + assert ค่าคงที่ตรง ๆ
3. `tests/test_screener_engine.py` — stub `_fetch_df` ด้วย DataFrame สังเคราะห์: AND ต้องไม่ผ่านเมื่อกฎข้อหนึ่งไม่ผ่าน · logic/field/operator ที่ไม่รู้จักต้อง raise · symbol ที่ดึงไม่ได้ต้องโผล่ใน `errors`
4. เพิ่มใน `tests/test_risk_underwater.py` — assert ค่าจริงของ `calculate_volatility`, `calculate_sharpe_ratio`, **และ `portfolio_mu_sigma`** จากซีรีส์ที่รู้คำตอบ (ทำก่อนเฟส 4 ① ของแผนเดิม)
5. เพิ่มใน `tests/test_money_math.py` — assert `P&L (THB)` รายตัว + invariant `sum(P&L ของแถว Price OK) == total_pnl_thb`
6. parametrize `_signal_label` ที่เส้นแบ่ง 70/55/40/25

---

## เฟส A — 12 ข้อ high (ไฟล์ไม่ทับกัน ทำขนานได้ 9 สาย)

### A1 · `alerts/price_alert.py` — อ่านไฟล์ไม่ได้ → เขียนทับด้วยลิสต์ว่าง (H2)

**อาการ** ลูกโซ่ 3 ชั้น
1. `_load_alerts()` บรรทัด 40–41 `except Exception: return []` → "อ่านไม่ได้" กลายเป็น "ไม่มี alert"
2. `check_alerts()` จบด้วย `_save_alerts(alerts)` **แบบไม่มีเงื่อนไข** (บรรทัด 329) แม้ไม่มีอะไร trigger
3. `_save_alerts` ใช้ `ALERTS_PATH.write_text(...)` (บรรทัด 47) — **ไม่ atomic** ซึ่งคือกลไกที่ทำให้ไฟล์เสียตั้งแต่แรก

**หลักฐาน** (sandbox `/tmp` ชี้ `pa.ALERTS_PATH` ไปที่อื่น ไม่แตะไฟล์จริง — รันซ้ำโดยผู้ตรวจ 2 คน)

```
alerts ที่ผู้ใช้ตั้งไว้ = ['VOO','QQQM','GLDM']
ตัดไฟล์ครึ่งเดียว (จำลองการเขียนที่ถูกขัดจังหวะ) → _load_alerts() = []
check_alerts() = {'success': True, 'checked': 0, 'triggered': []}
ไฟล์หลังรัน = {"alerts": []}          ← alert ทั้ง 3 หายถาวร
daily_summary บรรทัดท้าย = '⚠️ Price Alerts: 0 รายการ'
```

**เส้นทางที่สองที่ไม่ต้องพึ่งไฟล์เสียเลย** (ผู้ตรวจซ้ำพบเพิ่ม): `check_alerts()` ทำ read-modify-write โดยไม่มี lock และเขียนคืนลิสต์ที่ load ไว้ตั้งแต่ต้นฟังก์ชัน → alert ที่ผู้ใช้เพิ่มจาก dashboard ระหว่างนั้นถูกลบ (lost update) หน้าต่าง = เวลาที่ `get_price_snapshots` ยิง yfinance = **วัดได้ 0.72 วินาที** · เธรดอ่านคู่กับเธรดเขียน 2 วินาที → ได้ผลผิด 1,418 จาก 2,958 ครั้ง

**ทำไมลดจาก critical เป็น high** เส้นทาง "ไฟล์เสีย" ต้องมีเหตุก่อน (คอนเทนเนอร์ถูกฆ่ากลางเขียน / ดิสก์เต็ม) ไม่ใช่เหตุการณ์ประจำวัน ส่วน lost update หน้าต่างแคบ (~1 วิ × 2 รอบ/วัน) และไฟล์จริงตอนนี้ว่างอยู่แล้ว (19 ไบต์) จึงยังไม่มีความเสียหายเกิดกับผู้ใช้

**แนวแก้**
1. `_load_alerts()` แยก "ไฟล์ไม่มี" (= ลิสต์ว่างจริง) ออกจาก "อ่าน/parse ไม่ได้" — กรณีหลัง `raise AlertStoreUnavailable`
2. `check_alerts()` / `add_*` / `delete_*` ต้อง **หยุดทันที** เมื่อโหลดไม่สำเร็จ ห้ามเขียนกลับ และรายงานออก Discord ว่าอ่านคลัง alert ไม่ได้
3. `_save_alerts` เขียนแบบ atomic (`.tmp` + `os.replace`) + `fcntl.flock` รอบ read-modify-write (สามคอนเทนเนอร์แชร์ bind mount เดียวกัน) + สำรอง `.bak` ก่อนทับ

**เทสต์**: `tests/test_price_alert_store.py` — ไฟล์เสีย → raise ไม่ใช่ `[]` · `check_alerts` ต้องไม่เขียนไฟล์เมื่อโหลดล้ม · เพิ่ม alert ระหว่าง `check_alerts` ทำงาน → ต้องไม่หาย

---

### A2 · `backend/services/report_service.py` — รายงานรายเดือนตายถาวร (H3) + 4 ข้อ medium ในไฟล์เดียวกัน

#### H3 — เทียบ `created_at` tz-naive จาก Postgres กับ cutoff tz-aware

`get_screener_summary()` สร้าง `cutoff = datetime.now(UTC) - timedelta(days=30)` (บรรทัด 83, tz-aware) แล้วเทียบกับ `created_at` ที่ประกาศเป็น `TIMESTAMP` (= `timestamp without time zone`) ใน `SCREENER_HISTORY_DDL`

**หลักฐาน** (ยิงกับ Postgres ตัวจริงที่รันอยู่ INSERT แล้ว ROLLBACK — ไม่มีแถวค้าง)

```
schema screener_history: created_at = 'timestamp without time zone'   จำนวนแถวปัจจุบัน = 0
created_at ที่ driver คืน = datetime(2026,8,6,21,49,44,316336) | tzinfo = None
get_screener_summary() → TypeError: can't compare offset-naive and offset-aware datetimes
กรณีตารางว่าง (สถานะปัจจุบัน) → {'total_signals': 0, ...} ไม่ระเบิด
```

**เส้นทางที่ exception เดินไปตาย** `get_screener_summary` → `_aggregate_data` (ไม่มี try) → `generate_and_save_report` (มีแต่ `finally: db.close()`) → (ก) APScheduler `run_monthly_report` cron วันที่ 1 08:00 **ตายทั้งงาน** ไม่บันทึก ไม่ส่ง Telegram (ข) `POST /api/reports/generate` = 500

**จะระเบิดถาวรทันทีที่ screener บันทึกสัญญาณแรก** เพราะ `get_history(limit=500)` ไม่กรองอายุ — เจอแถวแรกก็เข้าเงื่อนไขเปรียบเทียบทันที เป็นบั๊กชนิด "ยิ่งระบบทำงานถูกยิ่งพัง"

**พ่วง** `NOW()` ถูกเรียกในคอนเทนเนอร์ postgres ที่ตั้ง `TZ: Asia/Bangkok` แล้วเก็บลงคอลัมน์ไร้ tz = เวลาไทย แต่ cutoff อ้าง UTC → ต่อให้แก้ชนิดให้ตรงกัน หน้าต่าง 30 วันก็ยังเลื่อน 7 ชม. และ `ScreenerResult.timestamp` (`datetime.now(UTC)` ใน engine) เป็นคนละเวลากับที่เก็บลงฐานสำหรับเหตุการณ์เดียวกัน

**แนวแก้** เปลี่ยน DDL เป็น `TIMESTAMPTZ` + insert `NOW()` (migrate ค่าเดิมด้วย `AT TIME ZONE 'Asia/Bangkok'`) และ normalize ทุกค่าที่อ่านมาเป็น aware ก่อนเทียบ · ครอบ `generate_and_save_report` ให้ log + แจ้งเตือนเมื่อ cron ตาย ไม่ใช่ตายเงียบ
**เทสต์**: ป้อน `created_at` แบบ naive เข้า `get_screener_summary` ตรง ๆ ต้องไม่ระเบิด

#### M-R1 — narrative กลายเป็นสตริง error แล้วทับรายงานเดิม + ส่ง Telegram

บรรทัด 221 `except Exception as exc: return f"ไม่สามารถสร้าง narrative ได้: {exc}"` วางคู่กับ `except LLMDisabledError` ที่ fallback ไป `_plain_narrative()` อย่างถูกต้อง — **ทางที่ผิดพลาดกลับเป็นทางที่ทำลายข้อมูลมากกว่า**

```
A1 LLMDisabledError → '📊 สรุปการเงินเดือน 2026-08 (จากโมเดล — ไม่ใช้ AI) | พอร์ต: 5 ETF | มูลค่า 16,028.00 USD | ...'
A2 RuntimeError     → 'ไม่สามารถสร้าง narrative ได้: เรียก LLM ไม่สำเร็จ: โมเดลใช้โควตา 3200 tokens หมด...'
                       มีเลข 16,028 ในรายงานไหม: False
A3 ผ่าน generate_and_save_report 2 รอบ → รอบ 2 ทับรอบ 1 จนเหลือ row เดียวที่ content = สตริง error
                                          Telegram ถูกส่ง 2 ครั้ง ครั้งหลังเป็นข้อความ error
```

`chat_text` ห่อทุกอย่างด้วย `except Exception as exc: raise RuntimeError(f"เรียก LLM ไม่สำเร็จ: anthropic: {exc}")` → คีย์ผิด / 529 overloaded / โควตาหมด ล้วนไม่ใช่ `LLMDisabledError`

**แก้ถ้อยคำในแผนเดิม**: FIX_PLAN ข้อ 1.1 ท้ายหัวข้อคาดว่าถ้าไม่ catch RuntimeError แล้ว "job พัง" — ความจริงคือ **ไม่พัง แต่แย่กว่า**: รายงานผ่านฉลุยแบบไม่มีตัวเลขและกลืนรายงานเดิมทิ้ง

**แก้ถ้อยคำจากรายงานเดิม**: ข้ออ้าง "กู้ไม่ได้ถาวร" **ผิด** — เรียก `generate_and_save_report` ซ้ำโดยไม่เปิด AI จะได้ `_plain_narrative` ตัวเลขครบเหมือนเดิม และ cron วันที่ 1 รันด้วย `user_initiated=False` จึงปลอดภัยอยู่แล้วเว้นแต่ตั้ง `VAULTIS_LLM_AUTO=1`

**แนวแก้** ให้ `except Exception` ตกไป `_plain_narrative(all_data, month)` เหมือนกัน แล้วต่อท้าย "⚠️ AI เขียนบทสรุปไม่สำเร็จ: {exc} — ตัวเลขด้านบนมาจากโมเดลในระบบตามปกติ" · เพิ่มคอลัมน์ `source` = `ai|plain` แล้วห้าม plain ทับ ai ของเดือนเดียวกัน

#### M-R2 — Net Worth ไม่มีจุดเทียบ ถูกรายงานเป็น "+0 / +0.0%"

บรรทัด 66 `previous_nw = previous.net_worth_thb if previous else current_nw` — ยัดค่าปัจจุบันเป็นฐานเทียบของตัวเอง แล้วยังคง `available: True`

```
snapshot เดียว → {'available': True, 'previous_net_worth_thb': 1234567.0, 'change_thb': 0.0, 'change_pct': 0.0}
บรรทัดในรายงาน: 'Net Worth: 1,234,567 บาท (+0 / +0.0%)'
prompt ที่ส่งให้ LLM: '- Net Worth 1,234,567 THB (เปลี่ยนแปลง +0 THB / +0.0%)'
มีจุดเทียบจริง → change_thb 234567.0 / 23.46%   (ทำงานถูกเมื่อมีข้อมูล)
ไม่มี snapshot เลย → {'available': False}       (เคสนี้โค้ดทำถูก — ทำให้เคส "มี 1 snapshot" โดดออกมาว่าเป็นข้อยกเว้นที่หลุด)
```

เป็นการกลับด้านของกฎ "AI อธิบาย โค้ดคำนวณ" — **โค้ดป้อนเลขที่ไม่มีอยู่จริงให้ AI**

**แนวแก้** `change_thb = None, change_pct = None, has_baseline: False` แล้วพิมพ์ "(ยังไม่มีเดือนก่อนหน้าให้เทียบ)" · ขยาย `get_history(db, months=3)` ให้ครอบเดือนก่อนได้แน่ หรือ query แถวล่าสุดที่ `snapshot_date[:7] < today_month` ตรง ๆ

#### M-R3 — ฐานข้อมูล screener ล่ม → "สัญญาณ screener 30 วัน: 0 รายการ"

```
(ก) ไม่ตั้ง DATABASE_URL → svc.engine = None → get_history() = [] → 'สัญญาณ screener 30 วัน: 0 รายการ'
(ข) ตั้ง DATABASE_URL ที่ต่อไม่ได้ → '[screener_history] get_history error: (psycopg2.OperationalError) ... Connection refused'
                                     → คืน [] เหมือนกันทุกไบต์
```

เป็นคนละจุดกับ FIX_PLAN 2.1: 2.1 อยู่ฝั่ง**เขียน** (`engine.run` กลืน exception) ส่วนนี้อยู่ฝั่ง**อ่าน** (`history_service.get_history`) — แก้ 2.1 แล้วข้อนี้ยังพังต่อ · ข้อความเท็จนี้ไหลเข้าทั้ง Telegram และ prompt ของ LLM
**จุดอ่อนของข้ออ้าง**: กรณี (ก) `DATABASE_URL` เป็น optional ตาม CLAUDE.md — ในกรณีนั้นระบบไม่มีประวัติให้จริง ๆ ความผิดคือไม่แยก "ไม่ได้เปิดใช้" ออกจาก "สแกนไม่ได้" ซึ่งเบากว่ากรณี (ข)

**แนวแก้** `get_history` คืน `(rows, status)` หรือ raise · `get_screener_summary` เพิ่ม `available` + `unavailable_reason` · narrative พิมพ์ "อ่านประวัติไม่ได้ (…)" แทนเลข 0

#### M-R4 — `TypeError` เมื่อ `return_pct` เป็น `None`

บรรทัด 171 `f"{h['ticker']} ({h['return_pct']:+.1f}%)"` ไม่ดัก `None` ที่ `portfolio_service._clean()` **ตั้งใจ**ผลิต และบรรทัดนี้อยู่ **ก่อน** try/except ที่ครอบ `chat_text` ⇒ เส้นทางฟรีตายด้วย

```
{'ticker':'SCHD','shares':0,'invested_usd':0,'current_value_usd':0.0,'return_pct':None,'price_ok':True}
generate_narrative(user_initiated=False) → TypeError: unsupported format string passed to NoneType.__format__
```

**ลดจาก critical เป็น medium**: ทริกเกอร์คือแถวที่ต้นทุนรวม = 0 ซึ่งช่องทางเขียนของระบบเองสร้างไม่ได้ (`add_transaction` ปฏิเสธ `shares<=0`/`price_usd<=0`, `add_dividend` เขียน `tx_type='dividend'` ที่ถูกกรองทิ้ง) ต้องเป็น CSV ที่แก้มือ/สะกด `tx_type` ผิด ประกอบกับ ledger จริงว่างเปล่า
**แนวแก้** แยก `None` ก่อนฟอร์แมตทุกที่ + ย้ายการประกอบ `top_txt`/`nw_txt` เข้าไปใน try เดียวกับ LLM · `grep -rn "generate_narrative" tests/` = **ว่างเปล่า** ต้องมีเทสต์ตรึง

---

### A3 · `backend/services/debt_service.py` — นับดอกเบี้ยเฉพาะส่วนที่จ่ายไหว (H4)

**อาการ** บรรทัด 80–82
```python
interest_paid = min(interest, payment)
principal_paid = payment - interest_paid
total_interest += interest_paid          # ← ดอกเบี้ย "ที่จ่ายไหว" ไม่ใช่ "ที่เกิดขึ้น"
```
เมื่อ `min_payment` น้อยกว่าดอกเบี้ยรายเดือน ส่วนต่างถูกทบเข้าเงินต้น (`new_balance` โตขึ้น) แล้วเวลาชำระคืนภายหลังกลับถูกจัดประเภทเป็น principal → `total_interest` ต่ำกว่าจริงเสมอ และต่ำ**ไม่เท่ากัน**ระหว่าง avalanche กับ snowball → `interest_saved = snowball.total_interest - avalanche.total_interest` **พลิกเครื่องหมายได้**

**หลักฐาน** — ผู้ตรวจซ้ำไม่ใช้ simulator ที่ผู้พบเขียนใหม่ (ซึ่งหักล้างไม่ได้ว่าซื่อสัตย์) แต่ใช้ identity ทางบัญชีจากผลลัพธ์ของ service เอง: `sum(PaymentEntry.payment) − ยอดหนี้ตั้งต้น = ดอกเบี้ยที่เกิดจริง`

```
เคส: สินเชื่อบุคคล 20,000@8% min1,000 + บัตรเครดิต 200,000@26% min3,000, งบ 6,000/เดือน
  avalanche  รายงาน 208,772.50 | เงินสดจ่ายจริง 428,772.50 | ดอกเบี้ยจริง 208,772.50 | 72 เดือน
  snowball   รายงาน 207,920.76 | เงินสดจ่ายจริง 437,410.66 | ดอกเบี้ยจริง 217,410.66 | 73 เดือน
  interest_saved ที่ API คืน = -851.74  → ตีความว่า "snowball ถูกกว่า 852 บาท"
  ความจริง: snowball แพงกว่า 8,638.16 บาท และช้ากว่า 1 เดือน
```

เคส BASE ในเทสต์เดิม (min คลุมดอกเบี้ย) รายงานตรงกับความจริงเป๊ะทั้งสองวิธี → ยืนยันว่าความคลาดเคลื่อนมาจาก negative amortization จริง

ตาราง amortization รายแถวก็ไม่ลงตัวกันเอง:
```
ยอดต้นงวด  | payment  | interest | principal | remaining
200,000.00 | 3,000.00 | 3,000.00 |      0.00 | 201,333.33   (ดอกเบี้ยที่เกิดจริง 4,333.33)
```
"จ่ายดอกเบี้ย 3,000 เงินต้น 0" ขณะยอดหนี้เพิ่ม 1,333.33/เดือน โดยไม่มีอะไรเตือนว่ากำลัง negative amortization

และ invariant ของเทสต์ที่มีอยู่แล้วพังกับข้อมูลจริง:
```
tests/test_phase2.py::test_avalanche_pays_less_interest_than_snowball
  BASE เดิม (บัตร18% + รถ5%)  → ผ่าน (18,192.31 <= 18,192.31)
  บัตรเครดิตจริง (min < ดอกเบี้ย) → ไม่ผ่าน (208,772.50 > 207,920.76)
```

**ขนาดปัญหา** — ผู้พบสแกน 714 ชุด: ผิด 444, พลิกคำแนะนำ 55, คลาดสูงสุด 45,101 บาท · ผู้ตรวจซ้ำสแกนเอง 69 ชุด: ต่ำกว่าจริง 19, พลิกคำแนะนำ 2, คลาดสูงสุด 12,043 บาท — **สัดส่วนอยู่ระดับเดียวกัน**

**ทำไมลดจาก critical เป็น high** (ก) ไม่มี consumer ใน dashboard — `grep 'api/debt|api/cashflow|api/networth'` เจอเฉพาะในไฟล์ router เอง เป็น API-only (ข) การพลิกเกิดเมื่อ `min_payment` ต่ำกว่าดอกเบี้ยรายเดือน = ≤1.5–3% ของยอด ซึ่งต่ำกว่าเกณฑ์ขั้นต่ำบัตรเครดิตไทย (8%) — เกิดได้กับหนี้นอกระบบ/หนี้ที่เจรจาแล้ว หรือผู้ใช้กรอกเอง แต่ไม่ใช่ค่าปกติ

**แนวแก้**
1. `total_interest += interest` และ `PaymentEntry(interest=round(interest,2), principal=round(payment - interest, 2))` — ปล่อยให้ principal ติดลบได้ เพราะยอดหนี้โตจริง **ห้ามบีบเป็น 0**
2. `DebtSchedule.total_interest` เก็บจากค่าเดียวกัน จะได้ตรงกับ `remaining_balance` ที่โชว์
3. ธง fail-loud: งวดใดที่ `payment < interest` ต้องรายงานออกไป (`negative_amortization_months: [...]` ต่อหนี้แต่ละก้อน) — "จ่ายขั้นต่ำแล้วหนี้โตขึ้น" คือข้อมูลที่ผู้ใช้ต้องเห็น
4. แก้ `tests/test_phase2.py::test_avalanche_pays_less_interest_than_snowball` ให้ใช้ชุดหนี้ที่มี `min_payment < ดอกเบี้ยรายเดือน` ด้วย ไม่งั้น invariant นี้จะยังผ่านทั้งที่ระบบผิด

---

### A4 · `utils/pdf_export.py` — 4 ข้อในไฟล์เดียว (H5, H6 + 2 medium)

#### H5 — `_safe_float(value, default=0.0)` แปลง NaN เป็น 0 ทุกช่องของรายงาน

บรรทัด 23 `return default if f != f else f` แล้วถูกใช้กับ**ทุกช่องตัวเลข**: สรุปหน้า 1 (บรรทัด 90–93), ตาราง Holdings (131–135), Return Analysis (156), Risk Metrics (170–172), Allocation (202–203)

ขัดกับ contract ของโมดูลต้นทางตรง ๆ — `analysis/returns.calculate_period_returns` เขียนเองว่า "ETF ที่เกิดทีหลังต้องได้ NaN ในช่วงยาว ไม่ใช่ 0 (C1)" · `backend/services/json_safe.py` เขียนว่า "**ห้ามแปลงเป็น 0** เพราะ 0 คือตัวเลขที่ตีความเป็นผลตอบแทน/ราคาได้" · `portfolio/tracker.py:611` มีคอมเมนต์ว่า "เดิม `fillna(0)` ทำให้ P&L โชว์ −100% ปลอม (AUDIT.md C1)" · CLAUDE.md เขียนว่า "missing prices stay NaN, never 0.0"

**หลักฐาน** (ดัก `_build_table`/`Paragraph` เพื่ออ่านสิ่งที่ถูกวาดลง PDF จริง — ผู้ตรวจ 4 รอบรันซ้ำได้เหมือนกัน)

```
ตาราง Holdings (GLDM ดึงราคาไม่ได้ → tracker คืน NaN ถูกต้อง):
  ['GLDM', '100.0000', '50.00', '0.00', '0.00', '0.00%']
  ['VOO',  '10.0000', '400.00', '500.00', '30,000.00', '25.00%']

ตาราง Return Analysis (คอลัมน์ XLV เป็น NaN ล้วน):
  ['1M', '-5.56%', '0.00%']  ['3M', '8.87%', '0.00%']  ['6M', '33.03%', '0.00%']  ['1Y', '18.94%', '0.00%']
ตาราง Risk Metrics:
  ['VOO', '0.1589', '0.4866', '-0.3399']
  ['XLV', '0.0000', '0.0000', '0.0000']
  → หน้า 2 ไม่มี Paragraph เตือนสักบรรทัด (ดักรายการย่อหน้าทั้งหมดแล้ว)
```

**premise ที่ผู้ตรวจซ้ำพิสูจน์เพิ่มให้ (ผู้พบแค่อ้าง)** — รัน fetcher ตัวจริงกับ yfinance จริง:
```
fetch_adjusted_close_data(tickers=['VOO','ZZZZQQFAKE'], years=1)
  → ไม่ raise, คืน 251 แถว, คอลัมน์ที่ NaN ล้วน: ['ZZZZQQFAKE']
```
⇒ `dropna(how="all")` เก็บคอลัมน์ที่ล้มเหลวไว้จริง และในรันเดียวกัน VOO เองก็ `Failed download` รอบแรกแล้วผ่านตอน retry — ความล้มเหลวรายตัวเกิดจริง

**ความรุนแรงที่ขัดกัน** R8 ให้ high (ทริกเกอร์ง่ายมาก: `tracker._get_latest_prices` คืน `{}` ทุกครั้งที่ `yf.download` โยน → **ทุกแถว**กลายเป็น 0.00 พร้อมกัน) · R2/R6 ให้ medium (คำเตือน `WARNING: current price unavailable for X` มีอยู่จริงเหนือตาราง Holdings หน้า 1) — **ตัดสินให้ high** เพราะตาราง Returns/Risk หน้า 2 ไม่มีคำเตือนใด ๆ เลย และเป็นเอกสารที่ผู้ใช้เก็บอ่านย้อนหลังโดยไม่มีบริบทหน้าจอ

**แนวแก้** เพิ่ม `_fmt(value, spec, na="N/A")` ที่คืน `"N/A"` เมื่อ NaN/None แล้วใช้แทน `_safe_float` ทุกช่องเชิงราคา/กำไร/ผลตอบแทน · เก็บ `_safe_float` ไว้เฉพาะที่ 0 เป็นคำตอบจริง (เช่น `unallocated > 0`) · ถ้าแถวใดเป็น N/A ทั้งแถว เพิ่ม Paragraph เตือนใต้ตารางนั้น โดยคำนวณจาก `prices[col].notna().sum() == 0` (คนละชุดกับ `missing_prices` ของ ledger)
**หน้าจอทำถูกแล้ว** — `dashboard/app.py` ใช้ `na_rep="N/A"` 8 จุดรวมตาราง holdings เฉพาะ PDF ที่ตกหล่น

#### H6 — ข้อความไทยใน PDF เป็นสี่เหลี่ยมดำ

PDF ลงทะเบียนเฉพาะฟอนต์มาตรฐาน (`Helvetica`/`Helvetica-Bold`, `WinAnsiEncoding`) reportlab **ไม่ error** แต่สลับไป `ZapfDingbats` แล้ววาด `n` (= ■) หนึ่งตัวต่ออักษรไทยหนึ่งตัว

**หลักฐาน** (ตั้ง `rl_config.pageCompression = 0` แล้วอ่าน content stream ดิบของ PDF ที่ `generate_monthly_report` สร้างเอง)

```
/F1 10 Tf (MARKERSTART ) Tj  /F4 10 Tf (nnnn) Tj  /F1 10 Tf ( 36800 ) Tj  /F4 10 Tf (nnn) Tj  /F1 10 Tf ( MARKEREND) Tj
  F4 = /BaseFont /ZapfDingbats   (F1–F3 = Helvetica* /WinAnsiEncoding)
  → "กำไร" 4 อักษร → (nnnn) · "บาท" 3 อักษร → (nnn) · เลข 36800 รอด

ฟอนต์ที่ฝังใน PDF: [b'Helvetica', b'Helvetica-Bold', b'Helvetica-BoldOblique', b'ZapfDingbats']
มีไบต์ non-ASCII ในสตรีมข้อความไหม: False      ← อักษรไทยไม่เหลืออยู่ใน PDF เลย
reportlab: encode Thai FAILED: UnicodeEncodeError 'charmap' codec can't encode characters in position 0-11
```

`advice_text` **เป็นไทยทุกเส้นทาง**: ดีฟอลต์ `include_ai=False` → `AI_DISABLED_MESSAGE` (ไทย, เป็นเนื้อหาเดียวของหน้า 3) · `include_ai=True` → บทวิเคราะห์ไทย · error → `f"AI analysis unavailable: {exc}"` ที่ exc เป็นไทย

**เป็นความล้มเหลว deterministic 100%** ไม่ต้องรอ data failure ใด ๆ — ผู้ใช้ที่ติ๊ก "ใส่บทวิเคราะห์ AI ในรายงาน (มีค่าใช้จ่าย)" จ่ายเงินแล้วอ่านสิ่งที่ซื้อมาไม่ได้เลยทั้งหน้า (ยาวได้ถึง 2,500 ตัวอักษร)

**แนวแก้** `pdfmetrics.registerFont(TTFont("NotoThai", <path>))` (Noto Sans Thai / Sarabun — ใส่ `.ttf` ใน repo หรือ `apt-get install fonts-thai-tlwg` ใน Dockerfile) แล้วใช้ฟอนต์นั้นทุกจุดรวม `FONTNAME` ใน `_build_table` · ถ้ายังไม่พร้อม **ต้อง fail loud แทนการวาดสี่เหลี่ยม**: ตรวจ `any(ord(c) > 0x0E00 for c in advice_text)` แล้วแทนด้วยข้อความอังกฤษ — ห้ามพิมพ์สิ่งที่รู้อยู่แล้วว่าอ่านไม่ออก

#### M-PDF-1 — ดึงราคาไม่สำเร็จถูกพิมพ์เป็น "No return data available." (medium, ตรวจซ้ำแล้ว)

`try: prices = fetch_adjusted_close_data(...) except Exception: prices = pd.DataFrame()` (บรรทัด 73–74) กลืน `PriceDataUnavailableError` ทิ้ง ไม่ log ไม่เก็บ exception

```
แทน pe.fetch_adjusted_close_data ด้วยตัวที่ raise PriceDataUnavailableError จริง:
  pdf bytes: 4614 (สร้างสำเร็จ ส่งดาวน์โหลดตามปกติ)
  PARA: No return data available.
  PARA: No risk metrics data available.
  ← ข้อความเดียวกันทุกไบต์กับกรณีที่ไม่มีข้อมูลจริง
```

**แนวแก้** เก็บ `price_error = exc` แล้วพิมพ์ "WARNING: price download failed (3 retries): {exc} — performance section could not be produced. Re-generate later." + `logger.warning`

#### M-PDF-2 — ความล้มเหลวของ `get_monthly_advice` กลายเป็นคำแนะนำการลงทุน (medium, **ยังไม่ผ่านการตรวจซ้ำโดยคนที่สอง** — ผู้ตรวจซ้ำของ R2 เป็นคนพบเอง)

`advice: dict = {}` แล้ว `advice = get_monthly_advice(...)` อยู่ใน try; ถ้าโยน (ซึ่ง `get_monthly_advice` ห่อทุกอย่างด้วย `raise RuntimeError(...)`) ตัวแปรค้างเป็น `{}` → `allocation` ว่าง → PDF พิมพ์

```
PARA: No ETF met the allocation threshold this month (model suggests holding cash).
```

ทั้งที่โมเดลไม่เคยประเมินอะไรเลย และร่องรอยความล้มเหลวเดียวคือ `AI analysis unavailable: <ข้อความไทย>` ซึ่งตาม H6 จะถูกวาดเป็นสี่เหลี่ยมดำ

**ความสมจริงของตัวจุดชนวน** (ผู้ตรวจซ้ำตรวจเอง): `get_macro_snapshot` ไม่ raise เมื่อไม่มี `FRED_API_KEY` และ `build_etf_scores` คืน `_no_data` แทนการโยน ⇒ ตัวจุดชนวนที่เหลือคือ `tracker.get_portfolio_summary()`/`load_config()` ล้ม ซึ่งพบไม่บ่อย

**เกี่ยวข้องกับ M-DCA-2** (เฟส B) ซึ่งเป็นข้อความเดียวกันที่ผลิตจากอีกสาเหตุหนึ่ง — **แก้พร้อมกัน**

---

### A5 · `backend/services/networth_service.py` + `backend/models/networth_models.py` (H11 + 3 low)

#### H11 — ETF ที่ดึงราคาไม่ได้หายจาก net worth เงียบ ๆ และ `etf_live` ยังเป็น `True`

`_etf_assets_live()` บรรทัด 33 กรอง `if h.get("price_ok") and h.get("current_value_usd")` · `etf_live = bool(etf_assets)` (บรรทัด 48) เป็น `True` ทันทีที่มี ETF **ตัวใดตัวหนึ่ง** มีราคา · `NetWorthResponse` **ไม่มีฟิลด์ใดเลย**สำหรับเรื่องนี้ · `get_holdings()` คืน `skipped_rows`/`skipped_reason` มาให้แล้วแต่ถูกทิ้งทั้งคู่

docstring เขียนเองว่า "ถือครองที่ดึงราคาไม่ได้จะถูกข้าม (ไม่นับเป็น 0 — AUDIT.md C1)" — แต่**การข้ามโดยไม่รายงานให้ผลลัพธ์ตัวเลขเหมือนกับการนับเป็น 0 ทุกประการ** เพราะมันหายจากตัวตั้งของ `total_assets_thb`/`net_worth_thb`

**หลักฐาน** (SQLite ชั่วคราวใน `/tmp`, stub `get_holdings` + fx ไม่แตะเน็ต — 4 รอบรันซ้ำได้)

```
A) VOO/SCHD/QQQM/XLV ปกติ + GLDM price_ok=False
   etf_live = True                            ← ยัง True ทั้งที่ GLDM หาย
   assets = [('เงินสด',500000),('VOO',330000),('SCHD',165000),('QQQM',132000),('XLV',99000)]
   net_worth_thb = -774,000.00 · ที่ถูกต้อง = -245,076.00   (ต่าง 528,924 บาท)
   keys = ['assets','etf_live','liabilities','net_worth_thb','snapshot_date','total_assets_thb','total_liabilities_thb']
                                                            ← ไม่มี missing_prices / skipped_rows

B) ราคาพังทุกตัว → etf_live=False  total_assets=500,000  net_worth=-1,500,000
C) ledger ว่างจริง → etf_live=False  total_assets=500,000  net_worth=-1,500,000
   B.model_dump() == C.model_dump() → True ทุกฟิลด์      ← "ดึงไม่ได้" กับ "ไม่มี" แยกไม่ออกเลย

D) snapshot ที่บันทึกไว้มี Asset('VOO','etf',100,000) แต่ตอนราคาพังหมด assets ไม่มี etf เลย
   → ไม่มี fallback ตามที่คอมเมนต์ etf_live ("True when ETF values are from live prices, not snapshot") สัญญาไว้
```

พี่น้องของมันทำถูกแล้ว: `portfolio_service.get_portfolio_summary()` จากสมุดเล่มเดียวกันคืน `missing_prices: ["GLDM"]` ครบ — **backend ตัวเดียวกัน แหล่งข้อมูลเดียวกัน แต่รายงานช่องโหว่คนละแบบ**

**ความรุนแรงที่ขัดกัน** R1 ให้ high (ละเมิดกฎแกน + `price_ok=False` เกิดจริงจาก rate-limit) · R6/R8 ลดเป็น medium (`grep -rn "networth" dashboard/app.py` = **0 บรรทัด** ไม่มีหน้าจอเรียกใช้ ผู้เสียหายคือผู้เรียก API ตรงเท่านั้น) — **ตัดสินให้ high** เพราะ endpoint นี้เพิ่งใช้งานได้จริงในเฟส 1 (git diff เปลี่ยน `get_holdings()` → `get_holdings()["holdings"]`) จึงยังไม่มีใครเคยเห็นบั๊กนี้ และถ้าต่อ UI เมื่อไรจะกลายเป็นตัวเลขความมั่งคั่งที่ผิดทันที

**หักล้างท่อนหนึ่งของคำอธิบายผลกระทบ** — "snapshot ที่ผิดจะถูกตรึงลง SQLite แล้วเป็นฐานของ `get_networth_change`" ไม่จริงโดยอัตโนมัติ: `save_snapshot()` บันทึก `SnapshotRequest` ที่ client ส่งมา ไม่ได้กินผลของ `get_current()` และ `report_service.get_networth_change()` เรียก `get_history()` ไม่ใช่ `get_current()`

**แนวแก้**
1. เพิ่ม `missing_prices: list[str]` + `skipped_reason: str` ใน `NetWorthResponse` แล้วเติมค่าจาก `get_holdings()` — **ใช้ชื่อคีย์ชุดเดียวกับ `/api/portfolio` ห้ามตั้งชื่อใหม่**
2. แยก `etf_live` เป็น `etf_status: "live" | "partial" | "from_snapshot" | "unavailable" | "no_holdings"` หรือถ้าจะคง bool ต้องทำ fallback ไปใช้ ETF จาก snapshot จริง ๆ ให้ตรงกับคอมเมนต์ (แล้วต้องแปะวันที่ของ snapshot นั้นด้วย)
3. `save_snapshot` ปฏิเสธ/เตือนเมื่อ ETF บางตัวไม่มีราคา (snapshot ที่ผิดจะอยู่ถาวร)

#### L-NW-1 — `get_history(months=N)` ใช้ `N × 30` วัน (low)

```
months=12  → cutoff 2025-08-11  ควรเป็น 2025-08-06  ขาด 5 วัน
months=120 → cutoff 2016-09-27  ควรเป็น 2016-08-06  ขาด 52 วัน   (router อนุญาตถึง 120)
```
**แนวแก้** `dateutil.relativedelta(months=months)` หรือคำนวณจากปี/เดือนตรง ๆ

#### L-NW-2 — ทิ้ง `is_live` ของอัตราแลกเปลี่ยน (low — ควรแก้พร้อม B10)

บรรทัด 25 เรียก `fx.get_usdthb_rate()` ซึ่ง `utils/fx.py` ระบุเองว่า "สำหรับผู้เรียกที่ไม่สนใจที่มา" · `grep -rn "get_usdthb(" --include=*.py .` → มีที่เดียวในทั้งระบบที่รับ `is_live` ไปแสดงผลคือ `dashboard/app.py:3359`
**แนวแก้** `rate, is_live = fx.get_usdthb()` แล้วเพิ่ม `fx_rate` + `fx_is_live` ใน response · แก้ `tracker.py:378` และ `rebalance_service.py:27` ด้วยเพราะเป็นแบบเดียวกัน

#### L-NW-3 — `snapshot_date = วันนี้` ให้กับข้อมูลที่ดึงมาจาก snapshot เก่าแค่ไหนก็ได้ (low, **ยังไม่ผ่านการตรวจซ้ำ**)

บรรทัด 66 `snapshot_date=date.today().isoformat()` ขณะที่ `non_etf_assets`/`liabilities` มาจาก `_latest_snapshot` ที่ไม่จำกัดอายุ
```
บันทึก snapshot 2019-01-01 (เงินสด 500,000 / สินเชื่อบ้าน 2,000,000) แล้วเรียก get_current
→ snapshot_date = '2026-08-06' พร้อมเงินสดและหนี้ก้อนเดิมของปี 2019
→ ไม่มีฟิลด์ใดบอกว่าข้อมูลเก่า 7 ปี
```
**แนวแก้** แยก `as_of_snapshot_date` (วันที่ของ snapshot ที่ใช้จริง) ออกจาก `snapshot_date` และเตือนเมื่อเก่ากว่า N วัน

---

### A6 · `backend/services/etf_service.py` + `backend/routers/etf.py` (H10 + 1 medium)

#### H10 — `ffill()` เปลี่ยน "ไม่มีแท่งราคาวันนี้" เป็น "ราคาไม่เปลี่ยน 0.00%"

`_prices_df()` บรรทัด 35 และ 53 คืน `fetch_adjusted_close_data(...).ffill()` — DataFrame ใช้ index ร่วมกันทุก ticker ตัวที่ผู้ให้ข้อมูลยังไม่ส่งแท่งของวันนั้นเป็น NaN แล้วถูกเติมด้วยราคาวันก่อน

**ผลตามมา 3 ชั้น**
1. `get_etf_daily_eod_snapshot()` หยิบ `iloc[-1]` เทียบ `iloc[-2]` ที่เท่ากันเป๊ะ → `change_pct: 0.0` และประทับ `date` เป็นวันล่าสุดของ **ticker อื่น** = วันที่ปลอม
2. `get_etf_technical()` คำนวณ RSI/MA50/MA200 บนแท่งปลอมแล้วติดป้าย `data_ok: True` → ตัวเลขที่ป้อน `signal_rules.dca_signal`
3. ผลปลอมถูกแคชไว้ 1 ชั่วโมง

**หลักฐาน** (stub เฉพาะ `fetch_adjusted_close_data` โค้ด service/router/jobs ของจริงทั้งหมด — 2 รอบรันซ้ำได้)

```
--- ข้อมูลครบ ---     GLDM: price=59.49 chg=-0.13% date=30/07/2026 | data_ok=True rsi14=69.57
--- GLDM 3 แท่งท้าย NaN --- GLDM: price=59.10 chg=0.0%  date=30/07/2026 | data_ok=True rsi14=68.48

ยิงผ่าน HTTP จริง (TestClient): GET /api/etf/daily-snapshot → 200
  GLDM = {'price':104.05,'previous_close':104.05,'change_pct':0.0,'date':'30/07/2026'}
  ทั้งที่แท่งจริงล่าสุดของ GLDM คือ 27/07/2026

ป้อนเข้า jobs/daily_check.build_discord_message ตัวจริง:
  GLDM   $59.10    (+0.00%) 🟢  (30/07/2026)

และเมื่อถอด ffill ออก (ทดลองใน /tmp ไม่แตะ repo):
  HTTP 500 {"detail":"Out of range float values are not JSON compliant: nan"}
  → ffill เปลี่ยน "ความล้มเหลวที่ดัง (500)" ให้เป็น "200 พร้อมตัวเลขที่กุขึ้น"
```

**ความรุนแรงที่ขัดกัน** R10 คง high (พิสูจน์ระดับ HTTP + RSI ขยับ 1.1 จุดจากแท่งที่กุขึ้น) · R8 ลดเป็น medium เพราะรันกับข้อมูลจริง 10 ปี (2512 แถว × 5 ETF) พบ **NaN ภายใน/ท้ายสุด = 0 ทุก ETF** (มีแต่ NaN นำหน้าก่อนวันเปิดกอง ซึ่ง ffill เติมไม่ได้) — ETF ทั้ง 5 ใช้ปฏิทินเทรดเดียวกันเป๊ะ ทริกเกอร์จึงต้องเป็น partial fetch ผิดปกติ
**ตัดสินให้ high** เพราะ partial fetch ที่ระบบยอมรับว่าเกิดได้จริง (log ของตัวเองพิมพ์ `ไม่แคช prices_10y:...: ได้ข้อมูลไม่ครบ ขาด GLDM`) และปลายทางคือข้อความ Discord ที่ผู้ใช้อ่านทุกวันทำการ **แต่ต้องบันทึกไว้ว่ายังไม่มีใครจับได้ในข้อมูลตลาดจริง**

**หักล้างข้อย่อยหนึ่ง** — ข้ออ้าง "ffill หลอกด่าน `expect_keys`/`_present_keys`" **ไม่จริง**:
```
เฟรมช่องว่างท้ายแบบ *ไม่* ffill → _incomplete_reason = None (แคชได้อยู่แล้ว)
เฟรมเดียวกันหลัง ffill        → _incomplete_reason = None (เหมือนเดิม)
คอลัมน์ว่างทั้งคอลัมน์         → 'ได้ข้อมูลไม่ครบ ขาด GLDM' → ไม่แคช (ด่านทำงานถูก)
```
เพราะ `_present_keys` ใช้ `value.notna().any()` จึงไม่เคยจับช่องว่างท้ายได้ไม่ว่าจะ ffill หรือไม่ — ผลปลอม**ถูกแคชจริง** แต่คนละสาเหตุ

**แนวแก้** หาแท่งล่าสุด **รายคอลัมน์** จากข้อมูลดิบ (`prices[t].dropna()`) แล้วรายงาน `date` ของ ticker นั้นเอง + `stale: true`/`data_ok: false` เมื่อวันล่าสุดของมันตามหลังของทั้งเฟรม · เหลือแท่งจริง < 2 → `change_pct: null` ไม่ใช่ 0.0 · `p_y <= 0` ต้องเป็น `None` ไม่ใช่ `0.0` · ย้าย `.ffill()` ไปทำเฉพาะจุดที่จำเป็น (risk/correlation) ไม่ใช่ที่แหล่งกลาง · `jobs/daily_check` ต้องเคารพธง `stale`
**หมายเหตุต่อแผนเดิม** FIX_PLAN 2.3 แก้ `_change_pct_yfinance` เท่านั้น แต่ `_row_for_ticker` เชื่อ snapshot ก่อนเสมอ ⇒ **แก้ 2.3 อย่างเดียวข้อความ Discord ยังผิดเหมือนเดิม**

#### M-ETF-1 — `/api/etf/daily-snapshot` ตอบ 500 ทั้ง endpoint เมื่อ ticker เดียวไม่มีข้อมูล (medium)

`if p_y > 0: ... else: chg = 0.0` เป็นเท็จเมื่อ `p_y` เป็น NaN (กุ 0.00% อีกจุด) และ `price` ยังเป็น nan ติดไปด้วย พอ `JSONResponse` render ด้วย `allow_nan=False` จึงระเบิด

```
GLDM เป็น NaN ทั้งคอลัมน์ → HTTP 500 {"detail":"Out of range float values are not JSON compliant: nan"}
→ VOO/SCHD/QQQM/XLV ที่ดึงได้ปกติหายไปด้วยทั้งหมด และข้อความเป็นอังกฤษที่ไม่บอกว่า ticker ไหนพัง
→ jobs/daily_check ได้ {} แล้วตกไปเส้น _change_pct_yfinance ที่คืน 0.0
```
**แนวแก้** รายงานรายตัว (`{"GLDM": {"error": "ดึงราคาไม่สำเร็จ"}}`) แทนการล้มทั้ง endpoint

---

### A7 · `.github/workflows/scheduler.yml` + `jobs/daily_check.py` + `main.py` (H12 + 4 medium + 1 low)

#### H12 — ส่ง "Daily Price Check" 2 ใบต่อวันไปเว็บฮุคเดียวกัน แล้วเลขขัดกันเอง

step `Daily price summary` (บรรทัด 66 → `main.py --job price_alert` → `check_alerts()`) กับ `Run Daily Price Check` (บรรทัด 71 → `jobs/daily_check.py`) มี `if:` เหมือนกันเป๊ะ และรับ `DISCORD_WEBHOOK_URL` ตัวเดียวกัน

```
check_alerts   : https://discord.example/webhook/AAA
daily_check.py : https://discord.example/webhook/AAA        ← เว็บฮุคเดียวกันจริง

ฐาน 1 (get_price_snapshots, 2 แท่งปิด): VOO +0.19% · SCHD -0.62% · QQQM +0.04% · XLV -0.29% · GLDM +0.41%
ฐาน 2 (fast_info previous_close):      VOO -0.17% · SCHD -0.61% · QQQM -0.72% · XLV +1.02% · GLDM +4.39%
  → วันที่วัด 3/5 ตัวพลิกเครื่องหมาย (ผู้พบวัดวันอื่นได้ 2/5 — ทิศทางเดียวกัน)
```

**ผู้ตรวจซ้ำพบว่าแรงกว่าที่รายงาน** — ความขัดแย้งไม่หายแม้ backend ทำงานปกติ ยิง production จริง:
```
curl https://vaultis-backend.onrender.com/api/etf/daily-snapshot → GLDM change_pct +4.1264% date 05/08/2026
ขณะที่ check_alerts ของวันเดียวกัน → +0.41%
```
เพราะ `get_etf_daily_eod_snapshot()` ใช้แท่งปิด EOD (เมื่อวาน) ส่วน `get_price_snapshots` ใช้แท่งวันนี้ที่ยังไม่ปิด — **ทั้งสองใบพูดถึงคนละวันแต่พาดหัวเหมือนกัน ขัดกันทุกวันทำการแบบ deterministic**

**แนวแก้** ตัด step `Daily price summary` ออก (daily_check มีสรุปราคา + สัญญาณอยู่แล้ว) หรือให้ `check_alerts()` มีพารามิเตอร์ `send_daily_summary=False` · และเปลี่ยน `_change_pct_yfinance` ไปใช้แท่งปิด 2 แท่งจาก history ให้ตรงฐานเดียวกันตามที่ FIX_PLAN 2.2 กำหนดไว้แล้ว

#### M-CI-1 — `secrets.BACKEND_URL` ที่ยังไม่ตั้ง = สตริงว่าง ทำให้ค่า default ในโค้ดไม่มีผล (medium)

```
os.environ['BACKEND_URL'] = ''  → BACKEND_URL ที่โมดูลใช้จริง = ''  (ไม่ใช่ค่า default)
URL ที่จะยิง = '/api/etf/daily-snapshot'
Daily snapshot error: Invalid URL: No scheme supplied.  → snapshot = {}
Backend error: ...                                       → prices = {}
ลบตัวแปรทิ้ง → 'https://vaultis-backend.onrender.com'   ← '' กับ 'ไม่มี' ให้ผลต่างกันจริง
```

**จุดอ่อนของข้ออ้าง** ผู้ตรวจพิสูจน์ "ผลของ env ว่าง" ได้ แต่พิสูจน์ไม่ได้ว่า secret ยังไม่ถูกตั้งจริง (ดู GitHub secrets จากเครื่องนี้ไม่ได้) — คำว่า "ทุกวัน" ยังเป็นเงื่อนไข

**แต่มีเส้นทางที่แข็งกว่าและไม่ต้องพึ่งสมมติฐานนั้นเลย** (ผู้ตรวจซ้ำวัดเอง):
```
curl -w '%{http_code} in %{time_total}s' --max-time 90 https://vaultis-backend.onrender.com/health
→ 200 in 80.78s          (cold start ของ Render free)
ขณะที่ jobs/daily_check.py ใช้ requests timeout=30 ทั้งสองคำขอ
```
⇒ ถึง secret ถูกตั้งถูกต้อง CI ก็ได้ `{}` แล้วตกเส้นทาง `fast_info` อยู่ดี

**แนวแก้** `BACKEND_URL = (os.environ.get("BACKEND_URL") or "").strip() or "<default>"` (สำนวนเดียวกับที่ `backend/database.py` ใช้อยู่แล้ว) · เพิ่ม timeout ให้ครอบ cold start หรือยิง warm-up ก่อน · ใส่บรรทัด "⚠️ ดึงจาก backend ไม่ได้ ใช้ค่าสำรองจาก yfinance" ลงข้อความ Discord ไม่ใช่แค่ `print`

#### M-CI-2 — Weekly Summary กุ "1W +0.00%" จาก `ffill` (medium)

`main.py` `generate_weekly_report_and_notify()` คำนวณ `((prices.ffill().iloc[-1] / prices.ffill().iloc[-6]) - 1) * 100`

```
GLDM ไม่มีข้อมูล 15 แท่งท้าย (3 สัปดาห์) ราคาจริงล่าสุด 99.0759 เมื่อ 2025-06-20
--- ข้อความ Weekly Summary ที่ส่งเข้า Discord ---
VOO: RSI 38.9 | 1W -1.33% | 1M -2.75%
SCHD: RSI 27.6 | 1W -4.09% | 1M -10.12%
GLDM: RSI 51.6 | 1W +0.00% | 1M +1.38%     ← หน้าตาเป็นแถวปกติทุกไบต์
is_positive = True                          ← 0.00% ถูกนับเข้า positive_count (กำหนดสีของ embed ทั้งใบ)
```
`data/fetcher.py` ใช้ `dropna(how="all")` ซึ่งตัดเฉพาะแถวที่ NaN ทุกคอลัมน์ → คอลัมน์เดียวที่ NaN ท้าย ๆ รอดมาถึง `main.py` แน่นอน
**แนวแก้** เลิก `ffill()` บนเส้นทางรายงาน → แสดง `GLDM: ⚠️ ดึงข้อมูลไม่ได้` แบบเดียวกับ `_build_daily_status_message` และห้ามนับเข้า `positive_count`

#### M-CI-3 — `generate_daily_technical_alerts()` มี `ffill` เดียวกัน แต่ปลายทางเป็น**สัญญาณ** (medium, พบโดยผู้ตรวจซ้ำ)

งานนี้ตั้งไว้ทุกวัน 09:00 ใน scheduler
```
GLDM ราคาจริงล่าสุด 105.0 เมื่อ 2025-06-20 · แถวสุดท้ายของกรอบข้อมูล 2025-07-11
แจ้งเตือนที่ถูกสร้าง: {'symbol':'GLDM','rsi':5.6981,'price':105.0,'ma200':125.9483,'previous_price':105.0}
```
ราคาเก่า 3 สัปดาห์ถูกส่งเป็นราคาวันนี้ และ `previous_price` เท่ากับ `price` เป๊ะ (ตัวตัดสินทิศทางใน `alerts/notifier` ตาบอดทันที) โดยไม่มีสัญลักษณ์เตือน

#### M-CI-4 — scheduler ตายถาวรเมื่อ job ใดโยน exception (medium)

`run_scheduler()` ครอบ `while True: schedule.run_pending()` ด้วย `except Exception as exc: print(...)` ที่**อยู่นอกลูป**
```
run_pending ถูกเรียก 3 ครั้ง → run_scheduler ยังมีชีวิตอยู่ไหม: False
jobs ที่เหลือรอ: 2 (แต่ไม่มีใครเรียก run_pending อีกแล้ว)
```
`check_alerts` เป็น job เดียวที่ลงทะเบียนโดยไม่มี try/except ของตัวเอง ใน Docker `restart: unless-stopped` ช่วยกู้ แต่ CLAUDE.md บอกให้รัน `python main.py` ตรง ๆ ได้ด้วย
**แนวแก้** ห่อ `run_pending()` ด้วย try/except **ในลูป** + wrapper `_safe(job)` ตอนลงทะเบียนทุก job

#### L-CI-1 — `permissions: contents: write` ที่ไม่มีอะไรใช้แล้ว (low)

step "Persist triggered alerts" ที่คอมเมนต์อ้างถึงถูกลบไปแล้ว `grep -nE "git |commit|push|actions/upload"` ไม่พบอะไรเหลือ — repo เป็น public และ `GITHUB_TOKEN` ยังถือสิทธิ์เขียนโค้ดกลับเข้า repo โดยไม่มีอะไรต้องใช้ (workflow ไม่ pin hash / ไม่มี `--require-hashes`)
**แนวแก้** `contents: read` + ลบคอมเมนต์ที่อ้าง step ที่ไม่มีอยู่จริง

#### หมายเหตุ: step "Run Sentiment Analysis" — **ไม่ใช่บั๊ก** (ดูหัวข้อ "ข้อที่ถูกหักล้าง")

---

## เฟส B — medium ที่ไฟล์ไม่ทับกัน (ทำขนานได้ 11 สาย)

### B1 · `backend/services/cashflow_service.py` + `backend/models/cashflow_models.py` + `backend/routers/cashflow.py` (6 ข้อ)

ทั้งกลุ่มนี้ **ยังไม่มีหน้าจอเรียกใช้** (`grep 'api/cashflow' dashboard/` = ว่าง) และ **ไม่มีเทสต์สักตัว** — ผลกระทบวันนี้จำกัดที่ผู้เรียก REST API แต่ทุกข้อผ่านการตรวจซ้ำแล้ว

| # | อาการ | หลักฐาน |
|---|---|---|
| B1.1 | เดือนที่ยังไม่จบถูกเฉลี่ยเป็นเดือนเต็ม | `n_months = len(by_month)` นับทุกคีย์ `YYYY-MM` รวมเดือนปัจจุบัน |
| B1.2 | scenario ที่ระบุหมวดไม่ตรงถูกทิ้งเงียบ | `if cat not in cat_monthly: continue` |
| B1.3 | scenario ซ้ำหมวดเดียวกัน อันก่อนหน้าถูกทับ | `category_multiplier[cat] = delta` (assign ไม่ใช่สะสม) |
| B1.4 | วันที่ผิดรูปสร้าง bucket ขยะ | `_month_key()` = `date_str[:7]` ไม่ตรวจอะไรเลย |
| B1.5 | `change_percent` ไม่มีขอบเขต → รายจ่ายติดลบ = เสกเงินเข้า | ประกาศเป็น `float` เปล่า ๆ ไม่มี `ge/le` |
| B1.6 | `detect_category_anomalies` มองไม่เห็นหมวดที่เพิ่งเกิดครั้งแรก | `if avg_val == 0: continue` |

**หลักฐานรวม** (ข้อมูล: มิ.ย.+ก.ค. เต็มเดือน เงินเดือน 60,000 วันที่ 25 รายจ่าย 50,000/เดือน + ส.ค. 1–6 มีแต่รายจ่าย 9,000)

```
B1.1  เต็มเดือนล้วน: n_months=2 avg_income=60,000.00 avg_expense=50,000.00 ending12=250,000.00 threshold(×3)=150,000.00
      มี ส.ค. ค้าง:  n_months=3 avg_income=40,000.00 (-33%) avg_expense=36,333.33 (-27%) ending12=174,000.00 threshold=109,000.00
      emergency_alert: ความจริง True ('⚠️ ยอดเงินคาดการณ์เดือน 2026-09 ต่ำกว่า emergency fund 150,000 บาท') vs ระบบ False
      anomalies ปลอม: ระบบแจ้ง [('อื่นๆ',-100.0),('อาหาร',-85.0),('ที่พัก',-76.0)] vs ข้อมูลเต็มเดือน []
      grep: บรรทัด 5 `from calendar import monthrange` import แล้วไม่ใช้ · บรรทัด 110 `total_exp_per_month` คำนวณแล้วไม่อ่าน
      → เป็นงานที่ทำค้างไว้จริง ไม่ใช่การออกแบบ

B1.2  ไม่ใส่ scenario: expense=50,000.00 | 'อาหารการกิน' (สะกดไม่ตรง): 50,000.00 | 'อาหาร' (ตรง): 42,000.00
      wrong.model_dump() == base.model_dump() → True  (เหมือนไม่ใส่เลยทุกฟิลด์, HTTP 200)

B1.3  [-50] อย่างเดียว: expense=40,000.00 | [-50,-10] สองรายการ: 48,000.00 | [-10] อย่างเดียว: 48,000.00
      → สองรายการให้ผลเท่ากับ '-10 อย่างเดียว' = รายการแรกถูกทิ้งทั้งใบ

B1.4  ใส่ธุรกรรมวันที่ '' และ '06/08/2026' อย่างละ 1,000 บาท
      buckets = ['', '06/08/2', '2026-06', '2026-07']   avg income 60,000.00 → 30,000.00

B1.5  ที่พัก 25,000 จากรายจ่ายรวม 50,000
      change_percent=-100 → projected_expense=25,000 (ถูก)
      change_percent=-300 → projected_expense=-25,000.00  net=+85,000.00  ending=185,000.00  ← HTTP 200 ไม่มีคำเตือน

B1.6  เพิ่ม 'ค่ารักษาพยาบาล' 80,000 บาทในเดือนล่าสุดที่ไม่เคยมีมาก่อน → anomalies = []
      (รายจ่ายก้อนใหญ่ที่สุดในชุดข้อมูลและตรงนิยาม "ผิดปกติ" ที่สุด กลับตรวจไม่เจอ)
```

**ความแม่นยำของการประเมิน (ถ่วงดุล)** — B1.1 ผู้พบให้ high แต่ผู้ตรวจซ้ำลดเป็น medium เพราะการพลิกของ `emergency_alert` ไม่ได้เกิดเสมอ (ต้องมี `current_balance` ตกในช่วงระหว่าง threshold ผิดกับ threshold จริง) สิ่งที่เกิด**เสมอ**คือ threshold ต่ำไป 27% และค่าเฉลี่ยต่ำไป 33%/27% · B1.4 ผู้พบอ้างว่า "เกิดง่ายมากจาก import CSV/OCR" แต่ในเรพ **ยังไม่มี importer ใด ๆ** ที่สร้าง `TransactionItem` (คอมเมนต์ใน router เขียนเองว่า "will connect to DB/OCR later") ⇒ ความถี่จริงต่ำกว่าที่อ้าง

**แนวแก้**
- B1.1 ใช้ `monthrange` ที่ import ไว้อยู่แล้วหา "เดือนสุดท้ายที่ครบ" แล้วตัดเดือนค้างออกจากทั้งค่าเฉลี่ยและ `last_month` ของ anomaly · **ห้ามตัดเงียบ** — เพิ่ม `months_used` / `excluded_partial_month` ใน `ForecastResponse` · ไม่มีเดือนครบเลย → บอกตรง ๆ ว่าพยากรณ์ไม่ได้
- B1.2 หมวดที่ไม่พบ → `raise ValueError` (router แปลงเป็น 422 อยู่แล้วในกรณี debt) หรืออย่างน้อย `unknown_categories: [...]` แล้วบังคับให้หน้าจอแสดง
- B1.3 สะสมด้วย `+=` หรือปฏิเสธ scenario ซ้ำหมวด
- B1.4 เปลี่ยน `TransactionItem.date` เป็น `datetime.date` ให้ Pydantic ปฏิเสธเอง (ได้ 422 ฟรีพร้อมชี้แถว)
- B1.5 `change_percent: float = Field(ge=-100, le=1000)`
- B1.6 หมวดใหม่ที่โผล่ครั้งแรกด้วยยอดสูง = anomaly ชนิด `new_category` ไม่ใช่ข้าม

**เทสต์**: ไฟล์ใหม่ `tests/test_cashflow_service.py` (ตอนนี้ **ไม่มีเทสต์ cashflow สักตัว**)

---

### B2 · `analysis/sentiment_analyzer.py` + `analysis/sentiment_aggregator.py` (2 ข้อ)

**อยู่ในรายการ "ยังไม่ผ่านการตรวจซ้ำ" ของ FIX_PLAN (`sentiment_analyzer.py:84,144`) — รอบนี้ยืนยันแล้ว 2 รอบอิสระ ย้ายเข้าคิวแก้ได้**

#### B2.1 — batch ที่เรียกโมเดลไม่สำเร็จหายเงียบ จนป้าย sentiment พลิก

```
stub sa.chat_text ทั้งหมด (assert user_initiated is False) ป้อนข่าว 30 ชิ้น = 3 batch (บวก10/ลบ10/กลาง10)
  ครบทุก batch  → total_articles=30 pos=10 neg=10 neu=10 score=+0.0000 overall=neutral
  batch 2 ล้ม   → total_articles=20 pos=10 neg=0  neu=10 score=+0.5000 overall=positive
  เหลือแค่ logger.warning('[VOO] sentiment batch 2/3 ล้มเหลว: 429 rate_limit_error')
  คีย์ของ aggregate: ['avg_confidence','negative','neutral','overall_sentiment','positive','score','symbol','total_articles']
  → ไม่มีฟิลด์ "ดึงมา 30 วิเคราะห์ได้ 20"

ทุก batch ล้ม → total_articles=0 overall='neutral' score=0.0
  run_sentiment_job(['VOO']) → **เขียน SentimentSummary จริง 1 แถว** แล้วพิมพ์ '[VOO] sentiment done: neutral score=0.0'

_normalize_row({'title':'x'}) → sentiment 'neutral' | label 'bullish' ×3 → neutral 3
```

ปลายทาง: `dashboard/app.py` `_render_sentiment_context_box` พิมพ์ `⚪ **VOO** — neutral · score +0.00 · 0 ข่าว` · `backend/routers/sentiment.py` ทำ `str(row.overall_sentiment or 'neutral')` และ `float(row.score or 0.0)` ซ้ำอีกชั้น

**invariant ยังสะอาด** — `grep -rn "sentiment" analysis/financial_model.py portfolio/targets.py technical/ backend/screener/` = 0 บรรทัด ⇒ ไม่ไหลเข้าคะแนน/DCA (นี่คือเหตุผลที่จำกัดที่ medium)

**แนวแก้** ขยายสัญญา `aggregate_sentiment(results, *, fetched=None, failed_batches=0)` → คืน `analyzed`, `fetched`, `coverage_pct`, `complete` และ **คืน `overall_sentiment="unknown"` เมื่อ `analyzed == 0`** แทน `"neutral"` · `analyze_batch` คืนจำนวน batch ที่ล้ม · `run_sentiment_job` ไม่เขียน SentimentSummary เมื่อ `analyzed == 0` · หน้าจอแสดง "วิเคราะห์ได้ 20 จาก 30 ข่าว"

#### B2.2 — แหล่งข่าวพังทั้งหมดถูกอ่านเป็น "ไม่มีข่าว" (low หลังตรวจซ้ำ)

`run_sentiment_job()` เรียก `get_news(sym)` (คืนรายการเปล่า ๆ) แล้วตัดสินด้วย `if not articles: print(f"[{sym}] no news; skip")` ทั้งที่ `get_news_with_status()` มีอยู่แล้วและคืน `has_error`/`all_news_sources_failed` มาให้ตรง ๆ — CLAUDE.md เขียนกฎนี้ไว้เอง หน้า News ทำถูก แต่ job นี้ตกหล่น

```
inspect: "get_news_with_status" in source(run_sentiment_job) → False
         hasattr(news_fetcher,"get_news_with_status")        → True
```

**ลดจาก medium เป็น low** เพราะ (ก) `_render_sentiment_context_box` พิมพ์ `created_at` ของแถวออกมาด้วย ⇒ ผู้ใช้ไม่ได้เห็นของเมื่อวานเป็นสถานะวันนี้แบบมองไม่ออก (ข) `.env` ของเครื่องนี้ `VAULTIS_LLM_AUTO=` (ว่าง) ⇒ job ออกตั้งแต่ `auto_enabled()` ไม่เคยไปถึงบรรทัดที่ผิด

---

### B3 · `analysis/backtest_engine.py` + `backend/routers/backtest.py` + `backend/models/backtest_models.py` (5 ข้อ)

`grep -rn "api/backtest" dashboard/` = ว่าง — **ไม่มีผู้บริโภคในแดชบอร์ดหรือ job อัตโนมัติ** ทุกข้อจึงจำกัดที่ "คำตอบที่ผิด" ไม่ใช่ "เงินที่จัดสรรผิด"

#### B3.1 — กลยุทธ์ที่ไม่เคยเทรดถูกรายงานว่า "ชนะดัชนี" พร้อม maxDD/win-rate = 0.0

บล็อก `else` ที่ `num_trades == 0` ยัด `total_return=0.0, sharpe=0.0, max_drawdown=0.0, win_rate=0.0` แล้วบรรทัดถัดมาคำนวณ `outperformed = total_return > bh_return` จากศูนย์ปลอมนั้น

```
สแกนหน้าต่าง 6 เดือนจริง (yfinance auto_adjust) 5 ETF ตั้งแต่ 2011 → พบ 11 หน้าต่างที่ trades=0 แต่ outperformed=True
  QQQ 2022-03-09..2022-09-14: trades=0 tot=0.0 maxDD=0.0 win=0.0 benchmark=-12.0486 OUTPERFORMED=True
  VOO 2022-03-09..2022-09-14: trades=0 ...            benchmark=-7.3344  OUTPERFORMED=True
  VOO 2024-01-01..2024-07-01: trades=0 ...            bh=+16.016         OUTPERFORMED=False (ขาขึ้นก็คืนศูนย์ปลอม)
BacktestResponse ประกาศ outperformed: bool (ไม่มี null ไม่มีฟิลด์สถานะ)
```
**ลดเป็น medium** เพราะ `num_trades: 0` อยู่ใน payload เดียวกัน ผู้อ่านที่ระวังตัวจับได้

#### B3.2 — `_sharpe_for` ยุบ 3 ความหมายเป็น `0.0` เดียว

`return 0.0` ทั้งกรณีไม่มีเทรด, Sharpe เป็น NaN, และ `except Exception:` → `optimize()` เลือก `max()` จากเลขชุดนั้น และเพราะ `0.0 > float('-inf')` เสมอ `best_params` ถูกเซ็ตตั้งแต่รอบแรกทุกครั้ง ทำให้ด่าน `if not best_params:` ("ไม่พบพารามิเตอร์ที่ให้สัญญาณเลย") เป็น**โค้ดตาย**

```
_sharpe_for(pd.DataFrame({'Close':[100,101,99]}), 14, 30.0) → 0.0  (ไม่ raise ทั้งที่ 3 แถวคำนวณ RSI14/MACD ไม่ได้)
QQQ 2021-06-01..2023-06-01: combo (21,25) ที่ trades=0 บน train ได้ 0.0 → อันดับ 3 จาก 12 เหนือค่าติดลบจริง 9 ตัว
best_params (10,25) ทำ trades=0 บน **test** แต่รายงาน test_sharpe: 0.0 (buy&hold ช่วง test = +26.85%)
เกิดซ้ำอีก 3/6 หน้าต่างในสแกน (QQQ 2021-11, GLD 2012-10, GLD 2011-09)
```

**หักล้างพาดหัวของผู้พบบางส่วน** — "ตัวที่ไม่เทรดถูกเลือกเป็น best" ผู้ตรวจซ้ำรัน optimize บนข้อมูลจริง 12 หน้าต่าง (VOO/SCHD/QQQ/XLV/GLD ทั้งขาขึ้นและตลาดหมี 2022, ทองปี 2012–2014) → **`BEST_IS_ZERO_TRADE = False` ทุกหน้าต่าง** เคสที่ผู้พบโชว์เป็นซีรีส์สังเคราะห์ ⇒ บั๊กจริงคือ "ตัวเลขที่รายงานถูกกุ" ไม่ใช่ "เลือกพารามิเตอร์ผิดบ่อย"

#### B3.3 — `train_sharpe` ปัดพื้นค่าติดลบเป็น 0.0 (พบโดยผู้ตรวจซ้ำ — รุนแรงกว่า B3.1)

`"train_sharpe": round(max(best_sharpe_train, 0.0), 4)`
```
GLD 2012-10-01..2014-06-01 → all_results ติดลบ **ทั้ง 12 combo** (ดีที่สุด -1.0947, แย่สุด -1.6049)
แต่ endpoint คืน train_sharpe: 0.0 และ test_sharpe: 0.0 พร้อม note ว่า "ให้ดู test_sharpe"
→ ทุกตัวเลขที่ผู้ใช้เห็นเป็นศูนย์ปลอม ทั้งที่ความจริงคือ "จูนแล้วแพ้ทุกชุด และชุดที่เลือกไม่ส่งสัญญาณเลยนอกกลุ่มตัวอย่าง"
→ ไม่มีฟิลด์ใดใน dict ที่ทำให้จับได้ (ต่างจาก /api/backtest ที่ยังมี num_trades ติดมา)
```

#### B3.4 — `strategy_used` และคำเตือน overfit หายก่อนถึงผู้ใช้

`BacktestEngine.run()` คืน `strategy_used` = `"rsi_macd_3day_window"` หรือ `"rsi_only_fallback"` พร้อมคอมเมนต์ที่ `backtest_engine.py:73` ว่า *"ต้องรายงาน strategy ที่ใช้จริงกลับไปเสมอ — ห้ามสลับเงียบ (AUDIT.md M2)"* แต่ `BacktestResponse` ไม่ประกาศฟิลด์นี้และ pydantic v2 ใช้ `extra='ignore'` → ถูกทิ้งเงียบสนิท

```
engine.run() → strategy_used='rsi_only_fallback'  |  response มีคีย์นี้ไหม: False
engine.optimize() keys: ['all_results','best_params','note','test_period','test_sharpe','train_period','train_sharpe']
endpoint keys        : [..., 'best_params', ...]  → test_sharpe/note หายทั้งคู่

ข้อมูลจริง 2021-01-01→2026-01-01: VOO/QQQM/XLV = rsi_only_fallback, SCHD/GLDM = rsi_macd_3day_window
→ 3 ใน 5 ETF ผู้ใช้ได้ผลของ RSI ล้วนโดยไม่มีอะไรบอก (ไม่ใช่เคสขอบ)
```

#### B3.5 — สาขา `except PriceDataUnavailableError → 503` เป็นโค้ดตาย (low)

```
stub backtest_engine.yf.download → pd.DataFrame() (สิ่งที่ yfinance คืนเมื่อดึงไม่ได้จริง ไม่ raise)
→ status_code = 400 | detail = 'backtest ไม่สำเร็จ: ดึงข้อมูลราคา VOO ไม่สำเร็จ (ผลว่าง)'
"PriceDataUnavailableError" in source(analysis.backtest_engine) → False
```
`BacktestEngine.fetch_data` ยิง `yf.download` เอง ไม่ผ่าน `data/fetcher` จึงไม่ได้ retry 3 ครั้ง ไม่ได้ fallback Stooq/AlphaVantage และไม่มีทางโยน exception ชนิดที่ router ดักไว้ ⇒ "ดึงไม่สำเร็จ" ถูกรายงานเป็น **400 = ความผิดของผู้เรียก**

**แนวแก้รวมของ B3** `num_trades == 0` → คืน `None` ทุกช่องที่ไม่นิยาม + `outperformed: None` + `detail` ภาษาไทย · `_sharpe_for` คืน `None` แยกจาก 0.0 และปล่อย exception ให้ดัง · เลิก `max(..., 0.0)` · เพิ่ม `strategy_used` และบล็อก `optimization: {...}` เข้า `BacktestResponse` · ให้ `fetch_data` ผ่าน `data/fetcher`
**เทสต์**: `grep -rn "strategy_used" tests/` = ว่างเปล่า · เพิ่มเทสต์ซีรีส์ที่ RSI ค้าง ~33 ไม่เคยต่ำกว่า 30 → assert `outperformed is not True`

---

### B4 · `backend/services/rebalance_service.py` (2 ข้อ)

#### B4.1 — ไม่ได้ใช้ `get_target_weights()` (ละเมิด "นิยามมีที่เดียว")

`TARGET_WEIGHTS = RISK_PROFILES` (บรรทัด 16) อ่าน preset ดิบ ทั้งที่คอมเมนต์เหนือบรรทัดนั้นเขียนเองว่า "สัดส่วนเป้าหมายมาจากแหล่งเดียว (portfolio/targets.py)"

```
stub target_weights = VOO .60/SCHD .10/QQQM .10/XLV .10/GLDM .10
  get_target_weights([...])                    = {'VOO':0.6,'SCHD':0.1,...}
  rebalance_service.TARGET_WEIGHTS['moderate'] = {'VOO':0.35,'SCHD':0.25,...}
  compute_rebalance([], 'moderate', 350000.0)  = [('VOO','buy',122500),('SCHD','buy',87500),...]
  → VOO ควรได้ 210,000 ต่าง 87,500 บาทใน ETF ตัวเดียว

และเข้าถึงได้ผ่าน UI ไม่ใช่แค่แก้ config มือ (หน้า Settings มี add_ticker()):
  tickers=[...,'VT'] → get_target_weights มี VT 0.1428  แต่ rebalance actions ไม่มี VT เลย
```

**ทำไม medium ไม่ใช่ high** `grep -rn 'rebalance' dashboard/app.py` → dashboard **ไม่เคยเรียก endpoint นี้** (ใช้ `portfolio/cashflow_rebalance.py` + `get_target_weights` ตรง ๆ) · `config.json` `portfolio.target_weights = {}` (ว่าง) และหน้า Settings เขียนเองว่าต้องแก้ config.json ด้วยมือ ⇒ ยังไม่เกิดกับผู้ใช้จริงวันนี้
**ข้อควรระวังตอนแก้** endpoint รับ `risk_profile` จาก payload ขณะที่ `get_target_weights()` อ่าน `risk_profile` จาก config.json เอง — เปลี่ยนไปเรียกดื้อ ๆ จะทำให้พารามิเตอร์ของ API ไร้ผล **ต้องออกแบบ signature ใหม่ก่อน**

#### B4.2 — ticker ที่ถืออยู่แต่ไม่มีในเป้าหมายถูกตัดเงียบ แล้วแผนสั่งซื้อเกินงบ (พบโดยผู้ตรวจซ้ำ)

```
compute_rebalance([{'symbol':'VT','shares':100}], 'moderate', budget=0.0)
  → needs_rebalance=True  max_drift_pct=35.0
  → actions = [('VOO','buy',3500),('SCHD','buy',2500),('QQQM','buy',2000),('XLV','buy',1000),('GLDM','buy',1000)]
     รวมสั่งซื้อ 10,000 USD (≈350,000 บาท) ทั้งที่งบ = 0 · ไม่มีคำสั่งขายเลย · VT ไม่ปรากฏ
     detail='' และ missing_prices=[] → ไม่มีคำเตือนใด ๆ
```
เหตุ: `total_usd = มูลค่าที่ถือทั้งหมด + budget` แต่วนเฉพาะ `for sym, target_w in target.items()` ⇒ มูลค่าของ ticker นอกเป้าถูกนับเข้าตัวตั้ง แต่ตัวมันเองไม่มีวันได้ action
**FIX_PLAN 1.3 ครอบเฉพาะกรณีราคาหาย ไม่ได้แตะเคสนี้**

---

### B5 · `backend/routers/etf_analysis.py` + `backend/services/etf_info_service.py` — แคชความล้มเหลว 6 ชม.

`ETFInfoService.get_info()` ปิดท้ายด้วย `except Exception: return ETFInfo(symbol=sym)` = sentinel ที่ทุกฟิลด์เป็น `None` แต่ **ไม่มีคีย์ `data_ok`** และ **ไม่ว่างเปล่า** → `is_cacheable()` มองว่าแคชได้ → เก็บไว้ `ETF_INFO_TTL = 21600` วิ (6 ชม.) และผู้เรียกไม่ส่ง `expect_keys` มาให้ตรวจเลย

```
stub yf.Ticker ให้ราise รอบแรก แล้วคืนข้อมูลจริงรอบหลัง:
  1) ตอน yfinance ล่ม   → name=None price=None yield=None
  2) yfinance กลับมาแล้ว → name=None price=None yield=None | จำนวนครั้งที่ยิง .info จริง = 1
  3) หลังล้างแคชด้วยมือ → name='Vanguard S&P 500 ETF' price=520.0 yield=1.24

รากของช่อง:
  CacheService: dict ว่าง            → None ✓
  CacheService: data_ok=False        → None ✓
  CacheService: sentinel ค่า None ล้วน → {'symbol':'VOO','price':None,'name':None}   ← รูรั่ว
```

**นี่คือช่องที่ FIX_PLAN 1.4 ปิดไม่ถึง** — ด่านที่เพิ่มเข้ามาจับได้แค่ `None` / คอลเลกชันว่าง / `data_ok=False`

**หักล้างครึ่งหลังของข้ออ้าง** — "technical `price=0.0` ถูกแคชทำให้ 500 ค้าง 15 นาที" **ไม่จริงในโค้ดจริง**: ผู้ตรวจซ้ำจำลองความล้มเหลวตามเส้นทางจริง (`TechnicalService._compute` raise `ValueError('no OHLCV')` เมื่อ df ว่าง — มีคอมเมนต์ห้าม fallback `price=0` กำกับไว้เอง) ได้
```
call#1 → 500, upstream tech=1
call#2 → 500, upstream tech=2      ← ลองดึงใหม่จริง ไม่ถูกแคช
```
เคสที่ผู้พบแสดงต้อง stub ให้ service **คืน** `TechnicalIndicators(price=0.0)` ซึ่งเกิดได้เฉพาะตอน download สำเร็จแต่ Close แถวสุดท้ายเป็น NaN ไม่ใช่ "yfinance สะดุด"

**ผลกระทบจริงแคบกว่าที่บรรยาย** — `grep -rn "/api/etf/" dashboard/*.py` = ว่างเปล่า (dashboard เรียก `analysis/` ตรง ๆ) ผู้บริโภคมีแค่ `/api/etf/{symbol}` และ `/api/etf/compare` ฝั่ง API และในกรณี yfinance ล่มทั้งตัว technical ก็ล้มด้วย → 500 ผู้ใช้จึงไม่ได้เห็น "กองทุนไม่มีชื่อ" เงียบ ๆ เคสที่ตรงตามอ้างคือ ".info ล้มขณะ history ยังดึงได้"

**แนวแก้** `ETFInfoService.get_info` เลิกกลืน exception (raise ขึ้นไปแบบเดียวกับ `TechnicalService` ที่ทำถูกแล้ว) หรืออย่างน้อยติดธง `data_ok=False` · `CacheService.set` เพิ่มเกณฑ์: dict ที่ทุกค่า (นอกจากคีย์ระบุตัวตนเช่น `symbol`) เป็น `None`/NaN = ไม่แคช โดยใช้ `_has_value()` ที่มีอยู่แล้วในไฟล์ **ห้ามเขียนตัวที่สอง** · ย้าย `_cache.set(tkey, ...)` ไปหลังด่าน `_technical_fetch_failed`

---

### B6 · `backend/screener/` — event loop + คำขอซ้ำ (2 ข้อ)

#### B6.1 — blocking I/O บน event loop ของ FastAPI + `misfire_grace_time` (medium)

`run_daily_screener` เป็น `async def` ที่ APScheduler (`AsyncIOScheduler`) รันบน event loop **เดียวกับ uvicorn** แต่ข้างในเรียก `engine.run()` ซึ่งยิง `yfinance.download()` แบบ sync ตรง ๆ — กฎนี้โปรเจกต์รู้อยู่แล้วและเขียนกำกับไว้เองที่ `backend/routers/websocket.py:70` ว่า *"yfinance เป็น sync I/O — ต้องออกจาก event loop ไม่งั้น API ทั้งตัวค้าง (AUDIT.md M13)"* · `routers/screener.py` ทั้ง 2 endpoint ก็เป็น `async def` เรียก `_engine.run()` ตรง

```
stub yfinance.download → time.sleep(0.25) (ของจริงช้ากว่านี้มาก)
  screener ใช้เวลา 5.05s | heartbeat (ตัวแทน request) ตื่นได้ 10 ครั้ง จากที่ควรได้ ~505
  ช่วงที่ loop ถูกบล็อกนานสุด = 5.063s (ควร ~0.01s)

job_defaults = {'misfire_grace_time': 1, 'coalesce': True, 'max_instances': 1}
  WARNING apscheduler: Run time of job ... was missed by 0:00:02.201315
  → ถูกข้ามเงียบ ๆ (misfire)
```

**แก้ถ้อยคำ 2 จุด** (ก) `misfire_grace_time: 1` **ไม่ใช่ค่าที่โปรเจกต์ตั้ง** — `backend/main.py` สร้าง `AsyncIOScheduler(timezone="Asia/Bangkok")` เปล่า ๆ นี่คือดีฟอลต์ของ APScheduler (ข) "สแกนหายทั้งวัน" ยังเป็นความเป็นไปได้ ไม่ใช่สิ่งที่พิสูจน์ในสภาพจริง — ต้องมีคำขอที่บล็อกคร่อม 07:00:00 พอดี
**แนวแก้** `await asyncio.to_thread(engine.run, ...)` ทั้งใน `scheduler_job` และ `routers/screener.py` (และ `routers/forecast.get_forecast` เช่นกัน หรือเปลี่ยนเป็น `def` ธรรมดาให้ FastAPI โยนเข้า threadpool) · ตั้ง `job_defaults={'misfire_grace_time': 3600, 'coalesce': True}` · เพิ่ม `EVENT_JOB_MISSED`/`EVENT_JOB_ERROR` listener ที่แจ้งเตือนออกไป

#### B6.2 — ดาวน์โหลดข้อมูลชุดเดียวกัน 4 รอบต่อสัญลักษณ์ (low)

```
('GLDM','1y')=4  ('QQQM','1y')=4  ('SCHD','1y')=4  ('VOO','1y')=4  ('XLV','1y')=4
รวม = 20 ครั้งต่อเช้า สำหรับ 5 สัญลักษณ์ (ต้องการจริง 5)
```
`run_daily_screener` วน 4 preset เรียก `engine.run(symbols, preset)` ทีละรอบ และ `_fetch_df` ยิงใหม่ทุกครั้ง ไม่ผ่านแคชใด ๆ
**เป็นตัวคูณความเสี่ยง ไม่ใช่ต้นเหตุ** — ผลของ rate limit คือ "ไม่มีสัญญาณวันนี้" ซึ่ง FIX_PLAN 2.1 คุมอยู่แล้ว **แต่ 2.1 จะเปลี่ยน `period` เป็น `2y` ซึ่งทำให้แต่ละคำขอหนักขึ้น 2 เท่า × 4 รอบ ⇒ ต้องแก้ไปพร้อมกัน**
**แนวแก้** ดึงราคาครั้งเดียวต่อ symbol ต่อรอบแล้วส่ง DataFrame เข้า `engine.run(symbols, preset, frames=...)`

#### B6.3 — โบนัส `+1.0` เมื่อ RSI > 65 ในพรีเซ็ตฝั่งซื้อ (low — อยู่ในรายการ "ยังไม่ยืนยัน" ของแผน ตอนนี้ยืนยันแล้ว)

```
ร้อน  RSI=70.33 → strength=8.0
กลาง  RSI=48.02 → strength=7.0
เย็น  RSI=26.48 → strength=8.5
signal_rules.overall_signal(BULLISH, golden_cross=True, rsi=72) = buy   (rsi_ok = rsi < 65)
signal_rules.overall_signal(BULLISH, golden_cross=True, rsi=50) = strong_buy
```
เกณฑ์ 35/65 ถูกเขียนซ้ำในเอนจินเอง ไม่ได้อ่านจาก `signal_rules.RSI_OVERSOLD/RSI_OVERBOUGHT` (30/70)
**ลดจาก medium เป็น low** เพราะ (ก) job รัน `overbought_warning` ด้วย — โบนัส >65 ให้ประโยชน์กับพรีเซ็ตนั้นตรง ๆ บั๊กที่แท้จริงคือฟังก์ชัน "ไม่รู้ทิศของพรีเซ็ต" ไม่ใช่ "โบนัสผิดเสมอ" (ข) การกลับลำดับจำกัดที่คู่ overbought(8.0) เหนือ neutral(7.0) — oversold ยังได้ 8.5 อยู่บนสุด (ค) ข้อความมี `matched_rules` กำกับทุกแถว ผู้ใช้ยังแยกออก (ง) ไม่มีเส้นทางไปแตะเงิน

---

### B7 · `backend/routers/websocket.py` — ไม่มีช่องรายงาน ticker ที่ดึงไม่ได้

`_fetch_ticker_snapshot()` คืน `None` เมื่อดึงไม่ได้ (ถูกต้อง ห้าม broadcast 0.0 ปลอม) แต่ `_price_broadcast_loop` แค่**ข้ามตัวนั้นไป**

```
ข้อความที่ broadcast: {"type":"price_update","data":{VOO,SCHD,QQQM,XLV}}
tickers ที่ประกาศไว้  : ['VOO','SCHD','QQQM','XLV','GLDM']
ฟิลด์ระดับบนสุด       : ['data','type']       ← ไม่มี errors/failed/stale/timestamp
```
JS ที่ `dashboard/app.py:453-463` วนเฉพาะคีย์ที่มีใน `data` ไม่มี `else` และไม่ล้างค่า → ตัวที่หายคง `innerHTML` เดิม (ราคา สี เครื่องหมาย) ไว้ตลอด แถบราคาสดอยู่บนสุดของ **ทุกหน้า**
**ตัวจำกัด** ค่าที่ค้างเป็นค่าที่เคยจริง ไม่ใช่ค่ากุ และถ้าไม่เคยมาเลยจะยังเป็น `GLDM ?` ตาม HTML ตั้งต้น อาการหนักต้องเป็น "เคยมาแล้วหยุดมา" ซึ่งเกิดจริงเมื่อโดน rate limit
**แนวแก้** `{"type":"price_update","ts":<iso>,"data":{...},"unavailable":["GLDM"]}` แล้ว JS แสดง `GLDM ⚠️ ดึงไม่ได้` (เทาลง) + เวลาอัปเดตล่าสุด · **แตะ `dashboard/app.py` ⇒ ประสานกับเฟส C**

---

### B8 · `portfolio/dca.py` — DCA Simulator ตัดช่วงเวลาทิ้งเงียบ (**ยังไม่ผ่านการตรวจซ้ำ**)

`simulate_monthly_dca()` ทำ `monthly_prices = prices.resample("MS").first().dropna(how="any")` — `how="any"` ตัด**ทั้งเดือน**ถ้ามี ticker ตัวใดยังไม่มีราคา

```
ช่วงข้อมูลที่ดึงได้: 2016-08-08 → 2026-08-05  2512 แถว
  GLDM แถวแรก 2018-06-26 · QQQM แถวแรก 2020-10-13 · SCHD/VOO/XLV แถวแรก 2016-08-08
DCA Simulator จำลองจริง: 2020-10-01 → 2026-08-01 = 71 เดือน
Total Invested ที่หน้าจอโชว์: $71,000     (ถ้ามีข้อมูลครบ 10 ปีจะเป็น 121 เดือน = $121,000)
→ 50 เดือนที่ VOO/SCHD/XLV ซื้อได้จริงถูกลบทิ้งเพราะกองอื่นยังไม่เกิด
```
`render_dca_simulator_page()` แสดงแค่ metric 3 ช่อง + caption "จำลองการซื้อทุกเดือนด้วยงบเท่ากัน (ไม่รวมค่าธรรมเนียม)" ฝั่ง API `/api/dca/simulate` ก็ไม่มีฟิลด์บอกช่วง

**จุดเทียบที่ชี้ว่าเป็นความผิดพลาด** `portfolio/backtest.py::run_portfolio_backtest` ตัดช่วงแบบเดียวกัน **แต่เขียน docstring อธิบายไว้ และ raise พร้อมข้อความ "ETF ที่เพิ่งเกิดใหม่จะตัดช่วงเริ่มต้นของพอร์ตให้สั้นลง"** — `dca.py` ไม่ทำทั้งสองอย่าง

**แนวแก้** คืน `simulated_from` / `simulated_to` / `months_dropped` / `limited_by` แล้วให้หน้าจอ `st.warning("จำลองได้ตั้งแต่ 2020-10 เท่านั้น — QQQM เพิ่งเริ่มซื้อขาย 2020-10-13 ตัดไป 50 เดือน")` เหนือ metric ทั้งสามช่อง
**เทสต์** `portfolio/dca.py` coverage 12% (90/102 statement ไม่เคยถูกรัน) — mutation ถอด `_normalize_weights` รอด ⇒ เขียนเทสต์พร้อมกับ FIX_PLAN 2.5 ที่กำลังจะแก้ไฟล์นี้อยู่แล้ว

---

### B9 · `utils/fx.py` — แคชความล้มเหลว 1 ชม. + ผู้เรียกทิ้ง `is_live` (**ทำหลัง A5 และ C1**)

อยู่ในรายการ "ยังไม่ผ่านการตรวจซ้ำ" ของ FIX_PLAN — **ตอนนี้ยืนยันแล้ว 2 รอบ**

```
_cached = (fallback, False, now)   ← แคชผลของความล้มเหลวเหมือนผลสำเร็จ, TTL 3600
  เรียกครั้งที่ 1 (ดึงสดล้ม)      → FxRate(rate=33.5, is_live=False)
  เรียกครั้งที่ 2 (ดึงสดกลับมาแล้ว) → FxRate(rate=33.5, is_live=False) | _fetch_live ถูกเรียกทั้งหมด 1 ครั้ง
  เรียกครั้งที่ 3..5               → เหมือนเดิม | ยังคง 1 ครั้ง
  force_refresh=True              → FxRate(rate=32.1, is_live=True)   ← ค่าสดพร้อมใช้แล้วแต่โค้ดไม่ไปเอา
```
ขัดกับนโยบายที่ `utils/cache.py` เขียนไว้เอง ("exception ไม่ถูก cache — เรียกครั้งถัดไปได้ลองใหม่เสมอ")

**ชั้นที่สอง** `grep -rn "is_live" --include=*.py .` (ตัด tests) เจอแค่ `utils/fx.py` (นิยาม) + `dashboard/app.py:3360,3409` (หน้า Scorecard) ⇒ `tracker._get_usdthb_rate`, `networth_service.py:25`, `rebalance_service.py:27` ทิ้งทั้งหมด และ `dashboard/app.py:2928` โชว์ `m4.metric("FX Rate วันนี้", ...)` ดิบ ๆ

**ขนาดที่วัดได้จริง** อัตราจริงวันที่วัด `FxRate(rate=33.04, is_live=True)` เทียบ config fallback 33.5 = ส่วนต่าง **−1.39%**
**ปรับ high → medium** เพราะ (ก) ต้นทุน (`Invested (THB)`) คิดจาก `fx_rate_thb` รายแถวในสมุด ไม่ใช่ FX วันนี้ → คลาดเคลื่อนเข้าเฉพาะ `current_value_thb`/`pnl_thb` (ข) ค่าสำรองผูกกับ config ที่ผู้ใช้ตั้งใกล้ราคาจริง (ค) แคชคลายเองใน 1 ชม. · **หักล้างตัวเลข "โม้กำไร 2.5 เท่า"** ในรายงานเดิม — เป็นผลของการเลือกสมุดที่กำไรจริงเล็กมาก ข้อความที่ตรงคือ **ความคลาดเคลื่อน = มูลค่าพอร์ต USD × ส่วนต่าง FX** เท่านั้น

**พ่วง (low)** `_config_fallback()` ข้ามด่าน sanity band ของตัวเอง:
```
MIN/MAX band ของ utils.fx = 20.0 50.0
config 5.0   → FxRate(rate=5.0, is_live=False)
config 900.0 → FxRate(rate=900.0, is_live=False)
```
และค่านี้เข้าถึงได้จาก UI: `dashboard/app.py` `st.number_input("อัตราแลกเปลี่ยนสำรอง...", min_value=1.0, ...)` **ไม่มี `max_value`** และไม่ผูกกับ `MIN_RATE/MAX_RATE` · `rebalance_service._usable_fx_rate` ดักได้แค่ `<= 0` และคอมเมนต์ในนั้นเขียนว่า "ไม่ตั้งช่วงค่าใหม่ (ช่วงเป็นของ utils/fx)" — แต่ `utils/fx` ไม่ได้ตรวจช่วงให้ค่าสำรองเลย **ความรับผิดชอบตกหล่นระหว่างสองไฟล์**

**แนวแก้** แคชเฉพาะค่าที่ `is_live=True`; ค่าสำรองแคชสั้นมาก (60 วิ กัน hammer) หรือไม่แคชเลย · ยกเลิก `get_usdthb_rate()` หรือบังคับให้ผู้เรียกรับ `FxRate` ทั้งก้อน แล้วส่ง `fx_is_live=False` ขึ้นถึงหน้าจอ/API แบบเดียวกับ `missing_prices` · ให้ `_config_fallback` ตรวจ band เดียวกัน

---

### B10 · `portfolio/targets.py` — น้ำหนักเป้าหมายที่ผู้ใช้ตั้งเองถูกทิ้ง/บิดเงียบ

```
preset moderate  : {'VOO':35.0,'SCHD':25.0,'QQQM':20.0,'XLV':10.0,'GLDM':10.0}
{"GLDM":0}       : {'VOO':35.0,'SCHD':25.0,'QQQM':20.0,'XLV':10.0,'GLDM':10.0}   ← ค่าที่ตั้งถูกทิ้งเงียบ
{"GLDM":0.20}    : {'VOO':31.82,'SCHD':22.73,'QQQM':18.18,'XLV':9.09,'GLDM':18.18} ← ขอ 20 ได้ 18.18 และ VOO เลื่อนทั้งที่ไม่ได้แตะ
{"VOO":35}       : {'VOO':98.18,'SCHD':0.7,'QQQM':0.56,'XLV':0.28,'GLDM':0.28}    ← ไม่ตรวจหน่วย
{"VOO":-0.35}    : เหมือน preset ทุกตัว (ถูกทิ้งเงียบ)
ครบทุกตัว 0.4/0.2/0.2/0.1/0.1 : {'VOO':40.0,...}  ← ทำถูกเมื่อผู้ใช้ตั้งครบ
```

**สิ่งที่ผู้ตรวจซ้ำเพิ่มให้ และทำให้ข้อนี้ชัดขึ้น** — โค้ดขัดกับ docstring ของตัวเอง: docstring บอก "ticker ที่ไม่มีเป้าหมายกำหนดไว้จะได้ส่วนแบ่งจากน้ำหนักที่เหลือแบบเท่า ๆ กัน" แต่กิ่ง `unset` **ไม่มีวันทำงาน**กับ 5 ticker ดีฟอลต์ เพราะ preset ครอบครบทุกตัว → ทุกตัวได้ค่า preset แล้วถูก normalize ทั้งชุด นั่นคือสาเหตุตรง ๆ ที่ขอ GLDM 20% แล้วได้ 18.18%

**ยังเป็นบั๊กแฝง** — `config.json` ปัจจุบัน `target_weights: {}` และไม่มี UI ให้ตั้ง ต้องแก้ไฟล์ด้วยมือตามที่ `dashboard/app.py:723` แนะนำ (`เช่น {"VOO": 0.4, "GLDM": 0.05}`) ซึ่งคำแนะนำนั้นชี้ไปที่รูปแบบ "ตั้งบางตัว" อันเป็นเคสที่ถูกบิดพอดี
**แนวแก้** แยก "ไม่ได้ตั้ง" (key หายไป) ออกจาก "ตั้งเป็น 0" (= ตั้งใจไม่ถือ ต้องเคารพ) · ค่าลบ/ไม่ใช่ตัวเลข → `ValueError` ข้อความไทย · ผลรวมไม่อยู่ในช่วง (0,1] และไม่ใช่ 100 → raise แทน normalize เดา · ถ้าค่าที่ใช้จริงต่างจากที่ตั้ง ต้องคืนธงให้หน้า Settings แสดง · **ต้องแก้ docstring ให้ตกลงกับโค้ดก่อนว่า "ตั้งบางตัว" หมายถึงอะไร**
**เทสต์** mutation ถอด normalize และเปลี่ยนสูตร leftover **รอดทั้งคู่** — เทสต์ปัจจุบัน assert แค่ "รวมเป็น 1" กับ "ทุกตัว > 0" ต้องเพิ่ม assert **ค่าจริง**

---

### B11 · `analysis/risk.py` + `analysis/correlation.py` — `pct_change()` ไม่ระบุ `fill_method`

`price_df.sort_index().pct_change().dropna(how="all")` — ใน pandas 2.2.3 ค่าเริ่มต้นคือ `fill_method='pad'` = **forward-fill ราคาก่อนคำนวณผลตอบแทน** วันที่ ETF ตัวหนึ่งไม่มีแท่งจึงกลายเป็นผลตอบแทน **0.00% พอดี** — รูปแบบเดียวกับ ffill ที่จับได้ใน `etf_service` และเป็นสิ่งที่ CLAUDE.md ห้ามตรง ๆ

```
เฟรม 5 ETF 2,511 แท่ง เจาะรู GLDM หาย 50 แท่ง (2%):
  จำนวนวันที่ผลตอบแทน GLDM = 0.0 พอดี (แท่งที่ถูกกุ): 50   vs  ไม่ ffill: 0
  GLDM annual vol 14.3213% (โค้ดปัจจุบัน) vs 14.2851% (fill_method=None) = +0.0362 จุด
  corr GLDM-VOO 0.0228 vs 0.0249
  pandas พ่น FutureWarning: The default fill_method='pad' ... is deprecated
```
`grep -rn "pct_change()" --include=*.py .` → ไม่มีจุดไหนระบุ `fill_method` เลย: `risk.py:21`, `correlation.py:50,90`, `returns.py:26`, `financial_model.py:102,127`, `edge_lab.py:78,99`
**แนวแก้** ระบุ `fill_method=None` ทุกจุด (จะได้ NaN ที่ `dropna` จัดการต่อได้อย่างซื่อสัตย์) — ขนาดผลกระทบเล็ก (0.03 จุด) แต่เป็นการกุค่าบนเส้นทางราคาและจะกลายเป็น error ในอนาคตเมื่อ pandas ถอด default ออก

---

## เฟส C — ไฟล์ที่หลายข้อทับกัน (ทำทีละคน)

> ⚠️ เฟสนี้มีข้อ **high 3 ข้อ** (H7, H8, H9) แต่ต้องอยู่ท้ายเพราะทุกข้อแตะไฟล์เดียวกันกับงานเฟส 3 ของ FIX_PLAN เดิม

### C1 · `portfolio/tracker.py` (+ `backend/services/portfolio_service.py`) — 5 ข้อ

#### H9 — เงินลงทุนรวมนับทุกแถว แต่มูลค่า/กำไร/% นับเฉพาะแถวที่มีราคา

```python
total_invested = holdings["Invested (THB)"].sum()      # ทุกแถว
ok = holdings[holdings["Price OK"]]
invested_ok = float(ok["Invested (THB)"].sum())
total_pnl = current_value - invested_ok                # เฉพาะแถวที่มีราคา
total_return_pct = (total_pnl / invested_ok * 100.0) if invested_ok else 0.0
```

**หลักฐาน** (สมุด VOO 140,000 + SCHD 34,000 + QQQM 108,000 บาท, QQQM ดึงราคาไม่ได้)

```
tracker.get_total_summary():
  invested=282,000.00 · value=209,349.00 · pnl=+35,349.00 · ret=20.32% · missing=['QQQM']
  ตรวจ: value − invested = −72,651.00  แต่ pnl = +35,349.00   → ขัดกัน 108,000.00 (= Invested ของ QQQM พอดี)

portfolio_service (/api/portfolio) ฝั่ง USD:
  invested_usd=8,000.00 · current_value_usd=6,300.00 · pnl_usd=+1,300.00  (cv − inv = −1,700)

ฉากราคาครบ: 282,000 / 328,977 / +46,977 → ตรงกันเป๊ะทุกชั้น
  ⇒ ความไม่ตรงมาจากราคาที่หายอย่างเดียว
```

**สิ่งที่ผู้ตรวจซ้ำเพิ่มและทำให้หนักกว่าที่รายงาน** — `total_return_pct` **สูงขึ้น**เมื่อข้อมูลหาย (16.66% ฉากราคาครบ → 20.32% ฉากราคาหาย) และ dashboard เอาไปวางเป็น `delta` ของ P&L ⇒ **ราคาที่ดึงไม่ได้ทำให้ตัวเลข "ผลตอบแทน" ดูดีขึ้น**

**พ่วง (พบโดยผู้ตรวจซ้ำ R4a)** เมื่อดึงราคา **ไม่ได้ทั้งพอร์ต** จะกุเลข 0 บนเส้นทางเงิน ไม่ใช่แค่ฐานไม่ตรงกัน:
```
stub _get_latest_prices = {} →
  {'total_invested_thb': 210800.0, 'current_value_thb': 0.0, 'total_pnl_thb': 0.0,
   'total_return_pct': 0.0, 'missing_prices': ['GLDM','VOO']}
→ หน้า Portfolio และ PDF พิมพ์ "Profit / Loss 0.00 · Total Return 0.00%" ให้พอร์ตที่ระบบไม่รู้มูลค่าเลยสักบาท
```
ค่าที่ถูกต้องคือ `None`/NaN ไม่ใช่ 0.0 — **ไม่อยู่ใน FIX_PLAN** (บรรทัด 284-285 ของแผนเป็นคนละเรื่อง: การหารด้วย FX วันนี้ในโหมด USD)

**ความรุนแรงที่ขัดกัน** R6 ให้ high · R4a/R8 ให้ medium (มี `missing_prices` เตือนอยู่จริง จึงเป็นการปนฐานไม่ติดป้าย ไม่ใช่การซ่อน) — **ตัดสินให้ high** เพราะตัวเลข 3 ตัวที่บวกลบกันไม่ลงตัวปรากฏพร้อมกันบนจอเดียว บนหน้าแรกของ PDF และไหลเข้า prompt ที่ป้อน AI · คำเตือนที่มี (`st.error("...มูลค่าและกำไร/ขาดทุนด้านล่างคิดเฉพาะ ETF ที่มีราคาเท่านั้น")`) **ไม่ได้บอกว่า "เงินลงทุนรวม" อยู่คนละฐาน** ผู้ใช้จึงประกอบตัวเลขกลับเองไม่ได้

**FIX_PLAN มีข้อนี้ในหมวด "ยังไม่ผ่านการตรวจซ้ำ" (`tracker.py:490`) — ตอนนี้ยืนยันแล้ว 3 รอบ และพบว่าซ้ำอีกจุดที่ `portfolio_service.py` ฝั่ง USD ซึ่งแผนไม่ได้ระบุ**

**แนวแก้** แยกให้ชัดเป็นสองค่าและติดป้าย: `invested_thb_priced` (ฐานที่ใช้คิด P&L/Return) กับ `invested_thb_all` (ยอดที่จ่ายไปจริงทั้งหมด) — **ห้ามใช้ชื่อกลาง ๆ ตัวเดียวกับสองฐาน** · ราคาหายทั้งหมด → `pnl`/`return_pct` เป็น `None` ไม่ใช่ 0.0 · แก้ `portfolio_service` และ `pdf_export` ให้ตรงกัน

#### C1.2 — อัตราแลกเปลี่ยนที่บันทึกในสมุดชนะเสมอ ไม่มีชั้นไหนตรวจกับยอดเงินบาทที่จ่ายจริง (medium)

`_load_transactions()` ใช้ `fx_rate_thb` ที่บันทึกไว้ทันทีถ้าอยู่ในช่วง 20–50 และเรียก `_derive_fx_from_amount()` **เฉพาะตอน fillna** ⇒ ค่าที่ผิดแต่ "อยู่ในช่วง" ไม่เคยถูกเทียบกับ `amount_thb` เลย ทั้งที่ทุกแถวมีข้อมูลพอจะเช็คได้

```
ไม้เดียวกัน: VOO 10 หุ้น @400 USD วันที่ 2024-01-15 จ่ายจริง 140,210 บาท (อัตราวันนั้น 35.00) ราคาวันนี้ 500
  [fx=35.00 ถูก]        Invested=140,000.00  P&L=26,150.00  ret=18.68%  | Invested+Fee=140,210.00 = amount_thb ✓
  [fx=33.23 อัตราวันนี้] Invested=132,920.00  P&L=33,230.00  ret=25.00%  | Invested+Fee=133,130.00 ≠ 140,210 (ขัดกัน 7,080)
  ทั้งสองเคส skipped_rows=[] skipped_reason=''   ← ไม่มีคำเตือนที่ชั้นไหนเลย
```

**เครื่องมือตรวจสอบมีอยู่แล้วครบ** — ลองบันทึก `fx=5` (นอกช่วง) → ระบบ **คำนวณย้อนจาก `amount_thb` ได้ 35.00 กลับมาเป๊ะ** พร้อม log warning ⇒ ใช้เฉพาะตอนค่า "ใช้ไม่ได้" ค่าที่ "ผิดแต่อยู่ในช่วง" จึงรอด

**ตัวจุดชนวนที่ทำให้เกิดจริง** `dashboard/app.py` ฟอร์ม Add Transaction ตั้งค่าเริ่มต้นช่อง FX Rate เป็น **อัตราวันนี้** (`value=float(today_fx_rate)`) โดยไม่ผูกกับช่อง Date ⇒ การย้อนบันทึกไม้เก่า (วิธีเริ่มใช้ระบบตามปกติ) เก็บอัตราวันนี้ทับอัตราวันซื้อ

**ปรับ high → medium** เพราะโค้ดเองไม่ได้ผลิตค่าผิด ต้องมีผู้ใช้กรอก fx ผิดก่อน (ระบบบันทึกตามที่ถูกบอก) — สิ่งที่ผิดคือ "ไม่ตรวจข้อมูลที่ขัดกันเองทั้งที่มีข้อมูลพอ" ไม่ใช่ "กุตัวเลข"
**แนวแก้** เพิ่มด่านความสอดคล้อง: `abs(amount_thb - fee_thb - shares*price_usd*fx_rate_thb) / amount_thb > 1%` → รายงานผ่าน `skipped_rows`/`skipped_reason` ที่มีอยู่แล้ว (เตือน ไม่ต้องตัดทิ้ง) · ฟอร์มให้ FX ดีฟอลต์ตามวันที่ที่เลือก หรือแสดงอัตราที่คำนวณย้อนให้ยืนยัน

#### C1.3 — แถวที่ถูก "ซ่อม" อัตราไม่ถูกรายงานให้ผู้ใช้เห็น (low)

แถวที่อัตราเดิม "มีค่าแต่ใช้ไม่ได้" (0/ติดลบ/นอกช่วง) ถูกแทนด้วยค่า derive แล้วเดินเข้าเลขเงินโดยมีแค่ `logger.warning` · `grep 'skipped_rows' dashboard/app.py` มีแต่ช่องแสดง "แถวที่ถูกตัด" (`app.py:2514`) ไม่มีช่องสำหรับ "แถวที่ถูกซ่อม"
**แนวแก้** เพิ่มคีย์ `derived_fx_rows` (tx_id, อัตราที่บันทึก, อัตราที่ใช้จริง) คู่กับ `skipped_rows` แล้วให้ dashboard/PDF แสดงเหมือนกัน

#### C1.4 — สรุปปันผลรายงานแถวที่ถูกตัดของ "ทั้งสมุด" (low)

`get_dividend_summary()` เอา `skipped_rows` มาจาก `_load_transactions()` ทั้งก้อน ไม่ได้กรองเฉพาะ `tx_type=dividend`
```
ledger 2 แถว: b1 = ไม้ซื้อที่ fx/amount ว่าง, d1 = ปันผลปกติ 345 บาท
  count(ปันผล)=1  total_thb=345.0   (ถูกต้องครบถ้วน)
  skipped_rows ที่โผล่ใต้หัวข้อ 'ปันผลรับจริง': [('b1','VOO')]
  skipped_reason: 'ข้ามธุรกรรม 1 แถว... ตัวเลขสรุปไม่รวมแถวเหล่านี้'
```
ผู้ใช้เข้าใจว่าปันผลที่บันทึกไว้หายไป และคำเตือนเดียวกันซ้ำสองที่ในหน้าเดียว
**แนวแก้** กรอง `skipped_rows` เฉพาะ `tx_type == dividend` (ต้องเก็บ `tx_type` ลง dict ของ `_collect_skipped_rows`) และแสดงตารางรายละเอียดที่เดียว

#### C1.5 — ทิ้ง `is_live` ของ FX (แก้พร้อม B9)

---

### C2 · `dashboard/app.py` — 4 ข้อ (2 ข้อ high)

#### H7 — drift advisory สร้างเป้าหมายชุดที่สองจากเฉพาะกองที่ถืออยู่

`_render_drift_advisory()` เรียก `get_target_weights([str(t) for t in priced["Ticker"]])` (บรรทัด 3441) — ส่งเฉพาะ ticker ที่ **ถืออยู่แล้ว** → `portfolio/targets.py` normalize ใหม่ให้รวมเป็น 1.0 บนเซ็ตย่อยนั้น ⇒ กองที่ยังไม่เคยซื้อหายจากตัวหารและจากรายการ drift **"ขาดกองนั้นทั้งกอง" ถูกแปลงเป็น drift = 0**

**หลักฐาน** — ผู้ตรวจซ้ำเรียก `_render_drift_advisory()` **ตัวจริง** (stub `st` เก็บข้อความ, stub `get_portfolio_summary`)

```
พอร์ตถือแค่ VOO 165,000 + QQQM 94,285.71 บาท (ราคาครบทั้งคู่)
  เป้าหมายที่ระบบใช้จริงในหน้านี้ = {'QQQM': 36.36, 'VOO': 63.64}
  เป้าหมายจริงจาก config          = {'VOO':35.0,'SCHD':25.0,'QQQM':20.0,'XLV':10.0,'GLDM':10.0}

  ETF     actual%   drift ที่แสดง   drift จริง
  QQQM      36.36       +0.00      +16.36
  VOO       63.64       +0.00      +28.64
  SCHD       0.00           —      -25.00
  XLV        0.00           —      -10.00
  GLDM       0.00           —      -10.00

  st.caption ที่พิมพ์จริง: "พอร์ตจริงใกล้เป้าหมายทุกตัว (drift < 5%) — DCA ตามแผนได้เลย"
```
คำแนะนำตรงข้ามกับสถานะจริง **28.6 จุด** และเป็นบั๊กชนิดเดียวกับที่ `portfolio/targets.py` ถูกสร้างมาแก้ กลับมาเกิดซ้ำ
ด่านที่มีอยู่ไม่ช่วย: `_unpriced_tickers()` กันเฉพาะเคสราคาหาย ไม่เกี่ยวกับกองที่ยังไม่เคยซื้อ

**⚠️ แก้แผนเดิม** FIX_PLAN ข้อ 3.4 ระบุบั๊กเดียวกันนี้ไว้เฉพาะที่ `_render_rebalance_mode` (บรรทัด 3313 `get_target_weights(list(current_values))`) แต่ **ไม่ได้ระบุ `_render_drift_advisory`** (บรรทัด 3441) ซึ่งอยู่บนหน้า Scorecard ที่ผู้ใช้เปิดทุกเดือน — **แก้ตามแผนตรง ๆ จุดนี้จะหลุด**
`grep` ทั้ง repo: call site ที่ส่งเซ็ตย่อยเข้าไปมี **2 จุดนี้เท่านั้น** (`financial_model.py:458` ส่ง `usable` ซึ่งเป็นพฤติกรรมที่ documented ไว้แล้ว)
**แนวแก้** `get_target_weights(get_tickers())` เสมอ แล้ววน drift บน union ของ `get_tickers()` กับ ticker ที่ถืออยู่ — กองที่ยังไม่ถือให้ `actual = 0%` (drift = −target) ไม่ใช่หายจากตาราง

#### H8 — XIRR คิดจากกระแสเงินสด USD แต่ถูกลบด้วยเงินเฟ้อไทยเป็น real return

`_render_benchmark_section()` สร้าง flows เป็น `-shares * price_usd` (USD, **ไม่รวมค่าธรรมเนียมที่จ่ายจริง**), ปันผลเป็น `amount_usd`, flow สุดท้ายเป็น `actual_value_usd` → `xirr()` ได้ผลตอบแทนฐาน **ดอลลาร์** แล้วบรรทัด 2661–2667 พิมพ์

```python
xirr_text = f"ผลตอบแทนจริง ~{annual_rate*100:+.1f}%/ปี (money-weighted รวมปันผลที่บันทึก)"
real_rate = annual_rate * 100.0 - float(thai_inflation["inflation_pct"])
```

**หลักฐาน** — ผู้ตรวจซ้ำเรียก `_render_benchmark_section()` ตัวจริง (stub เฉพาะ `st`, ledger, ราคา, เงินเฟ้อ 1.2%) ด้วยอัตราแลกเปลี่ยนที่ **ดึงสดจาก yfinance `THB=X` ในคอนเทนเนอร์เดียวกัน**

```
สมุด 4 ไม้ VOO (10@400 15/1/24, 8@450 17/6/24, 8@520 15/1/25, 6@560 15/7/25) มูลค่าปลายทาง 32 หุ้น × 640
FX จริง: 34.947 / 36.680 / 34.690 / 32.460 · ล่าสุด 2026-08-06 = 33.050

  XIRR ที่จอแสดง (USD ล้วน ไม่มีค่าธรรมเนียม) = +17.580 %/ปี
  XIRR ของเงินบาทที่จ่าย/ได้คืนจริง          = +14.401 %/ปี
  XIRR USD ที่รวมค่าธรรมเนียม Dime จริง       = +17.487 %/ปี
  ส่วนต่าง (USD ที่โชว์ − บาทจริง) = +3.178 จุด/ปี
  ค่าธรรมเนียมที่หายไป = 0.093 จุด/ปี

st.info ที่พิมพ์จริง: 'ผลตอบแทนจริง ~+17.6%/ปี ... · หักเงินเฟ้อไทย ~1.2% → real return ≈ +16.4%/ปี'
real return ที่ถูกต้อง (ฐานบาท − เงินเฟ้อไทย) = +13.2%/ปี
```

caption ที่หน้าเดียวกัน (`app.py:3040-3043`) ชี้มาที่ตัวเลขนี้ตรง ๆ ว่า "เป้าขั้นต่ำที่พอร์ตต้องชนะต่อปีเพื่อรักษา**อำนาจซื้อเงินบาท** … real return เต็มรูปจะมากับ XIRR ในหัวข้อ benchmark" ⇒ **เจตนาของตัวเลขคือฐานบาทชัดเจน แต่ที่คำนวณคือฐานดอลลาร์**

ในสถานการณ์บาทแข็งที่เกิดขึ้นจริงอยู่ตอนนี้ (34.95 → 33.05) ตัวเลขสูงเกินจริง ~3.2 จุด/ปี และถ้าบาทแข็งแรงกว่านี้ **พลิกเครื่องหมายได้** (USD บวก แต่บาทติดลบ)

**⚠️ แก้แผนเดิม** FIX_PLAN 3.1 ครอบหัวข้อนี้ 3 บั๊ก (ก)(ข)(ค) แต่ **ไม่มีข้อไหนพูดถึงสกุลเงินของ XIRR** — เป็นบั๊กที่ 4 ที่ต้องเพิ่มเข้าแผน 3.1
**แนวแก้** สร้าง flows จากเงินบาทที่ไหลออก/เข้าจริง: ขาซื้อ `-amount_thb` (รวมค่าธรรมเนียมแล้ว), ปันผล `+amount_thb`, flow ปลายทาง `Current Value (THB)` ของแถวที่ `Price OK` — แล้วค่อยลบเงินเฟ้อ · ถ้าจะคงตัวเลขฐาน USD ไว้ด้วย ต้องติดป้าย "%/ปี ฐานดอลลาร์" และ **ห้าม**เอาไปลบ CPI ไทย

#### C2.2 — "โมเดลแนะนำถือเงินสดรอ" ที่ผลิตจากความล้มเหลว (medium, **ยังไม่ผ่านการตรวจซ้ำ**)

`dashboard/app.py:2098` พิมพ์ `"เดือนนี้ไม่มี ETF ที่คะแนนถึงเกณฑ์จัดสรร — โมเดลแนะนำถือเงินสดรอ"` และ PDF พิมพ์ `"No ETF met the allocation threshold this month (model suggests holding cash)."` เมื่อ `allocation` ว่าง

แต่ `calculate_allocation()` คืน `{}` ใน **3 กรณีที่แยกจากกันไม่ได้เลย**: (ก) `budget_thb <= 0` (ข) ไม่มี ticker ไหน `data_ok=True` = **ดึงราคาไม่ได้ทั้งหมด** (ค) `total_units == 0` คืองบ 1–99 บาท

```
fm._yf_close_series = lambda t: pd.Series(dtype=float)   # yfinance ล่ม
  data_ok ของทุกตัว: {'VOO':False,'SCHD':False,'QQQM':False,'XLV':False,'GLDM':False}
  calculate_allocation(งบ 5,000 บาท) = {}
  งบ 0 บาท                            = {}      ← สองสถานการณ์ให้ผลเหมือนกันทุกไบต์

ย่อหน้าที่ลง PDF จริง:
  * WARNING: current price unavailable for GLDM — ...
  * No ETF met the allocation threshold this month (model suggests holding cash).
  * NO DATA (excluded from scoring): VOO, SCHD, QQQM, XLV, GLDM     ← ขัดแย้งกันเองในหน้าเดียว
```

ซ้ำร้าย ข้อความนี้อ้าง **"เกณฑ์คะแนน" ที่ระบบไม่มีแล้ว** — นโยบาย DCA ปัจจุบัน (CLAUDE.md + docstring ของ `calculate_allocation`) คือ "ซื้อทุก ETF ที่มีข้อมูลทุกเดือน คะแนนแค่เอียงน้ำหนัก 0.6–1.4 เท่า ไม่มีวันเป็น 0" **ไม่มีเกณฑ์ตัดตัวไหนออกอยู่แล้ว**

โค้ดพี่น้องเขียนถูกอยู่แล้ว: `analysis/ai_advisor._allocation_summary_lines()` ใช้ `"• ไม่มี ETF ที่มีข้อมูลพร้อมจัดสรร (ดึงข้อมูลไม่ได้)"` — **ระบบเดียวกันพูดคนละเรื่องจากข้อมูลชุดเดียวกัน**
**แนวแก้** ให้ `calculate_allocation` แยกสาเหตุ (`reason: "no_data"|"no_budget"|"below_unit"|"ok"`) แล้วใช้ถ้อยคำชุดเดียวกับ `_allocation_summary_lines()` และ **ลบคำว่า "เกณฑ์คะแนน" ออก** · แก้พร้อม M-PDF-2 (เฟส A4) และ D-FM-1

#### C2.3 — รายงานเหตุผลของไม้ที่เทียบ VOO ไม่ได้ผิดชนิด (low)

`shadow_benchmark()` ที่เขียนใหม่แยก `skipped_bad_row` (แถวในสมุดเสียเอง) ออกจาก `skipped_no_price` แต่ `_render_benchmark_section` ยังอ่าน `shadow['skipped']` (ผลรวม) แล้วพิมพ์เหตุผลตายตัว
```
{'rounds':1,'skipped':1,'skipped_bad_row':1,'skipped_no_price':0}
ข้อความที่แสดง: "1 ไม้เทียบไม่ได้ (ไม่มีราคา VOO ณ วันซื้อ) — ตัดออกจากขา VOO เงา"   ← สาเหตุผิด
```
และ `benchmark_asof` / `benchmark_prices_dropped` ที่ docstring บอกว่า "ผู้เรียกต้องเตือนก่อนเอาไปเทียบ" **ไม่มีโค้ดโปรดักชันอ่านเลย**

#### C2.4 — WebSocket unavailable list (แก้พร้อม B7)

---

## เฟส D — medium/low ที่เหลือ (ทำขนานได้)

### D1 · `alerts/price_alert.py` — ต่อจาก A1 (สายเดียวกัน ห้ามแยกคน)

**D1.1 (medium)** `check_alerts()` นับ alert ที่ดึงราคาไม่ได้ว่า "ตรวจแล้ว"

`get_price_snapshots()` มี `except Exception: return {}` และ `continue` ทีละ ticker → ลูปทำ `if current_price is None: continue` แต่คืน `"checked": len(pending)` ซึ่งนับทุก alert ที่รออยู่

```
R1 ดึงราคาไม่ได้เลย: alert VOO/SCHD (above 1.0 = ต้อง trigger แน่) + GLDM
   → {'success': True, 'checked': 3, 'triggered': []}   คีย์ที่คืน: ไม่มี error/skipped
R2 alert AAPL/NVDA (นอก DAILY_CHECK_TICKERS) → checked=2 triggered=0
   สรุป Discord ไม่เอ่ยถึง AAPL/NVDA เลย (mentions AAPL? False | NVDA? False)
R3 คุมตัวแปร: ราคามาครบ → trigger 2 รายการทันที
```
ปลายทาง: `dashboard/app.py:944` แสดง `st.info("ยังไม่มี Alert ที่ถึงเงื่อนไข")` · `main.py` พิมพ์ "ตรวจ alert 2 รายการ, trigger 0 รายการ"
**ตัวลดทอนที่ผู้ตรวจซ้ำชี้** (ก) สำหรับ 5 ticker ที่แดชบอร์ดให้ตั้ง alert ได้จริง สรุป Discord พิมพ์ `⚠️ ดึงราคาไม่ได้` ให้เห็น (ข) alert ที่ยังไม่ trigger ยังค้างเป็น pending และรัน 2 รอบ/วัน → เหตุขัดข้องชั่วคราวทำให้ "ช้า" ไม่ใช่ "หายถาวร"
**แนวแก้** เก็บ `unchecked: [{'ticker','reason'}]` · `checked` นับเฉพาะที่มีราคาจริง · สรุป Discord ต้องครอบคลุม ticker ของ alert ที่รออยู่ ไม่ใช่แค่ `DAILY_CHECK_TICKERS`

**D1.2 (low)** `get_price_snapshots` เมื่อมี close แถวเดียวตั้ง `previous_close = latest_price` → `_build_daily_status_message` พิมพ์ `VOO $500.00 🟡 (+0.00%)` = "ราคาไม่เปลี่ยน" จากข้อมูลที่ไม่มีจริง (รูปแบบเดียวกับที่คอมเมนต์ AUDIT.md C1 เหนือฟังก์ชันนั้นอ้างว่าแก้ไปแล้ว — แก้สูงไปหนึ่งชั้น) · `period="5d"` ทำให้เกิดยาก จึงเป็น low

**D1.3 (medium)** `jobs/daily_check._alert_status_line()` — `_load_alerts()` เรียก `_ensure_storage()` ที่ **สร้างไฟล์ว่างให้เอง** ⇒ ใน GitHub Actions (ที่ไฟล์ถูก gitignore) ข้อความบอกว่า "รอ trigger 0 รายการ" = "คุณไม่มี alert ค้างอยู่" คนละความหมายกับ "ระบบนี้มองไม่เห็น alert ของคุณ" · docstring ของฟังก์ชันเองเขียนว่า "เดิม hardcode '0 items' ทำให้เข้าใจผิด (AUDIT.md C6)" แต่ในสภาพแวดล้อมที่มันรันจริง **ผลลัพธ์เหมือนเดิมทุกตัวอักษร**

---

### D2 · `backend/routers/transactions.py` (slip OCR) — 3 ข้อ low

`grep -rn "upload-slip|upload_slip" --include=*.py .` → เจอที่เดียวคือคอมเมนต์ใน `backend/security.py` · dashboard/ และ tests/ ไม่มีเลย ⇒ **ยังไม่มีผู้เรียก ผลกระทบวันนี้ = 0** แต่สัญญาที่ผิดจะกลายเป็นบั๊กทันทีที่มีคนต่อ UI

```
D2.1  amount ติดลบ + date มั่ว + category นอกรายการ → HTTP 200 {"success":true,"amount":-5000.0,"date":"not-a-date",...}
      is_slip=true แต่ทุกฟิลด์ null                  → HTTP 200 {"success":true,"amount":null,...}
      amount มหาศาล                                  → HTTP 200 {"success":true,"amount":999999999999.0,...}
D2.2  amount เป็นสตริงมีคอมมา "1,234.56" → HTTP 500 Internal Server Error (ภาษาอังกฤษ)
      amount สตริงตัวเลขล้วน "1234.56"   → HTTP 200 (pydantic v2 แปลงได้)
D2.3  อัปโหลด 200 MB (MAX_FILE_SIZE = 5 MB) → HTTP 422 ถูกต้อง แต่ peak heap ฝั่ง handler = 200.4 MB
```

**แก้ถ้อยคำจากรายงานเดิม 2 จุด**
- "ผิดกฎ AI อธิบาย โค้ดคำนวณ" เป็นการตีความเกินตัวบท — กฎนั้นพูดถึงคะแนน/DCA/ระดับ price alert ที่ Python คำนวณเองได้ ส่วนการอ่านเลขจากรูปสลิปไม่มีทางให้ Python ทำแทน และ CLAUDE.md ยกเว้นให้ slip OCR เรียก client เองอย่างชัดเจน
- "จอง 600 MB → OOM-kill บน instance 512 MB" **ผิด** — ตัวเลข 600 MB เป็น artifact ของการวัดผ่าน TestClient (โปรเซสเดียวกัน จึงนับ body ฝั่งไคลเอนต์ด้วย) วัดใหม่เฉพาะฝั่ง handler ได้ `_rolled=False → False` (starlette spool ลงดิสก์เมื่อ >1 MB) และ peak = **200.4 MB = 1× ของขนาดอัปโหลด ไม่ใช่ 3×** ⇒ รูปจากกล้องมือถือ (3–12 MB) ทำให้เกิดไม่ได้เลย

**แนวแก้ (แก้ที่เดียวกันทั้ง 3)** normalize `amount` ก่อนสร้าง response (`float(str(raw).replace(",","").strip())` ครอบด้วย try) · `amount` ต้อง finite และ `> 0` ไม่งั้น `success=False, error="อ่านยอดเงินจากสลิปไม่ได้"` (ภาษาไทย) · `date` ต้อง parse ผ่าน · `category` ต้องอยู่ในชุดที่ system prompt ประกาศ · อ่านไฟล์ทีละ chunk แล้วตัดทันทีที่เกินเพดาน + เช็ค `file.size` ที่ starlette ให้มาก่อนเป็นด่านแรก

---

### D3 · ข้ออื่นระดับ low (ตารางรวม)

| # | ประเด็น | ไฟล์ | หลักฐานย่อ |
|---|---|---|---|
| D3.1 | `GET /api/analysis/dcf/{ticker}` ตอบ 500 เมื่อ ticker ไม่มีจริง | `routers/analysis.py` | `dcf_valuation` เรียก `yf.Ticker(t).info` ก่อนแตะราคา → `requests.HTTPError 404` ไม่ถูกดัก · `ZZZZNOTAREALTICKER` → 500 · แต่ `GLDM` → 422 ข้อความไทยถูกต้อง (ทำถูกแล้ว) |
| D3.2 | endpoint ที่คืนภาษาไทยไม่ตั้ง `charset=utf-8` | `etf_analysis.py`, `forecast.py`, `screener.py`, `networth.py` | `/api/etf/{symbol}` 200 THAI=True CHARSET=`application/json` · `/api/goals` และ `/api/portfolio` ตั้งถูก · ผลจริงต่ำ (FastAPI ใช้ `ensure_ascii=False`) แต่ผิดสัญญาที่ CLAUDE.md ประกาศเอง |
| D3.3 | CORS บล็อก PUT ทั้งที่ openapi ประกาศ `PUT /api/goals/{id}/contribute` | `backend/main.py:66` | `preflight PUT → 400 allow-methods='GET, POST, DELETE'` · จำกัด เพราะ dashboard เรียกโมดูลตรง และ `/docs` เป็น same-origin |
| D3.4 | `scripts/fix_goals.py` ลบข้อมูลจากไฟล์ฐานผิดตัว | `scripts/fix_goals.py` | `sqlite3.connect("vaultis.db")` เป็น path สัมพัทธ์ · รันจากโฟลเดอร์ที่ไม่มีไฟล์ → สร้างฐานเปล่า 0 ไบต์แล้วตายด้วย `OperationalError` · ไม่มี dry-run/ยืนยัน/สำรอง ก่อน `DELETE FROM investment_goals WHERE target_date = 'string'` (ค่าคงที่ `'string'` เป็นเศษ debug) |
| D3.5 | `scripts/check_db.py` เปิดฐานผิดตัว รายงาน "ไม่มีตารางเลย" | `scripts/check_db.py` | เหมือน D3.4 · รันแล้ว output `=== Tables ===` ว่างเปล่า และทิ้ง `vaultis.db` 0 ไบต์ไว้ ขณะที่ `VAULTIS_DB_PATH=/data/vaultis.db` มี 4 ตารางจริง |
| D3.6 | Prophet พยากรณ์วันที่ตลาดปิด | `analysis/forecaster.py` | `make_future_dataframe(periods=days)` ใช้ `freq='D'` · **แถวเสาร์/อาทิตย์ = 8 จาก 30** (ข้อมูลจริง 9/30) · VOO จริง 2 ปี: `days=29` → Friday trend=sideways(−0.29) · `days=30` (ดีฟอลต์) → Saturday trend=**down**(−1.17) |
| D3.7 | `days` ของ `/api/forecast` ไม่มี validation | `routers/forecast.py` | `days=0` → `IndexError: list index out of range` · `days=-5` → `OutOfBoundsDatetime` ⇒ HTTP 500 พร้อม traceback แทน 422 |
| D3.8 | `ta.rsi` คืนค่าเร็วไป 1 แท่งเป็น 0/100 ปลอม | `analysis/ta_compat.py` | ราคา 14 จุด: `ta` (ที่ใช้จริง) = **100.0** → zone=overbought · `_rsi_fallback` (ไฟล์เดียวกัน) = NaN · เทสต์ `assert rsi.iloc[:13].isna().all()` หยุดก่อนแท่งที่มีปัญหาพอดี 1 index |
| D3.9 | เศษงบ <100 บาทหายไปโดยไม่รายงาน | `portfolio/cashflow_rebalance.py` | งบ 9,999 → แจกจริง 9,900 (หาย 99) · งบ 99 → plan ว่าง `{}` เงียบ ๆ · เส้นทางนี้ข้าม `_render_verdict_cards` ที่เป็นที่เดียวที่มีบรรทัด "เศษจากการปัดหลักร้อย X บาท" |
| D3.10 | งบ DCA 1–99 บาทคืนแผนว่าง แล้วรายงานสาเหตุผิด | `analysis/financial_model.py` | `budget=50` → `{}` แล้ว `_allocation_summary_lines` พิมพ์ "ไม่มี ETF ที่มีข้อมูลพร้อมจัดสรร (ดึงข้อมูลไม่ได้)" ทั้งที่ข้อมูลครบ · เข้าถึงได้ทาง `Query(5000, ge=1)` เท่านั้น (dashboard บังคับ ≥500) |
| D3.11 | `ab_backtest` แขน benchmark VOO ซื้อเดือนแรกคนละวัน | `portfolio/ab_backtest.py` | window proxy: multi ซื้อ 2011-10-20 @85.85 vs voo_only 2011-10-03 @77.48 (ถูกกว่า 10.80%) · window real: ต่าง 0 เดือน |
| D3.12 | `portfolio/rebalance.py` เป็นโค้ดตายที่ประกาศ `threshold=0.05` ชุดที่สาม | `portfolio/rebalance.py` | coverage 0% ทั้งไฟล์ · `grep` ผู้เรียก = เจอแต่บรรทัดนิยามในไฟล์ตัวเอง |
| D3.13 | คอมเมนต์ `ai_used=False # ไม่มีค่าใช้จ่าย LLM` ไม่ตรงกับสาขา `except RuntimeError` ที่จ่ายเงินไปแล้ว | `analysis/ai_advisor.py:396` | ดูหัวข้อ "ข้อที่ถูกหักล้าง" — เหลือแค่คอมเมนต์ผิด ไม่ใช่บั๊กพฤติกรรม |

**D3.11 — ขนาดของ artifact เทียบกับสัญญาณ** (ผู้ตรวจซ้ำคำนวณต่อ)
```
[proxy] plain=6,026,106 | voo_only(ที่ส่งมอบ)=6,022,303 | voo_only(วันซื้อเดียวกัน)=6,013,540
  artifact จากวันซื้อคนละวัน = 8,763 บาท   vs   ระยะห่าง plain–VOO ที่รายงาน = 3,803 บาท  (ใหญ่กว่า 2.3 เท่า)
[real] artifact = 0 บาท
แก้แล้ว plain ยัง**ชนะ** VOO อยู่ดี (12,566 แทน 3,803) → ข้อสรุปไม่พลิก
ทั้งสองค่าคือ 0.06%/0.21% ของพอร์ต 6 ล้าน = noise ทั้งคู่
ด่านตัดสิน tilt vs plain ไม่ถูกกระทบ (สองแขนใช้กรอบเดียวกัน)
```
⇒ **ลดจาก medium เป็น low** — แก้เพื่อความถูกต้องของ harness ไม่ใช่เพราะข้อสรุปผิด

---

### D4 · เอกสารและ dependency (พบโดย completeness critic — ยังไม่มีใครตรวจซ้ำ)

| # | ประเด็น | หลักฐาน |
|---|---|---|
| D4.1 (medium) | คำอ้าง "Dependencies are pinned" ไม่จริง | pin 28 จาก **134** แพ็กเกจที่ติดตั้งจริง · ตัวที่ผลิตตัวเลขลอยหมด: `scipy==1.17.1`, `numba==0.66.0`, `llvmlite==0.48.0`, `urllib3==2.7.0`, `starlette==0.41.3` · ความไม่เข้ากันเกิดแล้ว: `RequestsDependencyWarning: urllib3 (2.7.0) or chardet (7.4.3)/charset_normalizer (3.4.9) doesn't match a supported version!` พ่นทุกครั้งที่ import อะไรที่ลาก requests |
| D4.2 (medium) | `render.yaml` หยุดอยู่ที่สภาพก่อน commit `8419d98` และ `0c0204c` | คอมเมนต์เขียน "LLM ตัวหลักคือ Claude Haiku 4.5" ขณะที่ `analysis/llm.py` ตั้ง `ANTHROPIC_MODEL = "claude-sonnet-5"` · ยังประกาศ `GROQ_API_KEY`/`GOOGLE_API_KEY` ที่ถอดไปแล้ว · **ไม่มี** `VAULTIS_API_KEY`, `DATABASE_URL`, `VAULTIS_DB_PATH`, `TELEGRAM_*`, `NEWSAPI_KEY`, `VAULTIS_ALLOWED_ORIGINS` และไม่มีบล็อก `disk:` (AUDIT.md:102 บันทึกเองว่า Render ฟรี = ephemeral disk) |
| D4.3 (low) | README.md บอกว่าโปรเจกต์ใช้ Groq + Google GenAI — repo เป็น public | บรรทัด 26/153/285 อ้าง Groq · บรรทัด 250 ลิสต์ `GOOGLE_API_KEY`, `GROQ_API_KEY` และ **ไม่มี `ANTHROPIC_API_KEY`** ทั้งที่เป็นคีย์เดียวที่ระบบใช้ · `grep -rn groq --include=*.py .` ไม่พบโค้ดเรียกเลย |
| D4.4 (low) | CLAUDE.md ระบุโมเดล slip OCR ผิด และเส้นทางนั้นไม่ log ค่าใช้จ่าย | CLAUDE.md บอก "slip OCR (Claude Sonnet 5)" แต่โค้ดฮาร์ดโค้ด `model="claude-haiku-4-5"` · ไม่ผ่าน `_log_cost()` เลย ขณะที่ CLAUDE.md โฆษณาว่า "ทุกการเรียกจะ log จำนวนโทเคนและค่าใช้จ่าย" |

---

## 3. ข้อที่ถูกหักล้าง — ห้ามกลับมาแก้อีก

### 3.1 step "Run Sentiment Analysis" ใน CI เป็น no-op — **ไม่ใช่บั๊ก เป็นนโยบาย**

```
tripwire แทน chat_text ทั้ง llm และ sentiment_analyzer, VAULTIS_LLM_AUTO=
  auto_enabled() = False
  [sentiment job] ข้ามการวิเคราะห์ sentiment — LLM ปิดอยู่เพื่อคุมค่าใช้จ่าย (ตั้ง VAULTIS_LLM_AUTO=1 ถ้าต้องการเปิด)
  exit=0   (tripwire ไม่ถูกยิง)
```
CLAUDE.md บรรทัด 113 ระบุชัดว่า sentiment "ต้องมีครบทั้ง `VAULTIS_LLM_AUTO=1` และ `DATABASE_URL` ที่ต่อได้ ไม่งั้น job ข้ามตัวเองเงียบ ๆ ตามนโยบายคุมค่าใช้จ่าย" · job พิมพ์เหตุผลออกมาตรง ๆ ไม่ได้อ้างว่า "ไม่มีข่าว" และ `exit 0` ถูกต้องเพราะ "ข้าม" ไม่ใช่ "ผิดพลาด"
**เหลือแค่สุขอนามัยระดับ low**: ส่ง `ANTHROPIC_API_KEY`/`NEWSAPI_KEY`/`REDDIT_*` เข้า step ที่ไม่มีวันใช้ และ workflow ไม่มีช่องให้เปิดสวิตช์

### 3.2 `simulate_drip` ใช้ Adj Close — **หักล้างเต็ม ตัวเลขที่ผู้พบเทียบด้วยผิดโมเดล**

`Adj Close` (auto_adjust=False) ปรับย้อนหลังด้วยปันผล+split ⇒ `amount / P_adj(t)` = **จำนวนหุ้นที่จะถือวันนี้หลังนำปันผลไปซื้อซ้ำทุกงวด** ซึ่งคือนิยามของ DRIP พอดี

```
SCHD ปันผล 25 USD วันที่ 2025-03-15 (ดึง raw Close + yf.Ticker('SCHD').dividends จริง)
  โค้ดคืน (Adj):                          extra_shares=0.955455  drip_value=32.1415  advantage=+7.1415
  DRIP เต็มรูปจากราคาปิดจริง (ซื้อ @27.64 แล้วเอาปันผลของหุ้นชุดใหม่ซื้อซ้ำทุกงวด):
                                          หุ้นวันนี้=0.955317  มูลค่า=32.1130  → ต่างจากโค้ด −0.014%
  ตัวเลข "ที่ถูก" ที่ผู้พบเสนอ (0.904486 / 30.4948) = โมเดล "ซื้อครั้งเดียวแล้ว **ทิ้งปันผลที่หุ้นชุดใหม่จ่ายมา**"
  → "หุ้นเกินจริง 5.64%" และ "advantage เกินจริง 30%" ไม่จริง: ส่วนต่างจริงคือ 0.014% / 0.4%
  "ราคาคนละฐานกับ tracker": Adj 33.6400 vs raw Close 33.6150 = 0.07% (เศษการปรับ)
```

### 3.3 `_derive_fx_from_amount` หัก fee → ต้นทุนต่ำกว่าจริง — **หักล้างข้อสรุป เหลือ low**

ตัวเลขซ้ำได้ (`derived fx 34.44825` แทน `34.5`) แต่ข้อสรุปตั้งอยู่บนธรรมเนียม `amount_thb ไม่รวม fee` ซึ่งพิสูจน์ไม่ได้ว่าเป็นของโปรเจกต์:
1. fixture ที่ผู้พบยกมา (`test_dividends_costs.py:62,:25`, `test_phase2.py:122`) **กรอก `fx_rate_thb` ครบทุกแถว** → เส้นทาง derive ไม่เคยถูกเรียกในเทสต์เหล่านั้นเลย
2. ธรรมเนียมตรงข้ามถูกประกาศและตรึงไว้ชัดเจนใน docstring ของ `tracker.py:17-19,164-179` + `tests/test_tracker_fx_missing.py::TestDeriveSubtractsRecordedFee`
3. `amount_thb` ไม่ได้เข้าเลขเงินที่อื่นเลย — `get_portfolio_summary` คิด invested จาก `shares*price*fx` ล้วน ⇒ ความคลาด 0.15% เฉพาะแถวที่ fx ว่าง/ผิดรูป
4. ข้ออ้าง "`amount_usd` ของแถวปันผลเพี้ยนตาม" **เกิดไม่ได้** — `_derive_fx_from_amount` ใช้ตัวหาร `shares*price_usd` ซึ่งแถวปันผล = 0 → NaN เสมอ (รันแล้ว: `count=0 skipped=['d1','d2']`)
5. ledger จริงมีแต่ header → ไม่มีข้อมูลจริงได้รับผลกระทบ

**เหลือเป็น low**: แถวที่ถูก "ซ่อม" ไม่ถูกรายงาน (= C1.3)

### 3.4 max_tokens จ่ายเงิน 2 ครั้ง + ไม่ถูกแคช + `ai_used=False` โกหก — **หักล้าง 3 ท่อน เหลือคอมเมนต์ผิด**

- **"2 ครั้งเป็นผลจากข้อ 1.1"** ผิด — รันด้วย stub ที่คืน**เนื้อหาบางส่วน** + `stop_reason='max_tokens'` (เคสปกติที่สุด) ได้ผลลัพธ์สำเร็จแต่ **calls: 2 เท่ากัน** ⇒ มาจากลูป retry-at-2× ที่มีมาก่อน diff นี้ และ CLAUDE.md ระบุไว้เองว่า "retries at 2× budget — the truncated first attempt is still billed" ข้อ 1.1 แค่เปลี่ยนสิ่งที่เกิด **หลัง** ยิงครบ 2 ครั้ง
- **"แคชที่เคยกันการจ่ายซ้ำถูกถอดออก"** กลับด้าน — early-return ที่ `routers/ai.py:75-79` คือการ**บังคับใช้ invariant ของโปรเจกต์เอง** ("never caches failures") การเรียกสิ่งนี้ว่าบั๊กคือขอให้ละเมิดกฎ C1
- **"deterministic → กดใหม่ไม่มีทางสำเร็จ"** พิสูจน์ไม่ได้ — `chat_text` ไม่ส่ง temperature ต่อไปที่ API เลย ผู้พบไม่ได้รันอะไรที่พิสูจน์ข้อนี้ (stub คืนค่าเดิมเพราะเขาเขียนให้คืนเอง = สมมติฐานที่ฝังไว้ในหลักฐาน)

**เหลือ low**: คอมเมนต์ `# False = ไม่มีค่าใช้จ่าย LLM ในรอบนี้` ที่ `ai_advisor.py:396` ไม่ตรงกับสาขา `except RuntimeError` — เป็นคอมเมนต์ในโค้ดผิด ไม่ใช่ข้อความผู้ใช้ (`advice_text` ขึ้นต้นด้วย "⚠️ เรียก AI ไม่สำเร็จ: ..." ชัดเจน)

---

## 4. ความสัมพันธ์กับ FIX_PLAN.md เดิม

### 4.1 ข้อในแผนเดิมที่รอบนี้ **ยืนยันแล้ว** (ย้ายออกจากหมวด "ยังไม่ผ่านการตรวจซ้ำ" ได้)

| รายการในแผน (บรรทัด 409) | สถานะใหม่ | อยู่ที่ข้อ |
|---|---|---|
| `tracker.py:490` ตัวหารไม่ตรงกันเมื่อราคาบางตัวหาย | ✅ ยืนยัน 3 รอบ + **พบซ้ำที่ `portfolio_service.py` ฝั่ง USD ซึ่งแผนไม่ได้ระบุ** | H9 / C1 |
| `utils/fx.py:76` แคช fallback 1 ชม. + ทิ้ง `is_live` | ✅ ยืนยัน 2 รอบ (ลดเป็น medium) | B9 |
| `sentiment_analyzer.py:84,144` batch ที่ล้มหายเงียบ + ทุกแหล่งพังอ่านเป็น no news | ✅ ยืนยัน 2 รอบ | B2 |
| `targets.py:43` weight ที่ตั้ง 0 ไม่มีผล | ✅ ยืนยัน + **พบเพิ่ม: ไม่ตรวจหน่วย (พิมพ์ 35 แทน 0.35 → VOO 98%)** | B10 |
| `screener/engine.py:74` โบนัส +1.0 เมื่อ RSI>65 | ✅ ยืนยัน (ลดเป็น low) | B6.3 |
| `ta_compat.py:59` RSI 2 ชุดต่างกัน + `test_money_math.py:240` warm-up สั้นไป 1 index | ✅ ยืนยันกลไก แต่ **ขอบเขตในแผนผิด** (ดู 4.3) | D3.8 |
| `cashflow_rebalance.py:63` เศษ <100 บาทหาย | ✅ ยืนยัน | D3.9 |
| `financial_model.py:444` งบ 1-99 บาทคืนแผนว่างเงียบ | ✅ ยืนยัน | D3.10 |
| `backtest_engine.py:120,154` กลืน exception + กลยุทธ์ที่ไม่เคยเทรดถูกรายงานว่าชนะ | ✅ ยืนยัน (ลดเป็น medium) | B3 |
| `ab_backtest.py:307` แขน benchmark ซื้อคนละวัน | ✅ ยืนยัน (ลดเป็น low — artifact ไม่พลิกข้อสรุป) | D3.11 |
| `price_alert.py:280` นับ alert ที่ดึงราคาไม่ได้รวมใน checked | ✅ ยืนยัน | D1.1 |
| `routers/analysis.py:45` DCF ticker ไม่มีจริง → 500 | ✅ ยืนยัน (low) | D3.1 |
| `benchmark.py:41` guard `or 0.0` ดัก NaN ไม่ได้ | — ไม่มีใครตรวจรอบนี้ | ยังค้าง |
| `goal_service.py:173` arithmetic mean ใช้เป็นอัตราทบต้น | ⚠️ **ยืนยันกลไก แต่ตัวเลขในแผนผิด 7–10 เท่า** (ดู 4.3) | 4.3 |

### 4.2 ข้อที่แผนเดิม **ครอบไม่ถึง** และต้องเพิ่มเข้าแผน

| แผนเดิม | สิ่งที่ครอบไม่ถึง |
|---|---|
| **1.1** (llm.py คำตอบว่าง) | ผู้เรียกรับมือ `RuntimeError` ไม่ได้ 2 จุด: `report_service.generate_narrative` (A2/M-R1) และคำอธิบายในแผนที่เขียนว่า "job พัง" ซึ่ง**ไม่ตรงความจริง** |
| **1.3** (rebalance ราคาหาย) | ticker ที่ถืออยู่แต่ไม่มีในเป้าหมายถูกตัดเงียบ + แผนซื้อเกินงบ (B4.2) และการไม่ใช้ `get_target_weights()` (B4.1) |
| **1.4** (cache ความล้มเหลว) | sentinel แบบ "dict ที่ทุกค่าเป็น None" ผ่านด่านไปได้ (B5) |
| **2.1** (screener engine) | ฝั่ง**อ่าน** ประวัติ (`history_service.get_history` → รายงานรายเดือน, A2/M-R3) · blocking I/O บน event loop + misfire (B6.1) · ดาวน์โหลดซ้ำ 4 เท่า ซึ่งจะแย่ลงเมื่อเปลี่ยน `period` เป็น `2y` ตามแผน (B6.2) |
| **2.3** (daily_check `_change_pct_yfinance`) | `_row_for_ticker` เชื่อ snapshot ก่อนเสมอ ⇒ **แก้ 2.3 อย่างเดียวข้อความ Discord ยังผิด** ต้องแก้ `etf_service` ffill ด้วย (H10) · และการซ้ำซ้อน 2 ใบต่อวัน (H12) |
| **2.5** (ticker ที่ไม่รู้จักถูกตัดเงียบ) | `dca.py` ตัด **เดือน** ทิ้งเงียบด้วย `dropna(how="any")` (B8) — คนละบั๊กในไฟล์เดียวกัน |
| **3.1** (ชนะ VOO ไหม) | บั๊กที่ 4: XIRR ฐาน USD ถูกลบด้วยเงินเฟ้อไทย (H8) และค่าธรรมเนียมหายจากกระแสเงินสด |
| **3.4** (โหมดดึงพอร์ตเข้าเป้า) | จุดที่สอง `_render_drift_advisory` (H7) — **แก้ตามแผนตรง ๆ จะหลุด** |
| **4 ①** (μ/σ ที่ป้อน MC) | ไม่มีเทสต์ตรึง `portfolio_mu_sigma` เลย (0-D) — จะแก้โดยไม่มีด่านตรวจ |

### 4.3 ข้อที่ **แผนเดิมเขียนตัวเลขผิด** ต้องแก้ข้อความในแผน

**(1) `goal_service` arithmetic mean — ตัวเลขในแผนสูงเกินจริง 7–10 เท่า และสลับทิศได้**

แผนเขียนว่า "สูงกว่า CAGR จริง 0.56 จุด/ปี ทำให้เงินออมที่ต้องการต่ำไป 8%" · วัดจริงกับพอร์ต 5 ETF ที่ระบบใช้:

```
real 5 ETF (น้ำหนัก 35/25/20/10/10, n=1459): mu=15.498% σ=14.26% CAGR=15.578%
  → mu − CAGR = **−0.080 จุด** (mu ต่ำกว่า ไม่ใช่สูงกว่า)
  → PMT เป้า 8 ล้าน/20 ปี = 4,979 vs 4,922 บาท/เดือน (**+1.2%** ออมมากไป ไม่ใช่น้อยไป)
proxy 16 ปี (n=3718): +0.053 จุด, PMT −0.7%
ตรวจสูตร: CAGR = exp(mu − σ²/2) − 1 = 15.58% ตรงเป๊ะ → ทิศทางขึ้นกับ σ เทียบ mu
เคสที่กลไกกัดจริง (σ > mu): VOO 100% +0.423 จุด (PMT −5.8%) · GLD 100% +1.107 จุด (PMT −12.9%)
probability_of_success ต่างกันแค่ 0.882 vs 0.887
```
สาเหตุ: ความผิดพลาดสองตัวหักล้างกันเอง — arithmetic > geometric ต่อวัน (ดันขึ้น ~σ²/2) ขณะที่ annualize แบบ ×252 แทนการทบต้น (กดลง)
**และห้ามเปลี่ยน input ของ `calculate_probability` เป็น geometric** เพราะ MC รับ arithmetic mean เป็น drift ซึ่งถูกต้องอยู่แล้ว — เปลี่ยนแล้วจะนับ volatility drag สองรอบ
เพิ่มเติม: ledger จริงว่าง → `real_portfolio_assumptions()` คืน `None` → ปัจจุบันเดินเส้น preset (7/9/12%) จึงยังไม่โดนกลไกนี้เลย
⇒ **แก้ข้อความในแผนเป็น "±0.08 จุด (สลับทิศได้ตาม σ vs μ)" และลดความสำคัญของ "แถม" ข้อนี้** · ผลกระทบจริงของเฟส 4 ① ยังอยู่ที่ 3 ข้อหลัก (ป้ายบอกช่วงข้อมูลไม่ตรง, μ อดีตถูกใช้เป็น μ อนาคต, normal iid) ซึ่งยังไม่ถูกหักล้าง

**(2) `ta_compat` RSI "ต่างกันสูงสุด 21.16 จุด" — วัดจากช่วงที่ระบบไม่ได้อ่าน**

```
ความต่างสลายตัวแบบ (13/14)^n: ต่ำกว่า 1 จุดหลังแท่งที่ 35 · ต่ำกว่า 0.01 จุดหลังแท่งที่ 93
ที่จุดอ่านจริงของระบบ (.iloc[-1]): n=120 → ต่าง 0.0087 จุด · n=250 และ n=504 → **0.0000 จุด**
ขณะที่ score_from_prices และ etf_service บังคับ ≥200 แท่ง และ screener ใช้ period='1y' (~250 แท่ง)
```
⇒ **ไม่มีคะแนน/ป้ายสัญญาณ/เงิน DCA ก้อนไหนเปลี่ยนจากเรื่องนี้** ควรลดขอบเขตในแผนเหลือเฉพาะ (ก) แท่งปลอมที่ iloc 13 (0/100 ปลอม) และ (ข) เส้นทาง `backtest_engine` ที่อ่านทั้งซีรีส์ (สุ่ม 200 ซีรีส์ 504 แท่ง → สัญญาณไม่ตรงกัน 92/200 เฉพาะช่วง ~60 แท่งแรก)
**และ `technical/indicators.calculate_rsi` ไม่ใช่สูตรที่สาม** — `np.allclose(_rsi_fallback, calculate_rsi)` = True, first valid iloc = 14 เท่ากัน (แต่ `get_signals()` ไม่มีผู้เรียกเลย = dead code)

**(3) หมวด "ยังไม่มีใครตรวจเลย" จัดลำดับผิด**

```
emergency_fund_service.py  59 stmt   5 miss  92%   (tests/test_emergency_fund.py 24 เทสต์)
debt_service.py            63 stmt  10 miss  84%   (tests/test_phase2.py เรียก _simulate/compare_methods ตรง)
cashflow_service.py       110 stmt  90 miss  18%   ← เปิดโล่งจริง
networth_service.py        51 stmt  38 miss  25%   ← เปิดโล่งจริง
report_service.py         134 stmt  89 miss  34%   ← เปิดโล่งจริง
```
⇒ ตัด `emergency_fund_service` และ `debt_service` ออกจากบรรทัดนั้น (สองตัวนี้มีตาข่ายอยู่แล้ว — แม้ `debt_service` จะมีบั๊ก H4 ก็ตาม เพราะเทสต์เลือกเคสที่ min คลุมดอกเบี้ยพอดี)
**หมายเหตุ**: ตัวเลข coverage ชุดนี้ผลิตซ้ำจาก image มาตรฐานไม่ได้ (ไม่มี `coverage`/`pytest-cov`) ใช้เป็นทิศทาง ไม่ใช่หลักฐานเด็ดขาด

**(4) เส้นฐานเทสต์ในแผนล้าสมัย** — แผนเขียน "347 passed, 1 failed ตอน mount / 348 หลังแก้ 1.8" ปัจจุบันคือ **572 passed** (mount + มีเน็ต) และ **0 passed** เมื่อไม่มีเน็ต

### 4.4 ข้อที่แผนเดิมตัดสินไปแล้วว่า "ทำแล้วไม่คุ้ม" — รอบนี้ไม่มีใครหักล้างได้

หัก expense ratio · premium/discount NAV · tracking error · realized TTM yield · ปรับสูตรคะแนนปันผล · ปรับจูน Prophet · ข่าว/sentiment เข้าคะแนน · risk-free 2% คงที่ — **คงไว้ตามเดิมทั้งหมด**
ข้อยกเว้นเล็ก: หัวข้อ "ปรับจูน Prophet" **ไม่ครอบ** D3.6 (freq='B') เพราะนั่นไม่ใช่การปรับความแม่น แต่คือการพยากรณ์วันที่ไม่มีอยู่จริง

---

## 5. สิ่งที่ยืนยันว่าสะอาด (ไม่ต้องกลับไปตรวจซ้ำ)

### 5.1 ความปลอดภัยและนโยบายค่าใช้จ่าย

- **ไม่มีเทสต์ตัวไหนเรียก LLM จริงเลย** — ติดตั้ง import hook (`sitecustomize.py` บน PYTHONPATH) ที่แทน `anthropic.Anthropic` ตัวจริงใน site-packages ด้วยคลาสที่ raise ทันทีเมื่อถูกสร้าง แล้วรันเต็มชุด → `572 passed` (tripwire ไม่ทำงานสักครั้ง) · ยืนยันว่า tripwire เองไม่ vacuous ด้วยการเรียกตรง → raise จริง และผ่านเส้นทาง `chat_text` → `RuntimeError` ครอบทั้ง `analysis/llm.py` และ slip OCR
- **`require_api_key` fail-closed ตามที่ CLAUDE.md อ้างทุกข้อ** — ไม่ตั้งคีย์: `127.0.0.1`/`::1`/`testclient` ผ่าน, `172.18.0.1` (bridge) และ `1.2.3.4` = **503** · ส่ง `X-Forwarded-For: 127.0.0.1` ปลอมจาก `1.2.3.4` **ยังได้ 503** (ใช้ `request.client.host`) · คีย์ที่เป็นช่องว่างล้วนถูก `.strip()` แล้วนับเป็นไม่ตั้ง → 503 · ตั้งคีย์แล้ว localhost ที่ไม่ส่ง header ก็ 401 (ไม่มีทางลัด) · `allowed_origins()` default ไม่ใช่ `*`
- **`/api/transactions` (slip OCR) อยู่หลัง `Depends(require_api_key)` จริง** — ยิงโดยไม่มี header ได้ 401 ทุกเคส
- **`POST /api/screener/run` และ `/api/etf/*` และ `/api/backtest` ป้องกันด้วย `include_ai=false` เป็นค่าเริ่มต้นจริง** — ยิงแล้วได้ `AI_DISABLED_MESSAGE` กลับมา · `build_ai_summary` ถูกเรียกโดยไม่ส่ง `user_initiated` → ตกไป `_plain_summary` ไม่มีค่าใช้จ่าย
- **ชุดเทสต์ไม่แตะข้อมูลจริงของผู้ใช้** — `md5sum` ของ `transactions.csv`, `price_alerts.json`, `config.json` ไม่เปลี่ยนก่อน-หลังรันชุดเต็ม 572 ตัว (ช่องโหว่ H1 อยู่ที่ SQLite เท่านั้น)
- **ไม่มี state รั่วข้ามไฟล์เทสต์** — รันทีละไฟล์ 40 ไฟล์แล้วบวกผลรวม = 568 เท่ากับรันรวม · รันสลับลำดับย้อนกลับ z→a = 568 เท่ากัน · fixture `_isolate_ttl_caches` ทำงานจริง (mutation ถอด `_is_cacheable` ถูกฆ่าโดย 13 เทสต์)
- **ไม่มีเทสต์ตัวไหนต้องใช้ฐานข้อมูลจริง** — รันด้วย `DATABASE_URL=""` ตลอด 44 รอบ mutation ไม่มี error เกี่ยวกับ Postgres เลย

### 5.2 invariant ของโปรเจกต์

- **ข่าว/sentiment/trend_channel ไม่ไหลเข้าเลขคะแนนหรือ DCA** — `grep -rn "news_fetcher|sentiment|trend_channel|lookthrough" analysis/financial_model.py portfolio/targets.py technical/ backend/screener/` = **0 บรรทัด** (ตรวจซ้ำ 3 รอบ)
- **สูตรค่าธรรมเนียมมีที่เดียวจริง** — `grep -rn "0.0015|DIME_FEE_RATE"` เจอเฉพาะ `portfolio/fees.py` เป็นผู้ประกาศ; `costs.py` และ `dashboard/app.py:3370` import ค่าคงที่จากไฟล์นั้น
- **`networth_service` ไม่อ่าน `default_fx_rate` ตรง ๆ** — ผ่าน `utils.fx` ตามกฎแหล่งเดียว (AUDIT.md M5 ปิดจริง) · และคูณ USD ด้วยอัตราแลกเปลี่ยน **ครั้งเดียว** ไม่มีการแปลงสองรอบ
- **ปันผลไม่เข้า cost basis จริง** — แถว dividend SCHD 862.5 บาทไม่กระทบ `Shares`/`Invested (USD)`/`Invested (THB)` และไม่ถูกนับเป็นเทรดในการประมาณค่าธรรมเนียม
- **การแปลงปันผล THB↔USD ไม่มีการแปลงสองรอบ** — `total_thb=862.5 / total_usd=25.0` ที่อัตราวันรับ 34.5 ย้อนกลับได้ตรง
- **หน่วยเงินและ timezone** — คอนเทนเนอร์ตั้ง `TZ=Asia/Bangkok` จริง (`time.tzname=('+07','+07')`, `ZoneInfo('Asia/Bangkok')` ไม่ raise) ⇒ `schedule.every().day.at("08:00")` ยิงตามเวลาไทย · cron mapping ใน workflow ถูกทุกบรรทัด · `_effective_dca_day()` จัดการเดือนสั้นถูก

### 5.3 โมดูลที่ตรวจแล้วสะอาด

- **`backend/services/emergency_fund_service.py` ทั้งไฟล์** — clamp 0–100 ถูก, `_MULTIPLIER_TABLE` ครอบทุกช่วง, ไม่มีการหารด้วยศูนย์ (`if gap <= 0 or monthly_saving_capacity <= 0: months_to_goal = None` กันก่อนหาร), `math.ceil` ถูกทิศ, ข้อความไทยครบ, 24 เทสต์คุมขอบทุกจุด
- **`debt_service` ด่าน fail-loud 2 ด่านทำงานจริง** — งบต่ำกว่าผลรวม min_payment → `ValueError` · ชนเพดาน 600 เดือนแล้วยังมีหนี้ → `ValueError` (ไม่รายงานว่าจ่ายหมด) · ตรรกะการจัดลำดับถูกต้องทั้ง avalanche/snowball · `months_to_payoff` ตรงกับ simulator อิสระเป๊ะทุกเคส · `routers/debt.py` แปลง `ValueError` → **422** พร้อมข้อความไทย (เป็นแบบที่ router อื่นควรทำตาม)
- **`backend/services/cache_service.py`** — import `is_cacheable` จาก `utils/cache.py` ตัวเดียว ไม่มีนิยามที่สอง · `expect_keys` ทำงานถูกตามสเปก (`ไม่แคช p: ได้ข้อมูลไม่ครบ ขาด GLDM`) · ผลบางส่วนไม่ถูกแคช · `copy.deepcopy` ทั้งขาเข้าขาออก (แก้แล้วรอบสองยังได้ค่าเดิม) · ชนิดที่ตรวจไม่ได้ = ไม่แคช (fail-closed) · **คีย์ไม่ชนกันข้าม ticker/ช่วงเวลา** (`prices_10y:` / `prices_11y:` / `latest_prices:` แยกกัน)
- **`utils/cache.py`** — exception ถูกเรียกซ้ำ 3/3 ครั้ง · `{"data_ok": False}` ซ้ำ 2/2 ครั้ง · ผลสำเร็จเรียกจริงครั้งเดียวและผู้เรียกแก้ list ที่ได้แล้วไม่ทำแคชสกปรก
- **`data/fallback.py`** — ticker ที่ดึงไม่ได้หายจาก dict (ไม่มี 0.0 แทรก) · Stooq ปฏิเสธ `N/D` · ไม่ได้ราคาเลย → `PriceDataUnavailableError` · log ระบุ ticker ที่ยังขาด
- **`analysis/returns.period_return_pct`** — `not (start > 0)` ดัก NaN ได้จริง (ต่างจาก `start <= 0`) · ข้อมูลสั้นกว่าหน้าต่างคืน NaN · ไม่มี `or 0.0`
- **`analysis/financial_model.score_from_prices`** — โมเมนตัมที่คำนวณไม่ได้ใช้ `mom_s if mom_s is not None else 0` คู่กับ `mom_max = 0` (ตัวหารหดตามพร้อมกัน) ไม่ใช่ `or 0` · `_dividend_score` ถูกตัดจาก `max_score` เมื่อไม่มีข้อมูล
- **`technical/signal_rules.py`** — `_valid()` ดัก None/NaN/แปลง float ไม่ได้ ครบ 3 ทาง · `dca_signal()` คืน `NO_DATA` เมื่อ price/ma200/rsi ตัวใดใช้ไม่ได้
- **`backend/services/rebalance_service` เส้นทาง fail-closed เมื่อราคาขาด** (การแก้เฟส 1.3) — `_usable_price()` แยก "ไม่มีคีย์" จาก 0/NaN/ติดลบ · `calculate_drift()` raise แทนคืน 1.0 · `_no_plan()` คืน `needs_rebalance=None` + `missing_prices` + `detail` ภาษาไทย HTTP 200 · `values.get(sym, 0.0)` ปลอดภัยเพราะผู้เรียกคัดกรองแล้ว (0 = "ยังไม่ถือกองนี้" จริง)
- **`portfolio/tracker._load_transactions`** (การแก้เฟส 1.2) — fx นอกช่วง 20–50 ถูกตีเป็นใช้ไม่ได้ · คำนวณอัตราจริงย้อนได้ · ที่เหลือปล่อย NaN ให้ `dropna` ทำงานแล้วรายงานผ่าน `.attrs['skipped_rows']` · ticker ว่าง/NaN ไม่กลายเป็นสินทรัพย์ผีชื่อ `'NAN'` · **ไม่มี `fillna(DEFAULT_USDTHB)` เหลืออยู่**
- **`portfolio/benchmark.xirr`** — แกนคณิตถูก (`0.1000718113939972` มาจาก convention `_DAYS_PER_YEAR = 365.25` ที่ตั้งใจ ไม่ใช่บั๊ก) · ตัวกรอง NaN/inf ด้านหน้าทำงาน
- **`simulate_dca_dynamic` ไม่มี look-ahead bias** — `history = frame.loc[frame.index < buy_date]` ตัดที่วันซื้อพอดี และแขน plain/tilt ใช้กรอบราคาเดียวกันเป๊ะ (179/179 และ 70/70 เดือน วันซื้อตรงกันทุกเดือน) ⇒ **คำตัดสิน `tilt_beats_plain` ที่ Roadmap ใช้ปิด Phase 1 ไม่ได้รับผลกระทบ**
- **`calculate_probability` (Monte Carlo)** ป้อน arithmetic mean เป็น drift ซึ่งถูกต้องตามหลักสถิติ — **ห้ามเปลี่ยนเป็น geometric**
- **หน้า News (เส้นทางฟรี)** — `cached_news()` โยน `NewsSourcesUnavailable` เมื่อแหล่งจริงล้มหมด (Streamlit ไม่เก็บผลของ call ที่ throw) และ `render_news_page()` แสดง st.error + "ยังไม่ทราบว่ามีข่าวหรือไม่" ไม่ใช่ "ไม่มีข่าว"
- **`alerts/line_notifier.py`** — ไม่ตั้ง token คืน `{"skipped": True}` · ส่งพลาดไม่เงียบ (`raise_for_status()` + คืน `{"success": False, "error": ...}` แล้ว `main.py:86` พิมพ์ออกมา) · ไม่มี token รั่วในสตริง error
- **`backend/routers/websocket.py` ทำถูกเรื่อง event loop** — `await asyncio.to_thread(...)` และดึงไม่ได้คืน `None` · `asyncio.Lock` กันสร้าง task ซ้ำ
- **route ของ `backend/routers/etf.py` เป็น `def` ธรรมดาทั้ง 6 ตัว** → FastAPI โยนเข้า threadpool ไม่บล็อก event loop
- **`data/fetcher.fetch_adjusted_close_data` ไม่มีบั๊กขอบวันจากเวลาไทย** — `end` เป็น exclusive และเซสชันสหรัฐที่ปิดตอน 03:00–04:00 ยังอยู่ในผลตอนรัน 07:00 น.
- **`json_safe.py`** แปลง NaN/inf/Timestamp เป็น null/ISO ได้จริง — endpoint ที่ผ่านชั้นนี้ไม่ 500
- **`portfolio_service._clean()` และ `_skipped_report()`** — NaN/±inf → None (ไม่ใช่ 0 ไม่ใช่ 500) และดึง `.attrs['skipped_rows']` ออกมา **ก่อน** `to_dict()` จริง
- **openapi ฝั่ง request ไม่โกหกสักเส้น** — generate body จาก schema แล้วยิง POST/PUT ทุกเส้น (ยกเว้น 4 เส้นทาง LLM) ได้ 200/201/400/503 ตามความหมายจริง **ไม่มี 422 สักเส้น** ⇒ บั๊ก 1.7 (route ซ้ำ) ปิดสนิทแล้ว · `app.routes` 56 เส้น ไม่มีคู่ `(path, method)` ซ้ำ
- **`POST /api/portfolio/rebalance`** ทำงานถูกทั้งสองทาง (ราคาครบ → actions ครบ 5 · GLDM หาย → `needs_rebalance:null` + `missing_prices` + `detail` ไทย + HTTP 200)
- **ฟิลด์ใหม่จากเฟส 1.5** (`momentum_score` เป็น None ได้, `momentum_available`, `return_1m_pct`/`return_3m_pct`) มีผู้บริโภครับมือครบทุกจุดใน dashboard
- **เทสต์ชุดใหม่ 10 ไฟล์ผ่าน 231/231** และเต็มชุด 572/572 ตอน mount ทับ

---

## 6. ยังไม่ได้ตรวจ / ตรวจไม่ได้

### 6.1 ตรวจไม่ได้เพราะข้อห้าม (ต้องให้ผู้ใช้ทำเอง)

| เรื่อง | ต้องใช้อะไร |
|---|---|
| **Sonnet 5 เปิด adaptive thinking เป็นค่าเริ่มต้นหรือไม่** | `_chat_anthropic` ไม่ส่งฟิลด์ `thinking` และ `max_tokens` เป็นเพดานรวมของ thinking + ข้อความ ขณะที่ผู้เรียกตั้ง 500–1500 → **มีความเป็นไปได้ที่ทุกคลิกจะชน `stop_reason=max_tokens` แล้ว retry ที่ 2× (จ่ายสองรอบ) หรือจบด้วย `RuntimeError` ตามด่านที่เพิ่งเพิ่มในเฟส 1.1** · พิสูจน์ไม่ได้เพราะต้องยิง provider จริง — **ควรวัดด้วยการยิงครั้งเดียวโดยผู้ใช้เอง** |
| ความเข้ากันได้ของ `anthropic==0.42.0` (SDK ธ.ค. 2024) กับ request shape ของ `claude-sonnet-5` | ต้องเรียก API จริง |
| พฤติกรรมจริงบน Render (ephemeral disk, cold start) | ไม่มีสิทธิ์ deploy — ข้ออ้างเรื่อง ephemeral disk มาจาก AUDIT.md:102 ที่โปรเจกต์บันทึกไว้เอง + ข้อเท็จจริงว่า `render.yaml` ไม่มีบล็อก `disk:` |
| ข้อความ Discord ซ้ำ 2–4 ฉบับ/วัน (CI + scheduler ในเครื่องยิงเว็บฮุคเดียวกัน) | ต้องยิง webhook จริง — จาก workflow + `main.py` เห็นว่าวันจันทร์ 08:00 ทั้งสองฝั่งส่ง weekly summary และวันทำการ CI ยิง 2 ใบ + scheduler อีก 2 รอบ **แต่ไม่รายงานเป็นข้อค้นพบเพราะพิสูจน์ไม่ได้** |
| `.env` มี `ANTHROPIC_API_KEY` จริงหรือค่าอะไร | คำสั่ง dump env ถูกบล็อก (สมเหตุสมผล) — อนุมานจาก FIX_PLAN + หลักฐานทางอ้อมว่า `VAULTIS_API_KEY` ถูกโหลดเข้าคอนเทนเนอร์ tests (ได้ 401 แทน 503) |
| ความถี่จริงที่ `_fetch_live` ของ FX ล้มเหลว | ต้องเก็บ log ระยะยาวจากคอนเทนเนอร์ที่รันอยู่ |

### 6.2 ตรวจไม่ทัน (คิวถัดไป)

**โมดูลที่ไม่มีใครแตะเลยรอบนี้**
`analysis/backtester.py` (WalkForwardBacktester) · `analysis/forecast_chart.py` · `analysis/sentiment_prompt.py` · `portfolio/edge_lab.py` · `backend/routers/{goals,reports,rebalance,etf}.py` · `db/sentiment_models.py` · `backend/services/goal_service.py` (ทับกับเฟส 4 ① ของแผน)

**ข้อสังเกตที่เห็นแต่ไม่ได้พิสูจน์ จึงไม่นับเป็นข้อค้นพบ**

- `analysis/backtester.py` มี `except Exception: pass` ต่อ fold → fold ที่ล้มเหลวหายเงียบ เหลือแค่ `n_folds` ที่ต่ำลงโดยไม่มีใครรู้ว่าเป็นเพราะข้อมูลไม่พอหรือโมเดลพัง (การแบ่ง train/test ดูถูกจากการอ่านโค้ด แต่ไม่ได้วัด)
- `report_service._send_telegram` ทิ้งผลของ `requests.post` ไม่ตรวจ HTTP status เลย → Telegram ตอบ 400 (เช่น `parse_mode=Markdown` เจอ `*`/`_` ไม่จับคู่จากข้อความ LLM) จะทำให้รายงานไม่ถูกส่งโดย `generate_and_save_report` ยังคืนผลว่าสำเร็จ
- `backend/routers/cashflow.py:20` `_stored_transactions` เป็น list ระดับโมดูล = store ชุดที่ 5 ที่ไม่มีในตาราง "One store per kind of data" ของ CLAUDE.md, แชร์ข้ามทุกคำขอในโปรเซส, หายเมื่อรีสตาร์ต
- `goal_service.real_portfolio_assumptions()` แคชผลลัพธ์ `None` (ความล้มเหลว) ไว้ 10 นาที และป้าย `assumptions_source` รวม "ดึงราคาไม่ได้" เข้ากับ "ยังไม่มีพอร์ตจริง" เป็นข้อความเดียว
- `goal_service.real_portfolio_assumptions()` ใช้น้ำหนักเป็นบาทแต่ μ/σ มาจากซีรีส์ราคา USD (ความเสี่ยงค่าเงินหายจาก MC ของเป้าหมายที่ตั้งเป็นบาท)
- `ScreenerHistoryService.__init__` สร้าง `create_engine` ใหม่ทุกครั้งที่ instantiate โดยไม่ `dispose()` — สงสัยว่ารั่ว connection pool แต่ยังไม่ได้วัดบน Postgres จริง
- `analysis/macro.get_macro_data` ใช้สำนวน `round(_to_float(x) or 0.0, 2)` บน Fed funds / CPI — ยังไม่ได้ไล่ว่า `_to_float` คืน `None` หรือ `NaN` เมื่อข้อมูลพัง จึงตัดสินไม่ได้ว่าเป็นการกุ 0.00% หรือไม่
- `analysis/correlation.calculate_correlation()` + `get_correlation_insight()`: ticker ที่ yfinance ไม่คืนจะถูก `reindex` เป็นคอลัมน์ NaN ล้วนแล้วหายจากประโยคสรุปเงียบ ๆ — **ไม่รายงานเพราะเป็นโค้ดตาย** (เส้นทางจริงคือ `calculate_correlation_matrix`)
- `portfolio/backtest.run_backtest()` สร้าง benchmark curve จากช่วงที่ยาวกว่าพอร์ต แล้วค่อย intersect ทีหลัง — **ไม่รายงานเพราะเป็นโค้ดตาย** (ผู้เรียกจริงคือ `run_portfolio_backtest`)
- `st.cache_data` ของ dashboard (`cached_full_analysis`, `cached_etf_scores`) **แคชผลที่มี `data_ok=False` ได้** ต่างจาก `utils/cache` — น่าจะทำให้ "ดึงไม่ได้" ค้าง 1 ชม.บน Scorecard แต่ต้องรัน Streamlit server จริง 2 session ถึงจะพิสูจน์
- การเขียน `portfolio/data/transactions.csv` พร้อมกันจาก 2 โปรเซส: `add_transaction`/`add_dividend` ใช้ append (ค่อนข้างปลอดภัย) แต่ `_ensure_storage()` และ `delete_transaction()` เขียนทับทั้งไฟล์แบบไม่ atomic **เหมือนกลไกของ H2 แต่ผลคือธุรกรรมหาย** — สร้างเงื่อนไขให้แข่งกันแบบ deterministic ไม่ทัน และห้ามแตะสมุดจริง **แนะนำให้ตรวจรอบถัดไปโดยเฉพาะ**
- SQLite `vaultis.db` ที่ backend + scheduler + dashboard เปิดพร้อมกันผ่าน bind mount เดียว — ไม่ได้ตรวจว่าเปิด WAL หรือตั้ง `timeout` ไว้ไหม (พบร่องรอย `OperationalError('database is locked')` ใน log ของ yfinance ระหว่างตรวจ)

**ข้อจำกัดเชิงวิธี**

- วัดเฉพาะ **line coverage** ไม่ได้วัด branch coverage — โมดูลที่ขึ้น 84–95% อาจยังมีสาขาที่ไม่เคยเดิน (ตัวอย่างจริง: `portfolio/benchmark.py` 87% แต่ทิศ "ปฏิเสธ" ของ guard ใน `xirr` ไม่เคยถูกรัน)
- ไม่ได้ mutate `dashboard/app.py` (3,700+ บรรทัด Streamlit) ทั้งที่เป็นที่อยู่ของบั๊กเงินที่รุนแรงที่สุดในแผน — โครงเทสต์ปัจจุบันเป็น monkeypatch ระดับ module ซึ่งทำ mutation testing ได้ยาก **ควรให้รอบถัดไปตรวจเฉพาะทาง**
- ยังไม่ได้ mutate: `backend/routers/*` ทั้งหมด, `report_service`, `networth_service`, `cashflow_service`, `news_fetcher`, `sentiment_analyzer`, `ai_advisor`, `pdf_export`
- ไม่ได้ตรวจ flakiness เชิงเวลา/สุ่ม (ไม่มี `pytest-randomly` ในภาพ สลับได้แค่ระดับไฟล์)
- ไม่ได้ยืนยันเชิงรูปนัยว่า mutation ที่ "รอด" 4 ตัว (ขอบ `p >= m200`, M22, M30, M31) เป็น equivalent mutant จริงหรือไม่ — จากการอ่านโค้ดทั้ง 4 ถูกดักซ้ำที่ชั้นถัดไป

---

## 7. ลำดับที่แนะนำ

```
1. เฟส 0     → 0-A (compose) ต้องทำก่อนใครแตะอะไร · 0-B/0-C/0-D ทำขนานกันได้
2. เฟส A     → 9 สายขนาน (A1 price_alert · A2 report_service · A3 debt_service ·
                A4 pdf_export · A5 networth · A6 etf_service · A7 workflows/jobs/main)
3. เฟส B     → 11 สายขนาน (B9 fx ต้องรอ A5 + C1)
4. เฟส C     → ทีละคน: C1 tracker.py ก่อน แล้ว C2 dashboard/app.py
                (เฟส 3 ของ FIX_PLAN เดิมแตะไฟล์เดียวกัน — ควรรวมเป็นงานก้อนเดียว)
5. เฟส D     → ขนานได้ทั้งหมด
6. ปิดงาน    → รันเต็มชุด (ต้อง ≥ 572 และต้องผ่าน **แบบไม่มีเน็ต** หลังแก้ 0-B) ·
                grep invariant ข่าว/sentiment/lookthrough = 0 ·
                docker compose build && up -d · อัปเดต CLAUDE.md/README/AUDIT.md/render.yaml
```

**ข้อควรระวังเรื่องการรวมกับ FIX_PLAN เดิม**: เฟส 2–5 ของแผนเดิมยังไม่ได้ทำเลย และหลายข้อในเอกสารนี้แตะไฟล์เดียวกัน — **ควรรวมสองเอกสารเป็นคิวเดียวก่อนเริ่ม** ไม่ใช่ทำสองแผนขนานกัน มิฉะนั้นจะชนกันที่ `dashboard/app.py`, `tracker.py`, `screener/engine.py`, `dca.py`, `daily_check.py`

---

## 8. สิ่งที่ต้องให้ผู้ใช้ตัดสินใจ

1. **ข้อมูลขยะ 2 แถวใน `.docker-data/vaultis.db`** — ลบทิ้ง หรือเก็บไว้เป็นหลักฐาน (ผู้แก้โค้ดไม่ควรตัดสินใจแทน)
2. **ยิง LLM ครั้งเดียวเพื่อวัดเรื่อง adaptive thinking ของ Sonnet 5** (ข้อ 6.1) — มีค่าใช้จ่ายจริง แต่เป็นสิ่งเดียวที่จะบอกได้ว่าปุ่ม AI ทุกปุ่มกำลังจ่ายซ้ำอยู่หรือไม่
3. **นิยามของ `amount_thb`** (รวมค่าธรรมเนียมหรือไม่) — โค้ดกับ fixture เดิมตีความคนละแบบ และไม่มีข้อมูลในระบบตัดสินได้ ต้องให้เจ้าของสมุดบัญชีชี้ขาด (ข้อ 3.3 / C1.2)
4. **ระดับความรุนแรงที่ขัดกัน 3 ข้อ** (H9, H10, H11) — ทั้งสามเป็น "ตัวเลขผิดที่ยังไม่มีหน้าจอเรียกใช้" ผู้ใช้ต้องบอกว่าจะจัดคิวตามความรุนแรงเชิงทฤษฎีหรือตามผู้ใช้งานจริง
5. **จะเพิ่ม CI (`tests.yml`) หรือไม่** — ต้องแก้ 0-B ก่อน และจะทำให้การ push ทุกครั้งใช้เวลา ~1 นาที
6. **`render.yaml` และ README** ยังโฆษณา Groq/Google GenAI บน repo สาธารณะ — จะแก้ตอนนี้หรือรอปิดงานทั้งหมด

---

*เอกสารนี้เป็นผลตรวจ ไม่ใช่บันทึกการแก้ไข · ทุกบล็อกโค้ดคือผลลัพธ์จริงจากคำสั่งที่รันในคอนเทนเนอร์ `tests` โดย mount โค้ดจาก host ทับ `/app` · ข้อที่ระบุว่า "ยังไม่ผ่านการตรวจซ้ำ" ให้ถือว่าหลักฐานอ่อนกว่าข้ออื่นและควรพิสูจน์ซ้ำก่อนแก้*
