services:
  # Redis ถูกถอดออก (AUDIT.md H3) — ไม่มีโค้ดเรียกใช้เลย; cache เป็น in-process TTL
  # (utils/cache.py + backend/services/cache_service.py)
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
